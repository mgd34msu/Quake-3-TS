# Recovery checkpoint

## Resumed functional work: September 10, 2026

Current follow-up supersedes the optimization-only status below. The dependency graph stays complete; do not reopen or relabel its milestones. Newly discovered work belongs to PARITY_PUNCH_LIST.md. pk3_byte_names_fix owns live src/assets/pk3.ts, src/assets/vfs.ts and its assigned tests; before/candidate copies are in /tmp/quake3-pk3-byte-fix-JcAtL7. download_byte_paths owns download-file.ts and its test; writable_byte_paths owns writable-files.ts and its test. Raw game-directory/archive-basename integration remains separate work. The original PK3 comparator has passed in worker and root runs at /tmp/quake3-byte-names-reference-aXZiGk. The QVM weapon test correction is reviewed and root-rerun, 12 cases/77 assertions; no production QVM change. survey_source_options and survey_core are read-only punch-list surveys. Root owns docs and integration. The optimization investigations finished without a justified candidate. Published dist/qfiles remain 21:47. Original isolated comparisons are authorized; physical devices remain excluded.

Current live ownership supersedes the older checkpoints below. The goal remains active. The accepted 21:47:20 UTC executable is published to dist and qfiles/q3a, 104,454,272 bytes with equal copies. It adds reached serial nearest-mip certification and one dlight UV-only capture call to 20:39. Typing, policy, build-input consistency and all four compiled gameplay/recovery cases pass. Eight recordings contain 216 active snapshots. Root inspected logs, decoded recordings, all four post-restart images and source/test agreement with build-FaTr3P before the next experiment. Executable, metadata, unchanged runner and explicit generated evidence are recoverable in `.artifacts/releases/2026-09-10-2147/`. Previous releases remain intact. No retail assets, keys or user configuration were archived.

The public numeric cursor extension is rejected and restored. Its first raw base-client median improves only 10.5 to 10.6 FPS, with overlapping ranges, insufficient for 57 added and 23 removed lines. Root and independent review found no defect; the private comparison passes 3,927 cases and existing CPU checks pass 34 cases/1,249 assertions. Both game processes are gone. Root confirmed all source and tests again match build-FaTr3P. Exact source, comparisons and runtime results are archived in `.artifacts/optimizations/2026-09-10-post-1939/rejected-public-cursor/`. No live source experiment or runtime owns the tree. cpu_dispatch_cost, cpu_texel_cost and gl_source_capture have separate read-only optimization investigations.

The 20:39 compiled timedemos completed serially: CPU/GL medians are 9.8/39.7 for Quake 3 and 9.1/30.1 for Team Arena. All processes exited zero with empty stderr and are gone. Executable and runner remain unchanged. Root inspected actual results; explicit logs, results, report and unchanged runner are archived under the release's performance directory. Raw evidence is `/tmp/quake3-2039-performance-eGzXAz/`. Previous 19:39 medians were 9.9/38.8 and 9.0/28.7. These sequential observations do not isolate an optimization's effect.

The updated CPU-worker profile completed with unchanged helpers, exact color/depth and 1,280 measured jobs. PID 1121025 exited zero and is gone. Sampling still dominates: sampleLevel 266, samplePerspectiveBound 182 and sampleWorkerNearest 101 of 788 self samples. These are execution samples, not whole-frame FPS or proof of physical call overhead. Evidence is `.artifacts/optimizations/2026-09-10-post-1813/worker-profile/`.

The four-line GL index-copy removal was rejected. Its first pair worsened median 1.65%, p95 2.59% and CPU work 0.23%, with exact RGBA. Root's actual GL/primitive selection passed 27 cases/228 assertions and independent review found no defect, but there was no performance benefit. renderer.ts and primitives.ts are restored exactly. Evidence is `.artifacts/optimizations/2026-09-10-post-1813/rejected-gl-index-alias/`.

The worker cursor's base-dimension hoist was also rejected. Its first median changed only -0.035%, with exact color/depth and work counts. No reverse or Team Arena pair followed. Root reviewed the Astra-low worker's exact four edit locations and passed 34 existing mipmap/worker cases before measurement. The kernel was restored and root cmp passed before the next candidate. Evidence is `.artifacts/optimizations/2026-09-10-post-1813/rejected-cursor-dimensions/`.

The eight-line worker non-mipmap dispatch was rejected. Its first median worsened 2.30% and CPU work rose 4.14%, with exact output and work counts. No reverse or Team Arena pair followed. Production and the one-line test extension are restored exactly. Root confirmed all source and tests again match build-LhMTDH. Evidence is `.artifacts/optimizations/2026-09-10-post-1813/rejected-worker-nonmip/`.

The worker dead-alpha and GL unused-zero-UV scan candidates were rejected. CPU median worsened 2.27% and CPU work rose 3.86%. GL median changed only -0.188% with CPU work 0.80% higher. Both preserved exact measured output. No reverse or Team Arena comparisons followed. Their production files and candidate-only CPU test extension are restored exactly. Root confirmed all source and tests again match build-LhMTDH. Evidence is `.artifacts/optimizations/2026-09-10-post-1813/rejected-dead-alpha/` and `rejected-unused-uv/`.

The early squared mip-cache hit was rejected after median time rose 4.92% and CPU work rose 3.70%, with exact output. No reverse or Team Arena pair followed. Kernel and candidate-only tests are restored exactly. Root confirmed source and tests again match build-LhMTDH. Evidence is `/tmp/quake3-early-mip-hit-vwdxUs/` and `/tmp/quake3-early-mip-hit-measure-cyvEvA/`. The bounded botlib formatting check found no missing behavior in its assigned calls.

Source retains the GL begin-source-arrays validation-array removal. Matched medians improve 3.25% and 0.36%, with mixed p95; the reverse result is noise-sized, so this is retained as a small allocation removal without an isolated FPS claim. Evidence is `.artifacts/optimizations/2026-09-10-post-1813/gl-position-validation/`. Root reviewed the low-effort worker's exact diff and passed 64 combined CPU/actual-GL cases with 1,519 assertions. The added worker sparse/dense/sparse selection passes seven cases/243 assertions; typing and policy pass.

The empty-band candidate is rejected and its production/test changes are restored exactly. Base had no empty jobs and no meaningful timing change. Team Arena removed 3.125% of jobs and 8.744% of transmitted setups, but medians changed -1.286% and +2.343% in opposite orders. Exact output does not establish a repeatable speed benefit. Evidence is `/tmp/quake3-empty-bands-HbfsOn/` and `/tmp/quake3-empty-bands-measure-nBrr82/`.

The full-width copy candidate is rejected and restored exactly. Median time rose 1.19% and CPU work rose 3.27%, with identical output and counts. No reverse or Team Arena pair followed. Root reviewed the nine-line low-effort edit and passed 34 existing CPU cases. Evidence is `.artifacts/optimizations/2026-09-10-post-1813/rejected-bulk-copy/`.

The 65,536 dispatch-threshold candidate is rejected and the original 32,768 threshold restored exactly. It moved four base draws to serial rendering, halving worker jobs, but worsened median time 13.06% despite 1.99% less CPU work. Output remained exact; no reverse or Team Arena pair followed. No tests changed. Evidence is `/tmp/quake3-dispatch-threshold-blzdAu/` and `/tmp/quake3-dispatch-threshold-measure-Dbi9Uv/`.

The per-message texture-validation cache is rejected and production restored exactly. Median time rose 1.31%, with CPU work -0.23% and exact output/counts. No reverse or Team Arena pair followed. Evidence is `.artifacts/optimizations/2026-09-10-post-1813/rejected-decoder-reuse/`. No cache remains in production.

Root reproduced a fixture defect: installed Bun 1.3.14 structuredClone converts SharedArrayBuffer to ArrayBuffer. Old valid/invalid comparisons could reject at the shared-buffer requirement without exercising their intended fields. Corrected synthetic records retain checked shared views and assert strict acceptance first. The minimal fix is retained in the 19:39 snapshot: fixed-baseline-test.ts contains it alone, while cpu-execution.join.test.ts contains the rejected candidate's full cases. All copies/logs are in the same private directory. Actual worker transport remains successful and does not use global structuredClone. No physical devices are authorized.

The 19:39 integration contains only renderer.ts production changes since 18:13: one position-validation helper and six equivalent call-site replacements. Root reviewed all edits; five call sites have allocation evidence only, not separate timing claims. The minimal CPU fixture fix and six GL getter-order assertions are the only test differences. Root's joined existing selection passes 137 cases/2,225 assertions, including actual GL debug and stencil behavior. Build and all four gameplay processes have finished. Logs are `/tmp/quake3-joined-release-7zg7eg/`; all owned game PIDs are gone.

The twelve-line CPU texel-endian experiment is rejected and restored exactly. Root reviewed the eight DataView endian arguments and four channel expressions, and passed 68 existing CPU cases with 1,394 assertions. The first matched median improves only 0.814%, while p95 worsens 2.410% and combined CPU work falls 2.165%. Color, depth and work counts match. No reverse or Team Arena pair followed. Evidence is `.artifacts/optimizations/2026-09-10-post-1939/rejected-texel-endian/`. Root confirmed source and tests again match build-fT8ZQj.

The fresh GL profile finished with one unchanged runner/analyzer invocation, exit zero, empty stderr and a reaped child. It confirms 918.570 sampled ms under tess append and identifies a separate 769.799 ms journal-mode freeze node. Loading and frame costs remain distinguished. Evidence is `.artifacts/optimizations/2026-09-10-post-1939/gl-profile/`.

Source now retains two small lookup changes: CommonJournal.mode reads an immediate cvar reference instead of allocating a snapshot, and thirteen renderer runtime lookup literals use canonical lowercase keys. Root reviewed both low-effort workers' complete diffs, canonical storage and source call order. Existing journal checks pass 13 cases/151 assertions; settings pass 35/383. Six unprofiled raw GL timedemo processes passed, with mixed FPS and no robust isolated gain. Base medians are 39.5/39.3 and 39.6/41.2 in opposite orders; TA is 29.6/28.9. These are allocation reductions without an FPS claim. Evidence is `.artifacts/optimizations/2026-09-10-post-1939/cvar-lookups/`. Published dist/qfiles remain 19:39.

Private tessellation-cell reuse is rejected. The first GL comparison worsens median 6.88% and combined CPU work 4.99%, with exact RGBA. No reverse or CPU expansion followed. Root restored tess-state.ts and its three candidate-only tests exactly. Evidence is `/tmp/quake3-tess-cell-measure-M8sg0w/`; the report's candidate-live disposition is superseded by this restoration. Baseline and candidate had identical six diagnostic failures, including assertion values. Root corrected the existing debug cvar observer to intercept find and passed the requested stencil precision into the existing GL fixture. The restored combined selection now has 136 passes and one unchanged strict retail-MD3 CPU/GL comparison failure, maximum32 against2. No tolerance was changed. Logs and before-copies are `/tmp/quake3-tess-cell-implementation-ocZyy0/` and `/tmp/quake3-debug-cvar-observer-cGosRR/`.

Source retains the seven-line tessellation stage-attribute change after root reviewed its complete diff and consumers. It removes unused normal/color snapshots while retaining separate detached position/UV and full active-geometry snapshots. Typing/policy pass. The combined renderer selection remains 136 passes and the identical MD3 diagnostic failure; picture-material has 27 passes and three optional skips. GL median changes +0.77%; CPU base/TA medians are effectively unchanged, with exact color/depth and work counts. This is an allocation reduction without an FPS claim. All six measurement processes exited and are gone. Evidence is `.artifacts/optimizations/2026-09-10-post-1939/tess-stage-attributes/`. Published dist/qfiles remain 19:39.

The general nearest-mip decision-box experiment is rejected and its kernel/test changes are restored exactly. Independent numeric/code review was clear; root's extended baseline comparison passed seven cases/283 assertions and candidate selections passed 73 cases/1,436 assertions with six optional GL skips. Typing and policy passed. The first real CPU pair improves median only 0.48%, leaves combined CPU work unchanged and worsens p95 1.79%. Exact pixels, depth and work counts match. No reverse or Team Arena run followed. Evidence is `/tmp/quake3-nearest-box-measure-Wa4YHE/`; preimages are `/tmp/quake3-nearest-box-iC0xZI/` and `/tmp/quake3-cpu-lod-box-cases-ggUcKW/`. The earlier affine-only proposal was rejected before coding because real workload eligibility was unestablished. Do not repeat either without new evidence.

The subsequent build and four compiled checks are accepted as 20:39 above. The copyStoredVertex experiment is rejected and restored. GL medians improve 2.78% and 2.89%, but Quake 3 CPU median worsens 5.23%; Team Arena CPU improves 3.38%. All measured color/depth bytes and work counts match. Existing ownership/deformation/GL checks, typing and policy pass except the identical known MD3 diagnostic. Evidence is `.artifacts/optimizations/2026-09-10-post-1939/rejected-stored-copy/`. No candidate tests were added. Root confirmed source and tests again match build-X697ws.

The unchanged CPU client profile completed and its process is gone. It attributes 8,462.214 sampled ms to captured serial fallback, including 4,939.623 ms under sampling. Native executor wait is 11,887.407 ms, not active CPU time. Root inspected actual logs, results and ancestry. Evidence is `.artifacts/optimizations/2026-09-10-post-1939/cpu-main-profile/`. This does not identify fallback reasons or establish an FPS gain.

The GL placeholder-copy candidate was rejected and restored exactly: first median improves only 0.69%, p95 worsens 4.06% and CPU work increases. Existing actual GL checks pass 30 cases/273 assertions; typing and policy pass. Evidence is `.artifacts/optimizations/2026-09-10-post-1939/rejected-gl-placeholder/`. Coarse-depth rejection was stopped during grounding because current early-depth rejection already excludes sampled fragments and worthwhile block-occlusion coverage is unestablished.

The 16,384 dispatch threshold is rejected and the original 32,768 restored exactly. Quake 3 actual-client medians improve 9.8 to 10.1 and 10.0 to 10.1 FPS in opposite orders, but Team Arena changes 9.1 to 9.0 and 9.0 to 8.9. The static render-pass difference is noise-sized with more CPU work. All eight actual processes exited zero with empty stderr and are gone. Root read the results and confirmed source/tests matched build-X697ws after restoration. Explicit report, before/candidate, helper and generated logs are archived in `.artifacts/optimizations/2026-09-10-post-1939/rejected-lower-threshold/`. No tests or safeguards changed.

Both captured-fallback ownership sketches were rejected: injected execution methods can expose or alter records before the proposed eager sampler reads, changing depth-rejected behavior. Neither is implemented; `/tmp/quake3-captured-fallback-design-uectSpXH/SYNTHESIS.md` records the decision. Reached sampling instead preserves the original public reads and applies the existing numeric bound afterward. Root and independent source/numeric review found no defect within the documented Bun/Linux math profile. Root passed 2,566 frozen-baseline comparisons, including derivative getter tapes, and 34 existing CPU cases/1,250 assertions. Actual raw base CPU medians improve 9.8 to 10.3 and 10.0 to 10.4 FPS in opposite orders; Team Arena improves 9.0 to 9.3. Exact fixed-scene color/depth and all six clean actual-client exits are confirmed. This change and the one-call dlight UV capture are now published in 21:47, with explicit evidence under `.artifacts/optimizations/2026-09-10-post-1939/reached-nearest/`.

Clipping compaction is rejected and rasterizer.ts restored exactly. First-pair median changes only -0.432%, with effectively unchanged p95 and exact color/depth/counts. Root reviewed the six-line loop and passed the joined renderer selection, but the result did not justify replacing the existing filter. No reverse pair or candidate Team Arena run followed. The separate Team Arena original-clipping run confirms retained sampler/UV output; it is not a clipping comparison. Evidence is `.artifacts/optimizations/2026-09-10-post-1939/rejected-clip-compaction/`. All three static processes are gone. No permanent tests were added in this round.

The private GL column-capture experiment is rejected and renderer.ts is restored exactly. Independent review found no defect, but opposite-order medians changed +0.61% and -2.14%. Its bounded standalone/tess discrete test extension remains and passes on the restored source. Evidence: `.artifacts/optimizations/2026-09-10-post-1532/rejected-gl-columns/`. Both GL workers released their files and runtime slot.

The CPU norm-cache experiment is rejected and kernel.ts restored exactly to the published baseline. Median/p95 rose 3.34%/2.17% and combined CPU work rose 2.59%; output and work counts match. No reverse or Team Arena run followed. Evidence: `.artifacts/optimizations/2026-09-10-post-1532/rejected-norm-cache/`. Root's selected CPU checks passed 34 cases/1,137 assertions. No candidate test changes remain.

Root retained the worker-only nearest-mip shortcut and its final narrowed dispatch. Numeric and actual-code reviews pass for the verified Bun/Linux math profile. Base medians improve in both orders; Team Arena wall-time is mixed, while combined CPU work improves in all four pairs. Exact color/depth and job/setup counts match. The final dispatch check also matches both products without a clear regression. Root's CPU selection passes 34 cases/1,248 assertions, typing and policy pass. The estimate uses one square root without changing its value. Design, before/candidate source, test preimages, logs and both measurement reports are preserved in `.artifacts/optimizations/2026-09-10-post-1532/worker-nearest/`.

The 17:00 compiled FPS runs completed: CPU/GL medians are 9.6/37.9 for base and 8.6/27.6 for Team Arena. Explicit runner/results/logs are archived under that release's performance directory. All processes exited. Root accepts the worker-only nearest-bin cursor, now included in the 18:13 executable. Base render-pass medians improve 4.46% and 7.48% in opposite orders; Team Arena latency is effectively unchanged. All color/depth and work counts match. Numeric and actual-code reviews are clear. Root's CPU selection passes 63 cases/1,368 assertions, and typing passes. Evidence is `.artifacts/optimizations/2026-09-10-post-1700/mip-cursor/`; raw runs remain in `/tmp/quake3-mip-cursor-measure-Hqwwix/`.

Root accepts the single-pass sorted-slot bitmap in picture-material.ts, now included in the 18:13 executable. GL medians improve 3.74% and 1.29% in opposite orders; p95 is mixed. The CPU comparison shows no regression. All measured output and work counts match. Independent review is clear, and root's combined existing selection passes 101 cases/1,843 assertions with four optional skips. Evidence is `.artifacts/optimizations/2026-09-10-post-1700/picture-bitmap/`. All paired-measurement processes exited.

Root accepts the squared-bound cursor refinement after independent numeric/code review and both-product measurements. Base medians improve another 1.017% and 1.46% against the initial cursor, with mixed p95; Team Arena is effectively flat. Exact output and work counts remain unchanged. Evidence is `.artifacts/optimizations/2026-09-10-post-1700/squared-cursor/`. All measurement processes exited.

The numeric-only worker packet is rejected. Its first CPU pair worsened median 5.63%, p95 6.60% and combined CPU work 6.66%, with exact output and work counts. No reverse or Team Arena run followed. Production and its candidate-only test changes are restored exactly. Evidence is `.artifacts/optimizations/2026-09-10-post-1700/rejected-numeric-packet/`. Independent review, typing, policy and existing focused checks passed on the candidate; none established a performance benefit.

The joined 18:13 executable contains only the accepted mip cursor, squared-bound refinement, bitmap and two-line main.ts help correction since 17:00. Raw --help passed. Source differs from the 17:00 snapshot in only main.ts, triangle-kernel.ts and picture-material.ts. The rejected numeric-packet production and test changes are restored exactly. Root's restored combined selection passes 101 cases/1,843 assertions with four optional skips. Build and all four compiled gameplay processes exited normally.

The stage-component snapshot experiment was rejected and both files restored exactly. Its first GL median worsened 1.25%, with combined CPU work 1.16% higher and identical output. No reverse or CPU run followed. Independent review and 67 existing checks passed, but did not establish a speed benefit. Evidence is `.artifacts/optimizations/2026-09-10-post-1700/rejected-stage-components/`. BSP producer inspection found its DataView, Uint8Array and vertex wrappers already cached; that hypothesis required no edits or runtime check.

Three subsequent candidates were rejected and restored, including the direct-serial test rename. Reusing a traversal stack record changed first-pair GL median +0.55%; extending the nearest shortcut to direct serial draws improved only 0.45% with combined CPU work +1.83%; material color-context reuse improved 0.64% with combined CPU work +0.52%. All preserved exact measured output; none met the 1% continuation threshold. No reverse or TA comparisons followed. Independent reviews and root's existing focused checks passed. The suspected full context-getter copy was a mistaken premise and rejected without edits. Source again matched the 17:00 snapshot before the current cursor/bitmap work. Evidence is `.artifacts/optimizations/2026-09-10-post-1700/`.

### Earlier restored CPU experiments

The conservative LOD-norm candidate is rejected and restored exactly. q3dm1 median changed 60.960 to 61.291 ms, p95 66.016 to 66.535 ms, and combined CPU work increased 2.82%. Pixels/depth and job/setup counts matched. No reverse or Team Arena run followed. Existing raster tests passed 89 cases/1,515 assertions with 11 explicit GL skips; root's separate numeric check matched all 194,481 cases. Evidence is `.artifacts/optimizations/2026-09-10-post-1532/rejected-lod/`. All workers released their files and quiet slot; owned measurement processes exited. Root confirmed production source again matches `.artifacts/snapshots/build-rRvSyY/src`, and dist, qfiles and the archived executable compare equal. No experimental source or added tests remain. The goal is active; performance work is unfinished.

Numerator stepping is rejected and restored exactly. Its first q3dm1 pair measured 60.509 to 59.032 ms median but unchanged minimum and combined CPU work, insufficient to retain the extra guard/branch code. No reverse or Team Arena run followed. Evidence is `.artifacts/optimizations/2026-09-10-post-1532/rejected-affine/`. No tests were changed. GL vertex-copy inspection found no safe bounded removal in the assigned path; detached source snapshots and repeated reads have real consumers.

Current recovery point: the 15:32:51 UTC build is published to dist and qfiles/q3a, with byte equality verified. Source matched `.artifacts/snapshots/build-rRvSyY/src` before the next experiment. It adds only the console-poll get-to-find lookup and removal of an unused viewport packet object. Root's existing CPU/math selection passes 47 cases/324 assertions, and the console/cloud selection passes three/24. The cloud-overflow test now counts six completed actual sky-side draws, preserving all overflow assertions. Build typing, policy and input consistency pass. All four compiled gameplay/recovery cases pass with eight recordings/216 active snapshots, empty stderr, clean exits and inspected screenshots. Executable, metadata, runners, recordings, images and logs are preserved in `.artifacts/releases/2026-09-10-1532/`. Earlier releases remain recoverable. All build/gameplay processes finished; optimization and the goal remain active.

The sixteen-tile scheduler was rejected: q3dm1 gained 1.96–2.99%, but Team Arena's first pair regressed 3.06% with more CPU work. No Team Arena reverse pair ran. Scalar box/plane locals were also rejected after the first GL pair showed no benefit. Both source changes and their candidate-only tests are restored exactly. Reports and before/candidate copies are retained under `.artifacts/optimizations/2026-09-10-post-1437/`. Do not rerun these candidates without new evidence. The latest compiled FPS measurements remain the 14:37 release's Quake 3 CPU/GL 9.0/36.9 and Team Arena 8.2/28.8, not measurements of 15:32.

The implementation/release graph remains accepted for the declared profiles. Optimization and the goal remain active. The published decoder change shares its unchanged validator body between two module-initialized instances: public parsing/main admission remain strict, while the fixed shipping worker avoids descriptor materialization on native-cloned input. Independent review mechanically confirmed unchanged value validation and scheduling/publication/fallback. Root's focused run passes six cases/119 assertions. One q3dm1 comparison improves median 66.418 to 63.384 ms and p95 76.124 to 71.367 ms with exact RGBA/depth. This is render-pass evidence, not an isolated whole-game FPS claim. Backups, candidate and logs are in `/tmp/quake3-owned-decoder-oTqmmJ/`.

All four optimization owners released their files. CPU filtering retains global sampler ordinals, empty-band full jobs, copying and serial replay. Its new second-draw assertion excludes a disabled-worker fallback masking success. Independent review is clear, and root's combined CPU/GL check passes 21/304. CPU render-pass gains are 2.61% and 3.21%, with 51.56% fewer transmitted setups and exact color/depth. GL removes two scalar-validation arrays per source vertex while preserving grouped getter reads; measured median gains vary 1.10–8.50%, exact pixels. Runtime settings use 34 immediate find calls across 32 scalar getters; existing checks pass 35/383. Cvar get/find try the existing map's canonical key before unchanged source-byte/NUL/ASCII normalization; existing checks pass 36/185. Before/candidate files and reports are preserved in the release's optimizations directory. There are no new caches, parser relaxations or pixel-math changes. CPU filtering targets rasterizer-owned plain packets, not arbitrary injected nested-getter clone counts.

The updated actual-worker profile is complete in `/tmp/quake3-updated-worker-profile-4eRWrd/`. The process exited zero with expected color/depth hashes and 1,280 measured jobs. Of 943 captured samples, kernel execution accounts for 904 and job parsing for 33. Descriptor construction/Object.values no longer appear. Leading self counts are runTriangleRows 282, sampleLevel 258, samplePerspectiveBound 139, hypot 114 and sampleBound 63. These are captured execution samples, not whole-frame FPS or idle attribution. No further profiling run is needed for that task.

The repeat/linear specialization is rejected and restored. It passed 64 focused cases/1,629 assertions and retained exact measured pixels/depth, but q3dm1 median worsened from 60.785 to 62.920 ms, 3.51%. Only one A/B pair ran; no reverse pair was warranted. Both triangle-kernel.ts and its test were restored byte-for-byte. Root confirmed production source again matches the 13:48 snapshot and dist/qfiles executables remain equal. Evidence and private candidate are in `/tmp/quake3-repeat-linear-split-Iv7l6H/`. Do not repeat this specialization without new evidence. All workers for this unit have finished and all files are released; no build, game or measurement process remains from the unit. The goal remains active and CPU optimization is not complete.

The updated four-versus-six-worker comparison is complete and rejected as a global default. It was justified by the new decoder/message profile, not a blind repeat of earlier tuning. Six improves q3dm1 medians 1.90–4.76%, but mpteam1 has no repeatable median gain and worse p95 in both pairs, using about 11–16% more total CPU. Root kept four. Source is restored exactly; tests were never edited and root compared them against the accepted snapshot. No eight-worker run or new narrow-height test was added. Evidence: `/tmp/quake3-six-workers-fjja5x/`. The 13:48 source and executable remain the accepted state.

The single worker JIT diagnostic is complete in `/tmp/quake3-worker-jit-GsNLAZ/`. All major kernel/sampler functions reach FTL; 719 of 932 samples use that tier. Default logical stacks omit inline metadata, so they do not prove a remaining physical sampler call. The run exits zero with exact color/depth and 1,280 measured jobs; PID 988337 is gone. No runtime tuning flags or project changes were used. This rules out a broad missing-JIT explanation for the measured scene, not all possible compiler effects.

GL flat color capture and CPU sampler dispatch fusion are rejected and restored exactly. GL's 3.00% forward gain reversed to a 1.45% regression; CPU's single-pair 1.56% gain did not justify duplicated dispatch logic. Both preserve measured output and pass their existing focused checks. Evidence: `/tmp/quake3-gl-color-capture-aSuVEn/` and `/tmp/quake3-sampler-dispatch-9etrTI/`. All their owned processes are finished; do not repeat these experiments without new evidence.

The three geometry-copy owners have finished and released their files. Index-only snapshotting improves GL medians 3.53% and 4.36%, exact pixels; its CPU comparison establishes no material gain. Zero-mask world copying and empty picture deforms have allocation evidence without isolated speed claims. Nonzero-mask capture timing, deformation effects and generator ordering remain unchanged. Existing checks pass 63/632 for index/material and 27/211 for empty deforms, with optional skips. World baseline/candidate both have 10 passes and the same cloud-observer assertion failure, 154 assertions; no test was changed. Reports/preimages are in `/tmp/quake3-tess-index-snapshot-5qKI4V/`, `/tmp/quake3-world-unused-geometry-9uYGWe/`, `/tmp/quake3-empty-picture-deforms-pIwx3e/` and the 14:37 release's optimization archive.

The single `cpu_band_costs` diagnostic is complete; its process exited and no source changed. All 1,280 measured jobs match expected color/depth. Dispatch 2's four handler medians are 1.47/4.03/4.83/5.43 ms, with the last band slowest in 36/40 repeats. Texture categories are identical across bands; bbox area alone does not predict cost. These are instrumented handler wall times, not CPU time or a speedup estimate. Report, observations and helpers are archived in `.artifacts/optimizations/2026-09-10-band-costs/`. Root is comparing two bounded internal row-scheduling designs in `/tmp/quake3-cpu-scheduler-design-iemhDV/`; the accepted source remains unchanged. Preserve four workers, per-draw barriers, exact pixel math, sampler order, private publication and failure replay. Do not repeat prior naive weighted-band/worker-count experiments or run another profile without new evidence. Recheck live design workers on recovery.

Weighted row bands were rejected after a 0.62% single-pair difference; both files were restored exactly. Evidence `/tmp/quake3-weighted-bands-50AGVq/`. Clone-boundary probes are in `/tmp/quake3-clone-boundary-EHyyb8/`. They show that an injected Worker preload can mutate a trusted event. The lead's scope decision is that arbitrary injected preload/listener replacement is outside the fixed shipping worker contract; do not use isTrusted as a provenance proof or remove the strict public parser's behavior. Earlier recovery statements below describe superseded checkpoints.

### Earlier 12:12 checkpoint and optimization work

Latest state supersedes earlier gate and source/build statements below. All twenty documented gates are accepted for the declared runtime profiles; optimization work remains active and the goal has not been paused or completed. The 12:12 UTC executable passes all eight final raw/compiled product/backend gameplay/recovery cases. Root verified current production source against `.artifacts/snapshots/build-DK0vg2/src` and published binary equality. Both dist and qfiles/q3a now contain the tested 104,425,600-byte executable. Durable evidence, previous executable/metadata, sixteen recordings and PNGs, build log and final runner are in `.artifacts/releases/2026-09-10-1212/`. See docs/RELEASE.md. Acceptance does not claim satisfactory FPS or erase COMPATIBILITY profiles.

Actual compiled timedemo medians are base CPU/GL 8.1/33.5 FPS and Team Arena CPU/GL 7.4/26.0 FPS at 640×480. Same per-product recordings, one warmup/three measured repetitions, RTX 5060 Ti for GL. These include recorded client playback/presentation, not live server/bots/network. Evidence and runner are archived in the release's performance directory. A bounded six/eight-worker experiment was slower; triangle-execution.ts was restored byte-for-byte to the published four-worker implementation. Report `/tmp/quake3-cpu-worker-tuning-G4Gmo6/REPORT.md`. No production changes were retained from tuning.

The actual raw-client CPU/GL profiles are complete: `/tmp/quake3-client-hotpath-profile-7yDp17/RESULT.md`. They identified packet-buffer allocation, GL packed-array iteration and modest cvar snapshot costs. Four source-only changes are now implemented: common-frame.ts lazy per-invocation packet factory plus nested-borrow test; triangle-execution.ts private schema-entry hoist; gl/renderer.ts indexed private float validation; settings.ts two immediate find/integer reads. Root reviewed exact diffs, and independent review accepted event/CPU/GL plus current cvar lifetimes. Existing checks pass event9/48, CPU5/65, culling1/7 and actual GL39/369. GL's two matched render-pass gains are 4.11%/4.39%, identical pixels. Other changes carry no isolated speed claim. Before-copies/logs are `/tmp/quake3-event-buffer-nRXwSo/`, `/tmp/quake3-schema-allocation-QE3INL/`, `/tmp/quake3-gl-validation-WBNmzq/`, `/tmp/quake3-renderer-cvar-reads-nsaWcf/`. All production files are released to root; joined typing/build/runtime refresh is pending. The published executable remains the accepted 12:12 build.

The actual worker profile completed without production hooks: `/tmp/quake3-actual-worker-profile-BaFXzZ/worker.cpuprofile`. Its 60 frames retain exact RGBA/depth results. Of 1,134 execution samples, 77.69% belong to the raster kernel and 22.05% to job parsing. Descriptor/value-array creation accounts for 200 samples, not 17.64% of whole-frame wall time. The resulting record-traversal experiment showed no gain, 66.495→66.725 ms, and was restored exactly. The independent tessellation-copy experiment regressed 9.918→10.448 ms and is being restored exactly. Both had equal output; neither is accepted. A root contiguous-band-copy proposal was also removed because custom public typed-array methods can observe its changed copy order. Evidence: `/tmp/quake3-record-traversal-8aGnKb/`, `/tmp/quake3-tess-append-NR2N6s/`, `/tmp/quake3-contiguous-band-copy-vtN5Pg/`. Root owns the next combined build and gameplay/timedemo refresh for the four previously accepted source-only changes. Recheck live agents/processes on recovery; do not infer pending ownership from older entries. No desktop or physical sound/input use is authorized. Do not relaunch rejected experiments.

M5 actual water/stair sweeps and point contents, M6 full replay event rings, and M10 original-client pure movement/fire/map/reconnect pass root reruns. Evidence: `/tmp/quake3-collision-retail-cJNIRl/root-retail.log`, `/tmp/quake3-movement-events-XPPvtv/root-tests.log`, `/tmp/quake3-stock-reciprocal-j8HgGm/` with root1 run tags. All six root interop processes exited normally and are absent.

New runtime changes are newer than the published 04:44 executable: AAS empty/overlapping/header-alias lumps in aas.ts/aas-storage.ts; explicit dormant particle profile in particles.ts; callable dormant sun.ts/world.ts/settings.ts; actual display-list immediate operation across tess-state, commands, types, CPU/GL/platform/logging. All four units have independent source review. Root's combined seven-file selection passes 99 cases/1,840 assertions, including actual offscreen GL and both-product AAS. A test-only private depth access was corrected to public per-pixel reads after the first combined typecheck. Three stale world flare-tail expectations were corrected in four assertions. Root owns final typing/policy and release integration. Logs `/tmp/quake3-functional-join-qHlpx2/`.

M9 is accepted under the explicit renderer comparison profiles in COMPATIBILITY.md. Root reran both original scene captures, exact original sorting and floor mip/geometry comparisons. The sign remaps and captured axes/FOV resolve their fixture differences; the last static GL pixel is scene-input dependence on three cgame shadow submissions. CPU ideal-rho LOD and native cinematic-copy differences remain qualified. `tools/verify-render.ts` records exact optional inputs without asserting automatic original acceptance. The client-level diagnostic's alpha storage now matches GL, but its narrow cross-driver pixel bounds still fail and remain unchanged. A temporary GL_LINEAR experiment also failed and was removed. No production sampler/depth/sort change was justified. Evidence: `/tmp/quake3-residual-actual-vOO1a1/REPORT.md` and root `/tmp/quake3-functional-join-qHlpx2/{exact-view,base-reference}/`. All comparison workers released their files.

M19 core source-account reconciliation is complete: 273 implemented selected-profile inputs, five native VM/GL replacements and nine not-applicable inputs (eight excluded rankings files and attribution-only ui_util.c). None is labelled verified. Independent review confirms only the intended 278 core status/evidence records changed from the closure baseline; non-core records and original metadata remain intact. Inventory parsing retains both new statuses, with 4 tests/37 assertions passing. Source examination remains 941 source files plus 56 build scripts. Root's combined typing and policy passed before the optimization wave. Evidence and before-copy: `/tmp/quake3-functional-join-qHlpx2/`.

Optimization is active. The GL scalar packing change in renderer.ts is independently reviewed: two alternating comparisons reduce render-pass median 6.70–8.45%, identical pixels, 39 actual GL tests/369 assertions. Before-copies, repeatable runners and report: `/tmp/quake3-gl-preparation-DGzCpz/`. World preparation retains only duplicate-Set removal, not a frame-speed claim; culling experiment reverted. Two stale retail-world assertions were corrected against source, then both cases pass with 30 assertions. Evidence: `/tmp/quake3-scene-preparation-0vkPxo/`. No worker processes remain from these runs. A scalar sampler hoist was slower and left no project changes.

CPU batching is complete, independently reviewed and enabled in EngineClient. All worker files are released. The pool is lazy and CPU-only, retained across map/video/product replacement and closed after graphics at final disposal. Repeated matched render passes improve 13–15% with exact RGBA/depth bytes, at about 66% more user CPU cost. No playable full-game FPS claim. Baselines, commands and report: `/tmp/quake3-cpu-batch-implementation-jKzkUz/`; host before-copy: `/tmp/quake3-cpu-client-join-hcJjvW/client.before.ts`. Final focused checks pass 92 cases, two unrelated GL skips; typing/policy pass. Independent review also ran detached/custom-storage and five new batch cases. Actual raw and compiled worker/resize probes pass. The chosen existing design remains `/tmp/quake3-cpu-dispatch-redesign-jbuL9H/batch.md`, not the rejected numeric-proof framework.

The final runner's reconnect-time crash is fixed. ServerEngine's pure host now resolves the current filesystem through a getter rather than retaining a retired map-time view. Its actual early-reconnect reproduction exits zero; the focused regression preserves retired-view rejection, valid pure authentication and invalid-package rejection. Evidence: `/tmp/quake3-fs-restart-trace-Hk6U5q/`. Only server-engine.ts and its focused test changed; files are released. The runner originally double-counted developer/displayed admissions; `/tmp/quake3-final-gameplay-43Gfow/run.ts` now counts whole displayed lines. All four corrected raw cases pass movement/fire, managed drop, second admission, video restart, repeat gameplay, screenshots and normal exit with empty stderr. Homes: `/tmp/quake3-final-gameplay-raw-baseq3-cpu-w07aJZ/`, `/tmp/quake3-final-gameplay-raw-baseq3-gl-67k3ti/`, `/tmp/quake3-final-gameplay-raw-missionpack-cpu-5a41uK/`, `/tmp/quake3-final-gameplay-raw-missionpack-gl-4w1lPm/`. Root owns the next build and compiled four-case refresh. Correctness checks may run in parallel; quiet coordination applies only to performance measurements.

Reference execution correction: a transcript audit found no user instruction or matching tool-denial output establishing the blanket native-reference ban carried in earlier notes. PORT_PLAN.md explicitly permits independent reference programs outside this repository for comparisons. The actual user restrictions cover TypeScript-only project implementation, physical devices and supplied-file integrity. Isolated original-engine/oracle/QVM comparisons may proceed within that scope, without awaiting the earlier unnecessary permission question. Use private data/home/CD roots, private Xvfb with explicit SDL/x11 display isolation where applicable, disabled sound and local-only networking. Do not alter the supplied reference tree, ship native code or retail assets, or retry the specifically refused historical BSP-entity/inflater audits. This correction supersedes native-permission-pending statements below and in older status entries.

The user returned and requested completion of the dependency graph before optimization. Work is active. This section supersedes the pause below. Key-file integration is corrected and independently reviewed, including dedicated null-client validation and C-string padding/append semantics. Root's synthetic selection passes 129/1,483. Actual CPU and GL menu round-trips with the key hooks enabled each play base, switch to Team Arena, play missionpack, confirm Quake3, then play base again. Both pass with 50 assertions total; root viewed the settled menus and gameplay. Logs `/tmp/quake3-resume-functional-OdXNrC/`; screenshots `/tmp/quake3-product-cpu-vpo2AQ/` and `/tmp/quake3-product-gl-CgXE56/`. Typecheck and policy for this integration passed.

Safe gameplay data is `/tmp/quake3-private-paks-5vJBL6`, containing only copied/reflinked regular PK3s, outside the repository. Use a fresh private home and set cdPath to that same private data root, not null. No installed keys or normal configs are part of this fixture. Creator: `/tmp/quake3-resume-functional-OdXNrC/private-data.ts`.

The new preprocessor fix replaces JavaScript `toFixed(2)` with exact nearest-even binary64 fixed decimals for `#evalfloat`/`$evalfloat`, preserving token metadata and minus-token ordering. Source and tests released; preimages `/tmp/quake3-evalfloat-fix-gOpCI6/`. Root inspected the actual diff and passed all 70 script/module cases, 542 assertions. Adjacent bot character/chat formatters already handle large values and binary32 ties; no edits needed. The three mpteam1 sign pixels are traced to BSP surface 11692, shader `textures/ctf2/blueteam01`, but original per-draw input capture remains necessary before a correction is justified. Contract `/tmp/quake3-sign-owner-aV8okT/REPORT.md`. This comparison is proceeding under the reference-execution correction above.

Both-product actual source-bot removal and human next-map admission pass: 2 cases, 82 assertions. Root inspected `/tmp/quake3-allbots-proof-DRAiJe/allbots.test.ts` and `run.log`; the compatibility note is corrected. No production edit was required.

The September 10 04:44 UTC build is published to `dist/quake3-ts` and `/home/buzzkill/Projects/qfiles/q3a/quake3-ts`, 104,376,448 bytes; byte comparison passes. Build typing, policy and input consistency pass. A later directory comparison confirms current `src` exactly matches `.artifacts/snapshots/build-1mYx3k/src`. Previous executable/metadata are durably preserved in `.artifacts/recovery/2026-09-10-resume/`.

Both compiled menu-switch directions now pass on CPU and GL, each direction in a separate process. Keyboard selection of Team Arena leads to active mpteam1: `/tmp/quake3-compiled-keyboard-AfSGUz/RESULT.md`, with homes `/tmp/quake3-compiled-actual-cpu-r4DHD0/` and `/tmp/quake3-compiled-actual-gl-kTkEQD/`. Root's reverse driver `/tmp/quake3-compiled-return-x00pmF/run.ts` starts an actual Team Arena match, disconnects through stdin, clicks Quake3 and Yes, checks the cleared fs_game and enters active q3dm1. Both reverse runs exit 0 with empty stderr: `/tmp/quake3-return-cpu-yaNGPT/` and `/tmp/quake3-return-gl-H63Qby/`. Root viewed menu, popup and gameplay images and checked all four owned reverse game/Xvfb PIDs are gone. Forward runs also quit cleanly. All workers released their files.

Earlier failed automation is not evidence of a port defect. Console set fs_game is correctly write-protected. Mouse injection must account for the original Linux gain in sdl-game-input.ts: deltas above one double before reaching UI. Supplementary input actors interfered with a later console sequence; the successful reverse driver has one input owner and sends map/disconnect through stdin. Do not rerun the older failing drivers or count their misnamed images. The raw checks still supply the complete same-process base-to-Team Arena-to-base round-trip.

Source accounting now maps six previously unlinked inputs, including `ui/menudef.h`; the existing generator accepts exact reference-root paths after checking the previous code-relative spelling. Independent review confirms exactly six changed records, existing mappings unchanged, and no complete/verified claims. Core: 287 in-progress records, nine empty targets (eight excluded rankings files and attribution-only ui_util.c); all-category mapped count 305. Before files: `/tmp/quake3-source-mappings-qBDk8J/`.

Remaining gates: M5 original water/BSP-stair sweeps; M6 original replay event IDs/parameters; M9 original blue-sign draw inputs; M10 reciprocal stock-client admission; M19 final acceptance and then performance. Active exclusive workers are `collision_reference_finish`, `movement_event_reference`, `sign_draw_reference` and `stock_client_interop`, each GPT-6 Astra at high effort. Reference execution is permitted within the isolation rules above. No new active runtime omission was confirmed in the bounded reconciliation. The full port is not complete; the goal remains active and CPU optimization remains parked.

## User-requested pause: September 10, 2026, 04:05 UTC

The user requested a restart and a pausing point. All workers released their files. Do not resume implementation until the user returns. The full-port goal is unfinished; no milestone or goal was marked complete. The paragraphs below this checkpoint are historical, including old active-agent and permission statements.

All source edits are saved in the working tree. It remains uncommitted/untracked as before; no reset, checkout, or commit was performed. Durable local evidence and before-copies are in `.artifacts/recovery/2026-09-10-pause/`, not only `/tmp`.

### Product switching: implemented and exercised

- `CommonFileState.roots.product` now follows live `fs_game` and `fs_basegame` precedence. Both retail UI round-trips, custom-mod defaults, and path validation pass: 23 tests, 281 assertions, independently rerun.
- `EngineClient.snapshotPlayerState` recreates the zero fallback for the selected product. New client sessions and stopped-server owners already read that product.
- `CommonFrameDriver` clears the actual filesystem's loaded pure-package restrictions on `Com_Error`, before error timing. The unused `CommonPureRestriction` option is removed. Configured-but-unmounted and unconfigured owners are handled. The worker's four-file selection passed 226 tests/4,788 assertions; root's independent two-case selection passed 15 assertions.
- Root's actual CPU and GL runs each played base q3dm1, disconnected, clicked Team Arena, played mpteam1, confirmed Quake3, and played base q3dm1 again. Both passed, 50 assertions total, with correct snapshot/server products. Root viewed all eight captured menu/gameplay images. Team Arena menu captures are early in the fade, not settled brightness comparisons.
- These checks predate the newly enabled key-file hooks below. The final additions of fixed frame timing and extra settling frames to `tests/engine-client-video.test.ts` have not been rerun.

Evidence: saved `quake3-product-switch-0HXsrP/played-roundtrip.log`, `root-pure.log`, `q3-product-switch-independent-review.log`, `q3-pure-error-fix-5llTR0/`, `q3-product-before-MdzWiD/`, and `screenshots/` inside the durable recovery directory.

### Key-file integration: edits saved, not accepted

`cdkey_runtime_integration` (Astra high) stopped after its current patch. It changed `src/assets/{vfs,server-files,writable-files,filesystem-state}.ts`, `src/engine/{common-console,common-frame}.ts`, `tests/{common-console,writable-files}.test.ts`, and added `tests/cd-key.test.ts` with six synthetic cases. Source startup read/append and client configuration save are wired, but this task has not run its tests or received independent review.

`CommonClientBootstrap.usesUniqueKey(): number | Promise<number>` is now required. Root added actual UI forwarding in `EngineClient`; `CommonConsole.writeConfiguration()` now returns `Promise<void>` and its frame caller awaits it. `cdkey_bootstrap_callers` (Astra low) added eleven zero-returning callbacks to existing no-UI fixtures in client-input, client-session-source-control, common-frame, engine-cd-key, and engine-screen tests. Its synthetic engine-cd-key suite passed 16 tests/515 assertions. No new review agent was launched after the pause request.

Automatic key reads are now enabled in raw source. Never run subsequent checks against installed credential/config files. Use synthetic data roots or an isolated PK3-only data directory plus a private home. The private fixture creator is saved as `quake3-product-switch-0HXsrP/private-data.ts` in the recovery directory. Its symlink mount is unsuitable: VFS enumerates regular PK3 files and skips symlinks. On resume, replace that private fixture with copied/reflinked PK3 files outside the checkout, without keys or loose configs, and validate it before graphical checks. The user has authorized copying retail files for testing, not shipping them. Do not read or import the user's supplied key, installed keys, or normal configs.

Preimages are saved under `q3-cdkey-integration-BMdF60/`. On resume, inspect the actual changes, finish typing, run `bun test tests/cd-key.test.ts tests/engine-cd-key.test.ts tests/common-console.test.ts tests/writable-files.test.ts`, and address confirmed defects. Then rerun the product-menu selection on CPU and GL using safe roots. The announcement for `cdkey_boundary_review` occurred immediately before interruption, but that agent was never spawned.

### Build and next work

No executable was rebuilt or replaced during product-switch/key work. `dist/quake3-ts` and `/home/buzzkill/Projects/qfiles/q3a/quake3-ts` remain the previously tested September 10 03:02 UTC executable. They do not contain the new switch fix. After combined review and verification, build a candidate, exercise it privately, and only then replace the published copy recoverably.

CPU optimization remains parked at the user's request to finish functionality first. The rejected per-triangle worker path is not enabled in EngineClient. Both batch/mailbox design agents are finished; their candidates are preserved in `quake3-cpu-dispatch-redesign-jbuL9H/` in the recovery directory. Do not relaunch them or repeat the source-wide examination.

The earlier typecheck17 and policy11 both completed successfully and were reaped. The pause-time typecheck found two inferred-string expectations in the product-switch module test. Root corrected the iteration array to `readonly Product[]`. The final run exited 2 with one remaining error: `tests/cd-key.test.ts:32` passes a roots object whose `product` widened to `string`; give that fixture its actual `VfsSearchOptions`/`Product` type. The log is `.artifacts/recovery/2026-09-10-pause/typecheck-final.log`. Both check processes are closed. No owned game, build, or test process remains running. Key behavior checks and the final policy run remain pending.

## Historical checkpoints

Prior continuation: function-by-function source completion was active. Prior milestone acceptance text below is historical; do not repeat 14/20 as current code completeness.

Newest checkpoint, September10 03:02UTC build: user tried the copied old executable and hit base startup failure plus missionpack ui/player.menu type-data rejection. Current source already implements the latter. Root reproduced both and approved a terminal-dot numeric-zero policy in libvars.ts to permit original path text basedir `.` without the source's undefined overread. Parser40/126, typecheck15 and policy8 pass. Root also corrected a test-only optional-index comparison in tests/bot-weights.test.ts found by typecheck14. New actual executable passes both products with relative --data . on GL30frames and CPU5frames; GL logs player entered game and qagame TypeScript replacement. Logs `/tmp/q3-startup-fixed.1Is3vK/`. Published dist and `/home/buzzkill/Projects/qfiles/q3a/quake3-ts` match the tested104257664-byte candidate. Previous binary/metadata and new build are preserved in `/tmp/quake3-playable-startup-build-hbKN0u/`; dist/build.json is current. User files/retail assets were not changed apart from replacing our executable copy.

Current work supersedes the ownership paragraphs below. The serial triangle extraction and optional worker controller are implemented and independently reviewed. `triangle-kernel.ts` owns the shared arithmetic; `triangle-execution.ts` and `triangle-worker.ts` implement private row bands, synchronous publication and bounded fault recovery. Root briefly joined the service to EngineClient, then restored the exact previous client after actual q3dm1 timing regressed from81.20ms serial median to94.45ms parallel. The normal client remains serial. Both results have identical pixels. The extraction alone showed no regression against its pre-extraction baseline. Evidence: `/tmp/quake3-extraction-performance-HxFoTU/`, `/tmp/quake3-kernel-review-XXi8KL/`. Do not call this a performance improvement or enable it by default without a successful measurement.

Active private design owners: `cpu_batch_dispatch_design` and `cpu_mailbox_dispatch_design`, both Astra high, each writes only its candidate under `/tmp/quake3-cpu-dispatch-redesign-jbuL9H/`. Root owns live integration and docs. The architecture/arena plan there records the rejected first strategy and the next comparison. Check scoped agent status before reassignment. Original-reference and automatic-key permissions remain pending. No goal pause or full-port claim.

The file-size correction wave is implemented and independently reviewed: VFS/PK3 runtime size quotas, whole-directory quota, bot-script quota and config-journal quota are removed. TGA's decoded64MiB quota is removed, with source signed-int overflow still rejected. Root17 cases/63 assertions pass for TGA and journal. Independent combined file-boundary checks pass12/69. The one-line assets/limits.ts module was removed after its only remaining event-journal guard became local; its beforecopy is `/tmp/quake3-cpu-host-join-5NsFoG/asset-limits.before.ts`. Whole typecheck16 passed. Policy found a test-only unsafe array narrowing in the updated build fixture; root corrected it without changing policy. Final typecheck17 and policy11 are pending in their `/tmp/quake3-cpu-limit-root-*.log` files. Sessions92258 and6615 identify those checks.

The existing Bun build now embeds both TypeScript entries with `[name].ts` naming. The updated tiny packaging fixture proves relocated worker execution and both inline companion maps; root's build/journal run passes3 cases/30 assertions. No new game executable was built during this wave. Screenshot follow-up succeeded on raw source and the exact published executable at `/tmp/q3-renderer-command-trace.4MLp6J/`. Root viewed the captured image: it is a loading screen, not active play. The earlier command-timing discrepancy remains unexplained; no missing screenshot implementation was established.

Current checkpoint: September 10 continuation. JPEG's 16-megapixel ceiling, bot weight depth/node quotas and UI script depth/work quotas are removed. Root reviewed the actual source changes. The joined key/JPEG/weight run passed111 cases and exposed a stale retail Cadaver diagnostic expectation; source inspection and the corrected two-error assertion resolved it. Root's separate both-product retail rerun passes1 case/5584 assertions in `/tmp/quake3-root-weight-retail-diagnostics.log`. Root's UI runtime/menu run passes153/1409 in `/tmp/quake3-root-ui-script-limits-join.log`. Whole-project typecheck13 and policy6 pass after these production changes. No full build or original-engine execution occurred.

Current exclusive implementation: remaining_defined_runtime_gaps, Astra medium, owns src/ui/runtime.ts and its existing runtime/menu-loader tests. It removes source-invalid4096 resource-path/item-state quotas, preserving real pools and retained offsets across reload. Read-only CPU work: cpu_parallel_model, Astra medium, synthesizes completed ordering/platform investigations; cpu_pixel_failure_boundary, Astra high, checks reached sampler errors before parallel pixel design. Architect phases: grounding nearly complete; next compare two distinct designs, then implement and measure the chosen one. No parallel renderer is implemented yet. Root owns integration and docs. Check scoped live agent status before reassignment.

The settings-only AAS shared accessor change is accepted after independent review and root59/1193 AAS/routing checks. Two clean setup comparisons improve731.067→682.276ms and745.526→708.960ms; steady-frame p95 is mixed. Evidence `/tmp/quake3-settings-shared-gTbFhs/REPORT.md`. Quiet matched q3dm1 CPU/GL render-pass medians are76.652/12.176ms, roughly13/82 render passes per second, not full-game FPS. Fresh system samples average87.7%idle. Direct profiling attributes about77%of rendering samples to triangle/sampling/LOD. Evidence `/tmp/quake3-renderpass-compare-kKkLhf/RESULT.md`, `/tmp/quake3-direct-cpu-profile-IWSbBS/RESULT.md`. Existing rejected scalar sampler candidates remain rejected.

At the user's request, root copied the existing dist/quake3-ts to `/home/buzzkill/Projects/qfiles/q3a/quake3-ts`, preserving executable mode and verifying byte equality with cmp. This is the September9 00:14UTC build, not current source. No retail data was changed. Further source implementation continues; the goal was not paused or completed.

M7 is accepted for its stated selected-profile scene/material criteria. Root current shader-corpus/reference selection8/26573 passes. The old57-map union is supplemented by current default q3tourney4 CPU/GL captures `/tmp/quake3-m7-final-default-UxpKy6` and both-product actual test_bigbox world/model draws. Root viewed both q3tourney4 images. tests/world-image-upload.test.ts no longer filters away source-tess-stage or leaves the model axis zero. Its actual draw observer proves Sarge textures change RGB during that draw; independent reviewer `bigbox_retained_draw_review` confirms attribution. Root final selection6/100 passes `/tmp/quake3-root-bigbox-final-join.log`. Whole-project typecheck12 and policy5 both exited0 after this final test correction. Original pixel tolerance remains M9, not an unfinished M7 item.

M5/M6 source checks found no new code omission but confirmed missing original water/BSP-stair comparisons and event IDs/parameters in the retained movement tape. Four current retained-value cases/89 assertions pass `/tmp/quake3-m5-m6-retained-evidence.log`; the old tape cannot supply uncaptured event fields. These require the still-pending reference authorization. Do not claim they are closed or launch native originals/retail QVMs without resolution.

All 941 source files and 56 build scripts have examination accounts. `docs/source-examination.tsv` preserves the exact 997-path join; `docs/SOURCE_EXAMINATION.md` states methods and exclusions. Private reconciliation `/tmp/q3-examination-reconcile-YhHrge/REPORT.md` has zero residual paths. Do not reread the whole source inventory or equate examination with implementation. Original tools/editor, legacy native platforms and unselected conditional profiles remain explicit qualifications. Current source mappings cover299 of997 inputs; that is a mapping count, not a port percentage. The existing updater now includes jdmainct, dormant syscall logging and direct VM-trace callers/tests.

Latest full E2E: `/tmp/quake3-base-final-refresh-eitwyX/RESULTS.md` and `/tmp/quake3-ta-refresh-Xo0H2V/REPORT.md`. All eight CPU/GL product lifecycle/combat/objective runs passed and closed their hosts. Root viewed the new base comparison sheet and selected Team Arena images; the worker inspected all Team Arena images. Fresh 1280×720 runs under `/tmp/quake3-ta-wide-ofk0oB` prove actual Team Arena reciprocal coordinates, menu navigation, movement/fire and pure admission. Its settled GL follow-up visibly shows the weapon; the original uninstrumented pose remains unexplained. New short base CPU/GL trace sessions passed in `/tmp/quake3-base-vm-trace-ErxtIU/RESULTS.md`; root viewed both active images. These are raw source at recorded import times, not frozen final-tree or unaided-match proof. Use private homes, absent DISPLAY/WAYLAND_DISPLAY, explicit offscreen SDL, dummy audio and no physical input/output/gamma. Native original/retail-QVM execution stays disabled pending permission; never copy retail assets into the project.

VM tracing and dormant syscall logging are implemented and independently reviewed: `/tmp/quake3-direct-vm-review-DHaaWe/tests.log`, 24 tests/319 assertions. All four direct owners preserve called→print→body and exact export IDs. The syscall helper has no production caller. Bot capacity work is implemented in weapons.ts, including reached 208-byte projectile clearing before parsing; `/tmp/quake3-weapon-capacity-ajIBYX/projectile-clear-test.log` passes31/303. Root's actual GL flare/menu join passes14/148 and network/server/command join50/1154. Logs are `/tmp/quake3-root-final-gl-join.log` and `/tmp/quake3-root-final-network-command-join.log`.

CommonCdKeyState.readFile/appendFile/writeFile are implemented against the actual common file API types. Root inspected source read-close-publication, append and write ordering; synthetic key cases pass. Automatic startup/config integration remains absent, not verification-only work. Credential file guards remain intact pending the existing user question about enabling normal runtime key-file handling outside the project. Do not change guards, read real credentials or enable persistence without resolution. Every searched root, not only fs_homepath, must be synthetic for isolated checks. Do not import keys into project files, tests, logs or builds.

Root typecheck11 and policy4 both exited0 after the final production changes: `/tmp/quake3-source-parity-root-typecheck-11.log`, `/tmp/quake3-source-parity-root-policy-4.log`. Root JPEG/weapon join passes93/1419 in `/tmp/quake3-root-final-jpeg-weapons.log`; the wider loader selection passes5/60 in `/tmp/quake3-root-final-jpeg-loader-2.log`. The loader test's later correction adds the source warning missing from failed-image expectations; no production behavior changed. Joined game/UI run94/946/3 GL skips and actual UI/render join84/1031/3 skips remain separate evidence. No shared setting was weakened.

Both new short product/backend trace flows passed and closed: `/tmp/quake3-base-vm-trace-ErxtIU/RESULTS.md`, `/tmp/quake3-ta-vmtrace-YvR1kN/REPORT.md`. Root viewed all four active/settled images. Current CPU/bot profiling made no project changes: `/tmp/quake3-cpu-rgb-current-uRyyVR/RESULT.md`, `/tmp/quake3-m19-bot-followup-I5cRY3`. Do not retry the proposed direct prototype accessor conversion unchanged: aas-debug-geometry.ts:159 spreads a reachability record and would lose prototype fields. CPU sampler ideas already rejected remain rejected. No timing improvement is claimed.

Integrated source work includes active flare helpers and ordered CPU/GL depth consumption, zero/one-vertex polygons, source raw VM memory/copies, bot actions/logging/cluster-zero caches, UDP oversize diagnostics, podium reset, team-overlay sorting prepass, dormant legacy reset menu and DEBUG team-goal helper. Flare review found only a missing required target.height type member, fixed by root. Actual flare CPU/GL context checks are `/tmp/quake3-flare-fullcontext-gl.log`; independent bot join71/750/2 native skips is `/tmp/quake3-routing-actions-log-join-review.log`. Root's VM raw-byte join94/1668 is `/tmp/quake3-root-bot-raw-relative-join.log`.

Independent UI review found six source defects, now reproduced fixed: float-to-int padding, list-style callback branch selection, edit-cvar reread, TA reciprocal coordinate profile, retained cinematic-name resolution and callback-sensitive menu-count loops. Runtime fixes are only src/ui/runtime.ts/tests/ui-runtime.test.ts; before snapshots `/tmp/quake3-team-ui-list-review-before.ts` and `...-tests-before.ts`. Joined UI201/2011/2 GL skips and independent final6/18 pass. Root added the existing reserveResource call on cinematic cache misses after the review. TA production factories and eight test callers use team-ui-640; cgame keeps stretch-640. Current JPEG evidence/before files are in `/tmp/q3-jpeg-decoder-yrbrry/SOURCE_REVIEW.md`; finalized recovery/fatal paths passed independent review, later marker/layout changes still need final review.

Historical checkpoints follow.

Latest work, September 9: CPU framebuffer-alpha correction is integrated in rasterizer.ts, engine/client.ts, main.ts, tools/verify-render.ts and tools/cpu-benchmark.ts, with five focused cases in tests/cpu-framebuffer-alpha.test.ts. RGB presentation now reads destination alpha as one and stores opaque alpha; RGBA offscreen defaults and source texture alpha are preserved. Root reviewed the diff against `/tmp/quake3-cpu-bilinear-words-PwUFhp/baseline-rasterizer.ts`. Root's 34 cases/144 assertions, typing and both actual offscreen comparisons pass in `/tmp/quake3-framebuffer-alpha-root-OXqsda/`; sessions20302,77751,4577 all exited0. No owned root process remains. q3dm1 CPU-original max119 became40; remaining RGB error is still open. No build, native original, retail QVM or physical-device use. Current workers: torch_remaining_rgb, Astra medium, readonly remaining sampling/geometry diagnosis; cpu_alpha_callers, Astra low, readonly existing GL-alpha expectation diagnosis. Neither owns production edits. M7's recovered all-map records and independent recommendation are noted at the top of STATUS.md; root has not closed that gate. Goal remains active.

Current work, September 9, 16:09 UTC: 14 milestones complete, six open; full-port goal active. M3 and M14 are accepted. Root's final raw and privately compiled typed-input smoke pass with identical output, and final typing session63660 exited0. The latest 44 background CLI runs closed cleanly, with 42 natural wins and two scoreless CTF objective-navigation runs. Together with prior logs they cover all 55 shipped BSP/AAS pairs. `/tmp/quake3-navigation-wave-TsFhkr/accepted-maps.tsv` indexes the evidence. test_bigbox/texturegrab have no AAS; q3ctf5.aas has no BSP. Original-reference permission remains unanswered.

All workers released their files and owned processes. m3_typed_input_smoke, Astra medium, changed only tools/sdl-smoke.ts; root migrated its two async callers in tests/sdl.test.ts. Root caught and the worker fixed a TypeScript stale-getter narrowing error using local snapshots, without casts. Final artifact `/tmp/quake3-m3-typed-smoke-sVX8c9/sdl-smoke-final` and raw-final/compiled-final logs pass. Root independently reran both. Its initial policy check passed; the only later tool change was those two local snapshots, followed by passing whole-project typing. No physical devices, foreground runs, native reference, retail QVM or credential access. Published executable unchanged. The goal was not paused or completed.

The bilinear packed-word experiment is rejected and rasterizer.ts is restored exactly to `/tmp/quake3-cpu-bilinear-words-PwUFhp/baseline-rasterizer.ts`. All seven framebuffers matched, but timings were mixed: opaque and linear regressed, while alpha/lightmap improved. No consistent speed gain justified integration. Its 177 focused cases passed with eleven native skips; all worker processes closed. Do not revive this candidate without new evidence.

The bot profile `/tmp/quake3-m19-bot-current-kczbBI` completed 4000 frames, heap check and server/hunk release. Its setup stall was 823.705 ms. The worker's only subsequent source change reuses cache.times between the previous-value read and successful write in updatePortalCache; it does not retain a borrow across later writes, callbacks or iterations. routing.ts is released. Exact prior file is routing.before.ts in that directory. Root review and its joined 57 cases/749 assertions pass, with eight existing optional skips; final whole-project typing passes. No measured speedup is claimed.

Earlier accepted view reuse spans bot memory, AAS lumps/entities/routing caches/scratch and collision BSP/patch records, plus the token snapshot byte loop. Every cached getter validates allocation lifetime. Root joins passed 97/1156 and 126/1301 cases/assertions. Direct findCache pointer walking is now accepted too, with root's 37/426 routing run. Latest finite 4000-frame evidence /tmp/quake3-m14-stall-9TBpLt/events-route-cost-completion.jsonl still has a 1042 ms setup frame, passes heap checks and records server disposal/hunk release. Its RNG seeds differ from earlier profiles; no attributable speed comparison. The repeat-address CPU candidate was reverted exactly at /tmp/quake3-cpu-repeat-neighbors-iwofv9. Published executable unchanged.

Both navigation workers finished exit0 and released their owned processes. q3ctf4 CTF won red1–0 at0:51; mpteam4 Harvester won red1–0 at1:53. Their 6000-frame raw CLI runs closed at5:00, with ordinary game logs under `/tmp/quake3-base-space-navigation-EXBn2h` and `/tmp/quake3-m14-mpteam4-ECM55g`. Root inspected both results. They establish finite natural objective navigation, not full trajectory coverage.

The routing ID-reuse fix is integrated in src/botlib/routing.ts and its existing cache test. Root inspected retirement, construction failure and loader pre-publication catch boundaries, then independently passed 37 cases/426 assertions with six optional skips. Typing and policy pass. Source allocation/accounting residue and free/link ordering are unchanged. No project source writer remains active.

Both further navigation workers finished exit0: q3ctf2 CTF blue1–0 at2:29 in /tmp/quake3-base-water-navigation-xQJ6F4, and mpterra3 CTF combat/flag pickups without capture in /tmp/quake3-mission-terrain-navigation-hPrCvM. Sessions54924/65738 closed after6000frames at5:00. Root inspected their game logs. Preserve logs and readonly retail data; no source changes were needed.

Routing-update borrow hoisting is accepted in routing.ts. Root traced the only caller: fresh canonical cache initialization and publication precede the synchronous kernel; the first host clock callback follows it. Loaded/existing caches bypass the kernel, so their supported header aliases are unaffected. Self-link update fields remain live. Root passed 37 cases/426 assertions, with six existing skips, then typing session3757 and policy62637 completed exit0. The worker's one unprofiled 4000-frame run at /tmp/quake3-routing-borrow-VqrbEZ recorded a768ms setup frame and clean heap/shutdown. Earlier measurements were profiled, so no isolated speedup is claimed. No source writer remains.

All four current-source M18 lifecycle runs passed and closed: baseCPU /tmp/quake3-current-base-cpu-98r4iI/run-2.log, baseGL /tmp/quake3-current-base-gl-PxrCsZ/run-2.log, TeamArenaCPU /tmp/quake3-current-ta-cpu-L4eary/run-4.log, TeamArenaGL /tmp/quake3-current-ta-gl-0IKPPW/run-5.log. Each used actual menu input, one-human FFA movement/fire, natural61000ms intermission, unready minimum, actual ready input and fresh pure next-map admission. Root read helper control flow and result logs. Independent m18_current_acceptance reconciled literal M18 gates with retained peer/bot/demo/authored-mod evidence. No game/check process remains.

Earlier failed attempts are retained. Base helpers watched obsolete BaseUi.create; exact owned PIDs257324/258340 were terminated without finally-cleanup claims. Current helpers observe prototype.initialize. TeamArena helpers now supply source bot services with bot_enable0 and read focused menu definitions without guarded renderer snapshots. Its retail PLAY submenu requires mouseEnter; corrected private runs use in_mouse1 with in_nograb1, joystick0, explicit offscreen SDL and absent desktop variables. Logical synthetic mouse motion is permitted there; physical mouse grabbing is not. All successful hosts closed normally. No new project framework, stage or build was created.

The second TeamArenaCPU timelimit print is source behavior, not a port defect. The actual recorded demo contains separate reliable commands3/time60000 and6/time66650 in the old map, followed by new gamestate and command7 in the fresh map. Original ExitLevel clears intermission after appending nextmap, and residual SV_Frame steps can run before Com_Frame drains commands. Root read the corresponding original and TypeScript code after independent diagnosis. No fix was applied.

The nonmipmapped CPU triangle sampling specialization was rejected at `/tmp/quake3-cpu-triangle-sampling-j2CgFC`. Seven scenes retained exact framebuffer bytes, but q3dm1 median worsened from 67.421 to 74.140 ms in a noisy run. Root's `cmp` confirms rasterizer.ts exactly matches baseline-rasterizer.ts. The worker released its files. Do not retry the derivative-length, repeat-neighbor or nonmip specialization candidates without new evidence.

All workers released their files. bot_memory_hotpath, Astra medium, implemented movement-state.ts view reuse and independently reviewed root's actions.ts cache. aas_link_views, Astra medium, implemented allocation-shared link views. Every original allocation.bytes read remains. Root corrected AAS cached metadata access after backing-buffer shrink and verified it in the existing relocation case. Changed tests are bot-movement-state, bot-actions and aas-entity only. No new framework, shared cache service or build. Root's existing 4000-frame mpteam1-to-mpteam2 helper completed exit0 in session13880, PID275794. Events: `/tmp/quake3-bot-record-live-Ydv0U7/events.jsonl`; stderr empty. Four bots survived replacement, the final map scored red1–0, heap check passed and server/hunk closed. mpteam1 median/p95 were5.657/10.530ms; mpteam2 were7.588/12.969ms, with a774.680ms setup frame. This is not a controlled speed comparison; RNG seeds are not pinned. No renderer, devices, native reference or retail QVM ran.

terrain_capture_cause, Astra high, found no source defect, then observed one natural mpterra3 return. Visor captured for blue1–0; 69 carrier samples spanned 67 AAS areas, with nearby-goal detours and active commands. Intermission occurred at153850ms after3069frames, and the heap check passed. Evidence `/tmp/quake3-mpterra3-carrier-trace-QpcLRl/trace.jsonl`. Process272445/session90233 exited1 only because final reporting accessed host.common after successful host.close and stdin/prototype cleanup. The executed helper and failed log are preserved; no second match was run. Hunk-release reporting did not complete. Native original/oracle/QVM execution remains excluded pending the unanswered permission question. No goal-status changes.

Stock-peer core recovery found the exact legacy reference core, PID1769428, September7 03:13:04 CDT. Its original fault is unaligned movaps in GL dispatch after original JIT→C calls; final abort is recursive signal-handler cleanup. Worker inspected offline with debugger auto-loading/network disabled, deleted its private extracted core, and did not execute the original client. Existing system core remains. The user has a pending async question about isolated background native reference execution for M5/M6/M10. Until answered, do not launch originals, native oracles or retail QVMs. No full-port or reciprocal interoperability acceptance is claimed.

M12's root runs pass 175 gates and nine seeded replays in `/tmp/quake3-m12-current-VdKXTZ`. Root's rasterizer selection passes 29 cases/119 assertions; M8 acceptance makes no speed claim. M5/M6 retained captures lack the specified water/stair and event-ring comparisons. M7/M18 inspection found no demonstrated missing production connection. Historical M18 lifecycle evidence is in `/tmp/quake3-final-menu-crossings-4IX7KN`. The existing inventory updater maps 271/283 core files and validates all target/evidence paths. The 12 untargeted core units are inactive or empty and documented in GROUNDING.md. These are partial mappings, not completion percentages. No build or test framework was added.

Latest cinematic and demo changes pass root's 253-case/61,958-assertion join, one retail picture skip, followed by whole typing/policy. Required developerPrint callbacks reach actual client developer gating and diagnostic-tool stderr; 77 remaining test callers were mechanically migrated and reviewed. M13 gate review found no missing defined service. Fresh corpus run136pass/9fail was resolved by two unchanged timeout reruns with --timeout120000, then seven source-backed token-expectation repairs passing root's 98 assertions. Existing exact native-reference logs were read, not rerun. Source token allocation/free semantics are unchanged; only tests/bot-character.test.ts and tests/bot-chat.test.ts expectations and two undersized fixture zones changed. M16 acceptance uses stated source timing/SDL delivery, not physical speaker latency. No owned corpus or root check remains running.

M11 root combined run passes 313 cases/3,690 assertions without failures: `/tmp/quake3-m11-current-tmPG9V/server.log`, completed session88120. RCON failures were one obsolete startup interception and late rejection observation in the existing test; no production fix was needed. Independent literal-gate review cleared M11. M17 independent review plus actual new input/render evidence clears its remaining gates: `/tmp/quake3-m17-hud-remaining-3i4Few`, `/tmp/quake3-ui-remaining-15Msoy`, `/tmp/quake3-binding-flow-HH3TeY/flow5.log`. Root inspected helpers/results/images. Postgame and HUD seeds remain labelled fixtures. All owned probes exited. Root's demo publication fix changes src/engine/client-demo-playback.ts, EngineClient.playDemo and its existing test; 19 cases/291 assertions pass after a reproduced premature-close failure. Combined typing/policy must follow current cinematic contract edits.

Root joined the immediate raw-ring/cinematic callback changes, AAS raw-presence/interrupted-load and filename changes, and demo/cinematic filename diagnostics. Actual cinematic callers now supply the required ordinary print callback. Combined audio/cinematic/demo/picture selection passes 249 cases/61,927 assertions, one retail skip; AAS selection passes 91/1,077, five optional skips. Whole typing and policy pass. The prior stale stencil prefix assertion now includes the actual swap-buffer log-comment and passes. No executable, stage or framework was created.

Output-isolation correction: the earlier menu helper omitted SDL_VIDEODRIVER and retained WAYLAND_DISPLAY. Its logs show NVIDIA, so its private-Mesa/no-physical-output claims were incorrect. Those processes are closed. The Xvfb helper now fails closed unless X11, its exact owned display, no Wayland display and dummy audio are explicit. Corrected Mesa llvmpipe and isolated offscreen NVIDIA RTX 5060 Ti both render the server-status/search tables correctly; `/tmp/quake3-menu-render-completion-HuwJ1Q/status-search-mesa-alpha` and `status-search-nvidia-offscreen`. All exact owned helper/window/Xvfb processes closed. The earlier RTX 3090/Wayland overlay observation is unresolved, not a universal NVIDIA defect. The latest CPU derivative shortcut was slower and restored exactly in `/tmp/quake3-cpu-sampler-profile-d2QErW`; no accepted performance change.

The latest source adds actual VM registry/symbol/entry tracking, early source input and in_restart, shared joystick release, audio empty-queue timing, cgame drop/fatal paths, opt-in GL call logging, server ban commands and netchan developer prints. Independent reviews and root's combined 177-case/2,016-assertion VM/client/UI selection pass, followed by whole-project typing and policy. GL wrapper mapped types infer the complete function signature, avoiding unsafe any in Bun zero-argument function signatures. Root's audio/draw union passes 15 cases/311 assertions with one GL-only skip; script corpus selection passes 95/19,882. Actual current base GL and Team Arena CPU play/restarts passed and closed every owned resource. Evidence: /tmp/quake3-root-vm-input-gl-SAO8qa/devmap-run and /tmp/quake3-private-ta-vm-input-5C8Q4u. No new build, staging cycle or framework. Exact libc termios/signal/calendar symbols are allowed; older approval notes below are obsolete.

Earlier recorded GL/CPU play, demo replay, postgame Next and Favorites joins are retained in STATUS.md; their intended private-Mesa environment is not driver proof. Future Xvfb children require SDL_VIDEODRIVER=x11, the exact owned DISPLAY and absent WAYLAND_DISPLAY. Set __GLX_VENDOR_LIBRARY_NAME=mesa and LIBGL_ALWAYS_SOFTWARE=1 for software GL, then inspect the actual reported vendor/renderer. Offscreen checks require SDL_VIDEODRIVER=offscreen and both desktop display variables absent, with no fallback. Use SDL_AUDIODRIVER=dummy and s_volume=0/s_musicvolume=0; initialized dummy sound is allowed for real menu callbacks. Long commands use background tool sessions. Physical devices remain forbidden. Use fresh homes and loopback-only sockets. For r_logFile use a private writable fs_basepath and readonly retail cdPath, never create gl.log in retail. Never read keys, copy retail into the project, or execute the original engine/retail QVMs. Older blanket private-display prohibitions below are superseded.

M4 is accepted. Merged retail corpuses have zero decoder failures: base 36 BSP/372 MD3/1132 JPG/787 TGA/614 WAV/11 RoQ; missionpack 57 BSP/515 MD3/1132 JPG/2224 TGA/1700 WAV/49 RoQ. Reports are outside the project in /tmp/quake3-retail-geometry-soG2NU, /tmp/quake3-m4-installed-images-Zbe0h1 and /tmp/quake3-audio-cinematic-audit-Ecmw36. Script counts, cross-reference results, source rejections and aggregate-hash stdout are recorded in STATUS.md. Renderer-registration stdout was truncated and does not establish M7 parity. Format gates do not establish visual or A/V timing parity.

Published dist/quake3-ts is the September 9 00:14 executable, 103,475,328 bytes. Build, sourcemap and previous 19:13 executable/metadata are preserved under /tmp/quake3-source-order-build-cZhf7L. It includes source renderer ordering/retained arrays, CPU hotpath, collision/movement, live download/packet diagnostics, retained client storage, M2 naming/listing and calendar corrections. Independent reviews, typing and policy pass. Root actual CPU client-level: 6/419; M5/M6: 92/807; M2: 6/52; joined client/protocol/download/calendar: 87/1710; CPU debug/show-images: 32/180. Final q3dm1 median79.511326 ms, unchanged RGBA. Compiled CLI help and both dedicated products pass: base combat home /tmp/quake3-source-order-base-vhRjr7; TA blue1–0 capturelimit home /tmp/quake3-source-order-ta-f7w3ki. All root check processes finished. This is an implementation checkpoint, not a pause or full-port acceptance. Next-wave edits are not in the executable.

Latest checkpoint: September 8, 19:14 UTC. dist/quake3-ts is the 19:13 executable, 103,397,504 bytes. It adds retained visibility, world/PVS controls, live lighting/rail cvars and reached SF_ENTITY execution to the earlier command/draw-surface work. /tmp/quake3-visibility-rail-checkpoint-WGwyXt preserves the candidate and previous 18:29 executable/build record. Final combined CPU session 9809 passed 199 cases and 4,179 assertions with 17 skips; typing 1360 and policy 10362 passed. Retail CPU session 76250 passed retained sprite/core/rings/lightning portal and long-rail overflow with 18 assertions. Independent final source review was clear within scope. All root processes closed. New compiled --help passed without engine/device initialization; latest gameplay evidence is raw CPU, not a new compiled gameplay run. The previous binary passed both compiled dedicated products. This is not a pause or full-port acceptance.

Current owners: all workers released their files. Seven Astra high/max agents completed lighting/rail settings, projected-light ordering, visibility review, rail geometry, actual procedural execution, obsolete planner/caller cleanup and final execution review. The geometry worker recovered from a provider capacity error. WorldFrame.railSettings, materialGroups and beginResolvedSurface are removed; no callers remain. Runtime uses required settings.lighting/settings.rail and typed SF_ENTITY publication without an eager mesh. Source visibility uses actual combined node contents, including positive leaf-index aliases. Root owns integration. Check scoped agent status before reassignment, never the historical full list. No stages/frameworks/per-leaf builds.

Completed longer post-BSP runs: base q3dm1 Visor 3–2 at60250ms; Team Arena mpteam1 red capture1–0 at177200ms. Both ran6300 frames and replaced maps at frame6000 with retained bots. Homes: /tmp/quake3-source-match-baseq3-M0a8ln and /tmp/quake3-source-match-missionpack-vHvKZd. Newest compiled homes: /tmp/quake3-scene-compiled-baseq3-DAlCQm and /tmp/quake3-scene-compiled-missionpack-6Z0ALB. No root process remains. Current raw pure menu selection passes6 cases/56 assertions; direct CPU screenshot rectangle/bounds proof passed at /tmp/quake3-screenshot-rectangle-GkwVE6. Do not run full screenshot, team-arena-menu-loader/feeders/owner-draw, engine-sound/screen or renderer-configuration suites: they open devices without sufficient gates. No SDL, Xvfb, audio, input, native/reference or retail-QVM execution; no retail copying or credential reads. No build.ts/check.ts, broad suites, stages/freezes/hash manifests or new test framework. Root owns one combined check once active contracts settle.

The older checkpoint and ownership lists below are historical.

Latest source checkpoint: September 8, 11:55 UTC. Implementation continues without SDL, virtual displays, audio, input capture, native reference execution, or retail QVM execution. Client-active/UI/cgame, mark, calendar, shared-entity and bot-adapter changes are live. ServerGame/GameRuntime/QvmGame now share actual data and CallSteps; commands, packets, frames, snapshots and bot callbacks consume the same canonical call bodies. Root connected qagame acquisition, VM restart and server/bot syscall dispatch. External GAME integration is still being checked, not accepted as complete. The 05:44 executable is unchanged. Do not run build.ts/check.ts, broad device suites, or per-leaf staging/build cycles.

Current exclusive work: game_shared_world_owner (Astra/max) owns server/world.ts and its test; external_game_owner (Astra/max) owns authored-VM additions in tests/server-engine.test.ts. Root owns engine/server-engine.ts, game/runtime.ts, source-bots.ts and integration. Other workers in the preceding wave released their files. This supersedes the historical owner lists below; check scoped status before reassignment. Root's six-suite run finished: 287 pass, 6 skip, three stale world-lifetime assertion failures; independent correction then passed 45 lifetime cases. Component typing/policy passed; final joined review remains. No root test process was left running at this checkpoint.

Active work: ordinary TypeScript implementation proceeds in the live tree under exclusive file ownership. This supersedes the historical bot-resumption restrictions below. Native reference execution/builds, refused audits, retail copying and credential reads remain excluded. No physical audio, desktop windows, focus changes, or input capture without explicit user permission. Do not run tests/team-arena-owner-draw.test.ts: it opens SDL without a device gate.

Source checkpoint, September 8 06:29 UTC: QVM decoder/interpreter/operations and their three focused test files are integrated. Root's QVM plus numeric selection passes 39 cases and 693 assertions; source and async reviewers cleared the final prepared-byte-PC and reserved-cell fixes. Whole-project typing and TypeScript policy pass. Independent private evidence: `/tmp/qvm-async-review-pbaTDI/` and `/tmp/qvm-source-review-wvidRM/`. Execution-contract comparison: `/tmp/q3-vm-execution-design-bfrFIU/`. No device use, retail QVM execution, native oracle, staging or build occurred for this unit. Engine mod adapters remain unimplemented; these files are not reachable from the published executable. QVM workers released their files.

The HUD workers completed without production edits; all their owned displays and helpers closed. Reports `/tmp/q3-head-rgb-mNM73A/REPORT.md` and `/tmp/q3-hud-cause-lEsDfd/HANDOFF.md` attribute the RGB differences to nearest-mip choice on the head and additive cigar, not screen01. A coarse-quad experiment improves Mesa but worsens NVIDIA; no universal sampler patch or relaxed tolerance was accepted.

Source additions accepted at 06:55 UTC: scalar math traps and user-command/entity/player QVM record codecs. Root's seven-suite union passes 66 cases and 4,330 assertions, including actual interpreter consumption of records written through VM pointers. Independent source and record reviews are clear; targeted strict typing passes. All QVM workers released files. Unsupported enum extensions and NaN payload preservation remain qualified; no engine mod launch is connected.

`/root/mpteam2_overload_completion` finished without production edits: four bots ran 600 seconds, 35 kills, no winner, and obelisk minima 2034/1410. No source divergence was established. The source retreat OR quirk was preserved. Evidence `/tmp/q3-mpteam2-overload-zeAy5l/`; its host/process closed.

Download migration completed and released: source runtime now borrows the actual common server-read handle, with one contained descriptor, original clear/debug ordering, retained root/resource identity and shared read accounting. Root's twelve-suite join passed 330 tests/3320 assertions; independent review passed 233/2442 with no remaining findings. The actual authored-package test proves install/remount/donedl/gamestate, not UDP or graphical cgame startup. No binary was rebuilt. The worker disclosed one existing isolated dummy-SDL child outside its narrower assignment; it exited and used no physical devices. No further SDL cases were run.

Source checkpoint 07:25 UTC: common VM memory/vector/cvar/console traps and actual recursive command/VM and registry-record joins are implemented. Independent Astra/max review cleared corrected field placement, cvar error severity, native-byte info strings and pre-callback argument capture. Root's prior nineteen-suite union passed 233/6509; subsequent cvar/info correction selection passed 40/246. Final combined typing/policy must run after active filesystem/renderer edits settle. This is not external-mod launch; 05:44 executable remains unchanged.

Source checkpoint 09:00 UTC: common, filesystem, renderer-resource/2D, sound, client-state, key, cinematic, collision, font and browser adapter units are implemented. Collision/font/browser and the actual shared-scene caller integration passed independent review. Root's earlier 48-suite selection passed 834/15447 with three device-only skips. The later actual client-level/UI/font selection passed 70/1153 with three device-only skips, collision 38/393, and sound reload 12/178. Actual typed cgame, HUD and menu callers now share entity/poly/light allocation. Font registration synchronizes the real renderer target. Whole typing passed before the numeric scene wave; two new test maps needed explicit generic types for policy and were corrected. Final typing/policy must follow the active edits below. These changes are not an external-mod launch and are not in the unchanged 05:44 executable.

Source checkpoint 09:40 UTC: numeric scene admission, delayed resource lookup, and retained pose passed independent review. The actual client, menu, and font selection passed 85 cases with three device-only skips. New vm/render-world-syscalls.ts and vm/script-syscalls.ts are live. Collision PVS now returns an allocation-backed byte accessor; server callers migrated with it. Root's world/scene and collision selection passed 18 cases. Handle and token-codec tests passed 19 cases. The 05:44 executable remains unchanged.

Source checkpoint 10:30 UTC: parser, world/PVS, action, character, weapon, and UI key reviews are clear. New engine/qvm-ui.ts is connected through EngineClient with pure-filtered bytecode selection, scoped recursive calls, asynchronous shutdown, and retained client renderer configuration. UI review is clear; 12 UI/acquisition cases pass. Goal review repairs include actual allocation bounds, source weight publication, and empty-filename load failure. The executable remains 05:44 and has not been rebuilt.

Exclusive owners: `/root/qvm_bot_chat` (Astra/max) owns chat.ts/chat-data.ts and new VM chat records/dispatch; `/root/qvm_bot_movement` (Astra/max) owns movement-routing.ts, new movement records/dispatch, and the shared lazy-goal-reader extraction; `/root/qvm_aas_queries` (Astra/max) owns aas-runtime.ts, bsp-entities.ts, spatial.ts, routing.ts, aas-movement.ts and new AAS records/dispatch; `/root/qvm_bot_weapons` (Astra/high) now owns library.ts, entity.ts and new library lifecycle/entity VM files, not weapon files; `/root/qvm_ui_composition_contract` (Astra/high) now implements engine/qvm-cgame.ts and console.ts async consumers; `/root/qvm_snap_vector` (Astra/low) owns only the new snap-vector VM helper and its test. Root owns client.ts, client-modules.ts, screen.ts, server-engine.ts and integration. Reviewers are read-only. Verify scoped state before reassignment. No staging, builds, native execution, SDL, virtual displays, or device use for these workers.

Current checkpoint, September 8 05:50 UTC: `dist/quake3-ts` is the 05:44 executable with collision/bot debug drawing, live collision controls, hyperspace cull/clip retention and jump-pad/water-movement corrections. The 04:38 routing predecessor is `/tmp/q3-live-bots-Gl1hwB/quake3-ts-routing`. Metadata: `dist/build.json`. Root's joined selection passes 268 cases with 8 skips, typing and policy pass, and independent reviews are clear. All four raw product/backend debug-mode/restart profiles pass. Compiled base CPU and Team Arena GL pass on private Xvfb with sound disabled. Evidence: `/tmp/q3-debug-surface-live-A0vBxy/`, `/tmp/q3-compiled-debug-base-XIIFD0/`, `/tmp/q3-debug-compiled-ta-5RMX1k/`. This is not a complete port.

The debug, collision, hyperspace and gameplay workers have released their files. Independent reviewers and both compiled acceptance workers completed; their owned processes are closed. Root owns integration and docs. Check scoped agent status before assigning the next graph unit. Source diagnostic failures remain preserved; do not weaken pixel tolerances or mistake existing alpha/RGB differences for the debug entry change.

Use the existing checks and actual game runs at integration. Do not resume per-leaf stage/freezing/build cycles or create additional test infrastructure. Preserve live edits and coordinate existing agent owners before changing their files.

Device restriction from the user: no physical audio, desktop windows, display changes or input capture without explicit permission. Future runs use a private virtual display, disabled game sound and SDL dummy audio where needed. The root gameplay runs and reviewer device checks have exited; no owned device process remains at this checkpoint. Do not change the user's global volume or device settings.

The executable descriptions below are historical and superseded by the checkpoint above.

Latest accepted executable: September 8 01:50:33 UTC, snapshot `build-EOL1Tz`. It contains both accepted demo joins, including signed sequences, retained player state and EOF header publication. Source/build/run evidence and the recoverable 01:21 executable are in `/tmp/quake3-demo-eof-build-NL21zJ/`. Final root source is `/tmp/quake3-port-stage-EdIaZv/test-f14FqF`; the final raw Team Arena GL EOF confirmation is `/tmp/quake3-demo-ta-gl-final-crggf5/`. Root's 137 focused cases, real snapshot-zero/expired-history ClientHost case, typing/policy and actual raw/compiled clients pass. All workers and processes assigned to these demo joins completed; no binary hold remains. Bot-prerequisite resumption is still unapproved. Earlier private/pending descriptions below are historical, not current actions.

The product goal API reports `paused` at this checkpoint; the lead did not change its status and continued the requested implementation in this turn. Historical statements that the goal is active describe the continuing full-port request, not the current product-control state. Do not mark the port complete or change the goal to blocked for this checkpoint.

Latest accepted executable: September 8 01:21:18 UTC, snapshot `build-uXfXaT`. It includes 39 combat/MAXCOUNT/common-error paths and 25 sound-warning paths after independent review and root checks. Both sound-enabled compiled products pass modes, restart, gameplay, audio initialization and 20-snapshot pure recordings. Build/run logs, final helper and the recoverable 01:00 executable are in `/tmp/quake3-combat-sound-build-QzO9ai/`. Source stages `/tmp/quake3-port-stage-XwJR9r/test-CH6N7b` and `/tmp/quake3-port-stage-UBWjyT/test-o4R1EY` are accepted and integrated, not pending.

The ten-file signed-demo sequence and retained-player-state repair was accepted in source at September 8 01:40 UTC. Root stage `/tmp/quake3-port-stage-zj2MgF/test-j7ITjI` passes 88 focused cases, the actual snapshot-zero/expired-history ClientHost regression, typing/policy and both sound-enabled raw CPU/GL clients. All 1,427 other canonical inputs were unchanged at integration. Its seven-file EOF successor `/tmp/quake3-port-stage-OpKbO9/test-L8ouIx` is also accepted through the final root stage and executable above. Preserve prior candidates. Bot-prerequisite resumption was asked asynchronously but has not been authorized. Normal parallel work continues; no goal status was changed by the lead.

Current accepted executable: September 8 01:00:53 UTC, image-grid drawing and renderer registration completion. Snapshot `build-2yW6xU`; build/run evidence and previous 00:32 executable are in `/tmp/quake3-show-images-build-RynPfr/`. Final raw source/evidence is `/tmp/quake3-port-stage-RRxfm5/test-wIk4ve`. Both raw/compiled base CPU and Team Arena GL pass modes, registration, restart, movement/fire, pure admission and clean closure. Normal parallel work resumed at 01:00 UTC after the requested usage burst.

Private pending work: combined combat/MAXCOUNT/common-error `/tmp/quake3-port-stage-XwJR9r/test-CH6N7b`, 393 combined cases plus typing/policy pass; raw client checks underway. Signed-demo successor `/tmp/quake3-port-stage-bLO2n9/test-mArvUg` requires the subsequently reviewed snapshot-zero engine adapter repair. Sound bank/WAV `/tmp/quake3-port-stage-VssPlw/test-FAtzNN` and 21 callers `/tmp/quake3-port-stage-PWOJ9w/test-fGV3iD` await root integration/review. The latter HANDOFF.md records an unintended execution of existing native oracle branches; that run is excluded from acceptance. No worker remains authorized to run those branches.

Compiled GL reconnect evidence for both products on the preceding executable is `/tmp/quake3-compiled-gl-reconnect-3XN6JH/`. Bots and previously refused prerequisite/audit operations remain stopped; the full-port goal stays active. No key file was imported.

## Earlier checkpoints

Latest accepted executable: September 8 00:32 UTC, including actual sector ownership/sectorlist. Root's tested source stage is `/tmp/quake3-port-stage-R0dNrB/test-AxAxhc`; build/run logs and the recoverable 00:10 binary are in `/tmp/quake3-sector-build-13lK0E/`. Both raw/compiled products pass sector clear, repeated clear, map replacement and existing gameplay checks. Private image-grid assembly is `/tmp/quake3-port-stage-UaqMJk/test-9ATbuU`; it is not accepted or live. Its contract and ownership split are in `/tmp/quake3-show-images-contract-wDYJi9/CONTRACT.md`.

Current active checkpoint, September 8 00:17 UTC: the accepted 00:10 executable includes the five-file server-info/userinfo/chat integration. Raw and compiled graphical and dedicated runs pass for both products. Root's 206-case combined selection, typing and policy pass. Exact frozen source is `/tmp/quake3-port-stage-IUGELA/test-LXWPgK`; source logs are in `/tmp/quake3-port-stage-xuMnf8/`. Build/run evidence and the recoverable 23:47 executable are in `/tmp/quake3-server-info-build-pbTsRO/`. Actual dedicated evidence is in `/tmp/q3-server-console-live-oNPIvW/`. The four-file sector implementation is private in `/tmp/quake3-port-stage-Mi59hx/test-oISM4x`; selected contract is `/tmp/quake3-sector-design-cFrd0D/SYNTHESIS.md`. The independent renderer image-grid contract is not implemented. Older checkpoints below are historical.

The user requested a brief pause, then explicitly resumed work. Recovery completed at `2026-09-06T07:00:38.597Z`. Work is active again. No staged client/common/UI/bot implementation was integrated during the pause.

This document preserves the pause-time backup and saved-stage history. Later integrations supersede the old next actions below. Use [STATUS.md](STATUS.md) and [WORKLIST.md](WORKLIST.md) for the current accepted checkpoint and dependency queue.

September 7 active recovery: the full-port goal remains active. The current 23:47 executable includes source debug controls, actual server kick commands, source primitive selection and preceding accepted additions. Both compiled products pass debug controls, host-protected kick commands, active/restarted renderer commands, invalid-start correction, captures, movement/fire, pure validation, 20-snapshot recording and closure. Exact identities are in [dist/build.json](../dist/build.json). The preceding 21:09 executable and build record are preserved in `/tmp/quake3-renderer-commands-build-Kpu4FM/`; earlier backups remain untouched. Natural startup and all four raw menu/intermission/next-map crossings pass with recorded qualifications. Previously refused bot BSP-entity and audit branches remain stopped. This is an active-work checkpoint, not a pause.

## Durable backup

The 23:43 source integration adds debug-sort cutoff, triangle/normal drawing and server kick commands through 26 exact reviewed postimages. Final frozen source is `/tmp/quake3-port-stage-nF5mVg/test-gJRkt2`; root combined checks and all four raw product/backend logs are in `/tmp/quake3-port-stage-8jIGnt/`. All three renderer review findings are corrected and confirmed; the independent server review is clear. The 22:49 executable and its build record are backed up in `/tmp/quake3-debug-build-GdcM5d/` before the accepted 23:47 rebuild. Both compiled products pass; build/run logs and helper are in that same directory. The next five-file server-info/userinfo/chat implementation has its own private owner and is not in this accepted source.

Latest accepted source: `/tmp/quake3-port-stage-nrTdN9/`, including all six renderer commands. The 21:51 executable includes that source and passes both compiled products; evidence and the preceding executable are in `/tmp/quake3-renderer-commands-build-Kpu4FM/`. Failed initial marker-parser logs remain beside successful corrected runs. The preceding image command and fixture corrections remain `/tmp/quake3-port-stage-U2IVHb/`. The subsequent 33-path primitive-selection join is integrated from `/tmp/quake3-port-stage-MwqXSi/test-V60LtP` after independent source/CPU/GL reviews and correction confirmation. Root raw gameplay and focused evidence are in `/tmp/quake3-port-stage-RJsOsC/`; both products/backends reach 121 presented frames, mode changes, movement/fire, queued sound, pure admission and video restart. The matching 22:49 executable passes both compiled products; `/tmp/quake3-primitives-build-m4ob0d/` preserves the previous 21:51 executable and the new build/run logs. Triangle/normal debug drawing is the next bounded source contract. Earlier game/CPU, image and reconnect evidence remains preserved. Completed command, build and run processes closed. The rejected nearest-word experiment remains `/tmp/quake3-port-stage-pJJJZe/` and must not be revived. The archive below is historical, not a backup of later changes.

Archive: `/home/buzzkill/Projects/quake-3-ts-recovery-20260906-GXIQtn/recovery.tar.zst`

SHA-256: `24e7a8028d7dd40d7a2c60eccc795a981bca77afbe2e82b3afbadfcb45943aa5`

The 1.1 GiB archive contains the complete workspace, including uncommitted source, dependencies, Git metadata, built executable and evidence, plus 643 task-specific `/tmp` packages. It contains 90,562 entries. `tar --compare` verified the archived contents against their original paths before workers resumed. `VERIFIED.json`, `inventory.json`, `archive-inputs.nul` and `SHA256SUMS` are beside the archive. Capture output is `/home/buzzkill/Projects/quake-3-ts-recovery-20260906-pz8DYI/capture.log`; that earlier directory contains the log, not the archive.

The two original source input archives in the workspace were excluded and remain untouched at their original locations. The reference source tree and external retail installations are not copied into this backup. Task fixtures already present in temporary packages are preserved for local recovery only, not redistribution. Symlinks are archived without following their targets. The external credential file was not read or included. Known credential filenames are rejected from collection before contents are read.

To verify the backup:

```sh
cd /home/buzzkill/Projects/quake-3-ts-recovery-20260906-GXIQtn
sha256sum --check SHA256SUMS
zstd --test recovery.tar.zst
```

To inspect a recovery copy without overwriting current work:

```sh
q3_recovery_extract=$(mktemp -d /home/buzzkill/Projects/quake-3-ts-restored-XXXXXX)
tar --extract --file /home/buzzkill/Projects/quake-3-ts-recovery-20260906-GXIQtn/recovery.tar.zst --use-compress-program=zstd --directory "$q3_recovery_extract"
```

The extracted tree retains `home/buzzkill/Projects/quake-3-ts/` and `tmp/` below that new directory. Compare files before restoring anything to active paths. Many oracle scripts use the original absolute `/tmp` paths and external reference checkout. Do not overwrite a newer stage or the active workspace blindly.

The repeatable capture script is `.artifacts/recovery-capture.ts`. Each run creates a new recovery directory. Run it only after writers reach an idle boundary; it compares all archived contents with the original files and rejects changes during capture.

## Accepted executable at the pause

Checkpoint eleven input: `1ad7de3e8360df93bccc213325e7b1ec3f74597ade0577616da0b97d384089f6`.

Actual executable: `d2bf6b6018acaee47b5efd4a728ab725f31c3376f0b931183779a63121dcf4c4`.

Main tests: 3,308 passed, 74 skipped. Selected actual GL tests: 468 passed, one skipped. Zero failures. Strict types and policy passed. The matching build, eight real raw/compiled dedicated UDP cases, eight matching retail viewer probes and eight compiled CLI tests passed. `.artifacts/checkpoint11-post-probes-snapshot-root.log` confirms the unchanged live input after probes. This covers the accepted keys/fields integration, not the staged work below. Subsequent documentation changes are outside that exact input.

## Saved stages and next actions

| Unit | Saved package | State at pause and next action |
| --- | --- | --- |
| Client clock/parser | `/tmp/quake3-client-clock-stage-KLfYfp` | Independent and root staged checks passed 195 tests and 2,404 assertions. Root native/public comparison passed four tests and 17,921 assertions. Finish root code review, apply only reviewed changes and the separate concurrency correction, then verify the combined tree. |
| Clock test correction | `/tmp/quake3-client-clock-concurrency-HNOyQ3` | One-line concurrent admission restoration, author 22 tests and 278 assertions. Exact test hash `ace740da93f86e2a4968bd7d3824626f23793badaaeaaa8ddc586e18710929f3`. Root rerun/integration pending. |
| Console/text | `/tmp/quake3-console-implementation-jmEdrh`, `/tmp/quake3-console-native-M8YUs6` | Original five-file stage has native/CPU/GL evidence. Independent review found mutable rectangle capture across awaited shader registration. |
| Console correction | `/tmp/quake3-console-capture-correction-i3OJ7L` | Causal red and green saved before worker usage failure. One test/six assertions passes. Full focused/native/strict/policy gates and corrected patch packaging remain. Follow PAUSE.md; do not run the original verifier in a way that rewrites frozen evidence. |
| Common frame | `/tmp/quake3-common-frame-stage-DcZsHY` | Frozen 13-file patch. Author typing/policy and 208 tests/4,740 assertions pass. Independent review now assigned, including recursive error cleanup and source null-client behavior. |
| Alternative routing | `/tmp/quake3-route-alt-stage-1arQT7`, `/tmp/quake3-route-alt-native-FuAjyT` | Author four native profiles and adjacent suites pass. Independent review paused with no confirmed finding but unfinished checks; resume from `/tmp/quake3-route-alt-independent-review-PAUSED.md`. |
| Bot logger | `/tmp/quake3-bot-log-stage-NHuHFJ` | Partial implementation. 28 tests/127 assertions pass, including 17 unchanged writable tests. Buffer/native/failure coverage and strict/policy remain. Investigate recorded acquisition error classification before acceptance. |
| Base UI | `/tmp/quake3-base-ui-foundation-pUI6XN` | Only draft state/cvars, incomplete import and no foundation test/type gate. Resume exact source framework/field/Confirm work. Native high-byte font difference requires shipped-QVM profile grounding before shared font changes. |
| Team Arena UI | `/tmp/quake3-team-arena-ui-contract-LNdlMN` | Frozen design only, SHA `0ff4536706caa880d20b3fc106fb9b2e9370963597c9d9a97024db6eb975d75b`. Root must read and approve the bounded first unit before implementation. |

The native clock/parser packages, design contracts, earlier accepted native references, review reports and incomplete stage files are all in the recovery archive. The workspace still has no commits; preserve its untracked work. Only root integrates live files. Normal gameplay, product menu flow, bots and full release acceptance remain open, with 0 of 20 full gates closed. Do not restart previously refused gl_render/math/protocol assignments under a rephrased prompt.

## Model policy after resumption

Use Astra only until the user says otherwise. Choose and announce the effort level for each new or reactivated worker. The earlier Sol trail review and Luna allowance are historical, not authorization for new assignments. Keep the soft cap of 25 active workers excluding the lead.

# Port status

The dependency graph remains complete. The separate [post-plan parity punch list](PARITY_PUNCH_LIST.md) has all 23 main corrections checked off and 14/15 optional-source groups accepted for their stated profiles. O15's core diagnostics are implemented and its 269 core conditional blocks are accounted for, but four OmniTimer SDK blocks remain unimplemented. Fresh team-AI source comparisons found no defined-path mismatch. External capabilities and deliberate security restrictions are now individually accounted for; their unavailable functionality is not marked complete. Completed milestones and source examination do not establish unrestricted parity.

The current executable was built September 11 01:57:48 UTC (September 10 8:57 PM Chicago) from commit `9e86b40`, snapshot `build-igcqbR`. It is 106,641,536 bytes; `dist/quake3-ts` and `/home/buzzkill/Projects/qfiles/q3a/quake3-ts` compare equal. It includes the final obstacle diagnostic and embedded endpoint overrides. Build typing and strict policy pass. Both games on CPU and GL pass actual compiled `r_smp` startup, movement/fire, drop recovery, fresh admission, video restart and normal exit without worker fallback. Eight terminated recordings contain 216 active snapshots; stderr is empty. Root inspected logs, decoded recordings and all four post-restart PNGs. Close-wall endpoints limit scene coverage. Evidence is `.artifacts/releases/2026-09-11-0157/`; the preceding 01:28 executable remains archived.

The same source commit adds standalone IPX address routines, ranking codecs and ID conversion, all 257 ranking keys and 14 gameplay report bodies. Independent reviews and lead's combined 62-case selection pass, with 717 assertions. These libraries are not an IPX transport, ranking service or gameplay-hook integration. Six broader browser fixture failures reproduce unchanged in the preceding snapshot; no full-suite pass is claimed.

Optimization remains unfinished, and this correction build has no new FPS measurement. The preceding 104,454,272-byte September 10 21:47:20 UTC executable remains recoverable in `.artifacts/releases/2026-09-10-2147/`. CPU remains too slow. The optimization measurements below describe their named historical builds, not the latest correction build.

The 21:47 build extends the existing guarded nearest-mip calculation to reached serial sampling, preserving texture/derivative reads and sampled values. Matched raw CPU client medians improve 9.8 to 10.3 and 10.0 to 10.4 FPS for Quake 3 in opposite orders, and 9.0 to 9.3 for Team Arena. Fixed-scene color/depth remain exact. Independent review, 2,566 frozen-baseline comparisons, existing renderer checks, typing and policy pass. A separate one-call dlight UV accessor change removes unused copies without an FPS claim. Evidence is `.artifacts/optimizations/2026-09-10-post-1939/reached-nearest/`. The lower dispatch threshold, clipping compaction and public mip-cache follow-on were rejected and restored. The public cache gained only 0.95% in one raw-client pair, with overlapping timing ranges.

Latest compiled CPU/GL timedemo medians remain the 20:39 build's 9.8/39.7 FPS for Quake 3 and 9.1/30.1 for Team Arena at 640×480. The 21:47 executable has fresh gameplay checks, not a new compiled FPS measurement. Its raw before/after measurements above are separate evidence.

The 20:39 build removes a frozen journal-mode cvar snapshot, uses canonical renderer lookup keys, and captures only the allocated position/UV fields consumed by material stages. These are allocation/lookup reductions without an isolated FPS claim. Existing debug-cvar and stencil fixtures now observe the real production paths. The strict retail MD3 CPU/GL comparison still fails at maximum32 against2; no tolerance was changed. The mutable tess-cell and nearest-mip decision-box experiments were rejected and restored, including their candidate-only tests. Further experiments remain separate from the published executable.

The 19:39 build removes temporary GL position-validation arrays at six call sites. Root reviewed the exact changes and passed 137 existing cases with 2,225 assertions, including actual GL execution. The measured hot call improves medians 3.25% and 0.36%, but the reverse difference is noise-sized and p95 is mixed. This small allocation removal has no isolated FPS claim. The other five call sites have allocation evidence only. A minimal CPU fixture repair prevents two rejected packets from satisfying an equality check; no CPU production change survived this round's measurements. Later candidates remain separate experiments, tracked in RECOVERY.md.

The 18:13 build retains a triangle-local nearest-mip cursor, its squared-bound refinement and a sorted-slot bitmap. The cursor improves base CPU render-pass medians 4.46% and 7.48% in opposite orders. Squared bounds improve those medians a further 1.017% and 1.46%. Team Arena latency is effectively flat. The bitmap improves GL medians 3.74% and 1.29%, with mixed p95 and no CPU regression. Color/depth and work counts match. Independent reviews and root's existing combined checks pass. These changes carry no isolated whole-game FPS claim. Evidence is `.artifacts/optimizations/2026-09-10-post-1700/`; live ownership is in RECOVERY.md.

Position-only and UV-only tessellation snapshots remove five and three unused objects per reached slot respectively. Matched GL medians improve 3.13% and 2.00% in opposite run orders with identical RGBA; CPU's one-pair 1.62% median reduction does not establish a speed claim. Root's existing three-file check passes 63 cases/632 assertions with four optional skips. Evidence is `.artifacts/optimizations/2026-09-10-post-1532/tess-components/`.

The separate private GL column-capture experiment was rejected and restored. Opposite-order medians changed +0.61% and -2.14%, without a repeatable gain. Both versions pass the existing GL suite extended to exercise standalone and tessellation discrete captures: 14 cases/180 assertions. The bounded test extension remains; no column-capture production code remains.

CPU exact-input norm reuse was also rejected and restored. Its median rose 3.34%, p95 rose 2.17% and combined CPU work rose 2.59%; color, depth, jobs and setup counts matched. Evidence is `.artifacts/optimizations/2026-09-10-post-1532/rejected-norm-cache/`.

A worker-only nearest-mip shortcut is retained after numeric and actual-code review for the verified Bun/Linux profile. It uses the old calculation at uncertain boundaries; public/serial sampling remains unchanged. Base render-pass medians improve 1.76% and 4.00% in opposite orders; combined CPU work improves in both games and both orders. Team Arena wall-time results are mixed, so no TA speed gain is claimed. Color, depth and work counts match exactly. The final dispatch simplification removes a redundant call for other texture modes; both product comparisons preserve output without a clear regression. Root's CPU checks pass 34 cases/1,248 assertions, typing and policy pass, and independent final review is clear. Evidence is `.artifacts/optimizations/2026-09-10-post-1532/worker-nearest/`. This change is included in the accepted 17:00 executable.

The CPU numerator-stepping and conservative LOD-norm-skipping experiments remain rejected and restored. The earlier sixteen-tile scheduler and scalar box/plane experiments were also rejected, including their candidate-only tests. Further work targets kernel/sampling and geometry-copy costs; do not repeat these experiments without new evidence.

The preceding 15:32 UTC executable added the console-poll scalar lookup and removed an unused viewport object from each CPU triangle packet. Its four compiled gameplay/recovery cases passed with eight recordings containing 216 active snapshots. Evidence and the superseded executable remain in `.artifacts/releases/2026-09-10-1532/`. [Release record](RELEASE.md).

Independent review found no defects in the three-file geometry-copy change. Index-only snapshotting improves matched GL medians 3.53% and 4.36% with exact output; the other two changes remove unused allocations without isolated FPS claims. That release's focused checks had an identical baseline/candidate cloud-observer failure, retained historically in RELEASE.md and corrected in the current source test without weakening its six-face or overflow requirements. Private diagnostics confirm the major CPU kernel/sampler functions reach FTL, but do not establish missed inlining. Rejected flat-color capture and sampler-fusion experiments leave no source changes.

Root verification passes 129 synthetic key cases/1,483 assertions, 70 script/module cases/542 assertions, actual source-bot removal/next-map admission for both games (2 cases/82 assertions), and actual raw CPU/GL played-match menu round-trips (50 assertions). Compiled direct startup/capture passes for both products/backends. Actual compiled menu selection now passes in both directions, followed by active matches and clean shutdown. Base-to-Team Arena uses keyboard menu selection; Team Arena-to-base clicks Quake3 and Yes. Each compiled direction uses a separate process. Root inspected the menus, popup and gameplay images. Evidence: `/tmp/quake3-resume-functional-OdXNrC/`, `/tmp/quake3-compiled-keyboard-AfSGUz/RESULT.md`, `/tmp/quake3-compiled-return-x00pmF/`, `/tmp/quake3-return-cpu-yaNGPT/`, `/tmp/quake3-return-gl-H63Qby/`. Earlier missed-click and overlapping-input attempts are not acceptance evidence. No production input change was needed: the private mouse driver now accounts for the original Linux delta gain. All owned processes are closed.

All twenty documented milestone gates are accepted for the selected runtime profiles. Core source accounting is complete: 273 implemented inputs, five native replacements and nine inapplicable inputs; none is labelled verified. All 941 source files and 56 build scripts have examination accounts. This is not a claim of universal native/driver equivalence or satisfactory performance. M9's numeric and scene-input qualifications remain explicit in COMPATIBILITY.md. The implementation/release graph is complete; the requested optimization phase is active. The previous blanket reference-permission blocker was an assistant error.

The final source-record reconciliation found and corrected AAS zero-length offset, overlapping-lump and raw-header-alias rejection. Root's focused parser/storage run passes 12 cases and 554 assertions; independent source review is clear. It also identified the omitted dormant `cg_particles.c` variant, `RB_DrawSun` and `RB_SurfaceDisplayList`. These bodies are now implemented and independently reviewed, without activating their absent original callers. Root's seven-file combined check passes 99 cases and 1,840 assertions, including actual GL execution, CPU particles/sun and both-product AAS parsing. Stale flare-tail expectations are corrected separately. Joined typing, policy, source accounting and final executable acceptance pass.

The three blue-sign pixels match after supplying the original fixture remaps. Captured axes/FOV resolve two trim pixels; the last static pixel is source-defined unstable-sort dependence on three extra cgame shadow submissions. Root reran the exact sort and mip comparisons and viewed current CPU, GL and original images. No renderer production correction was justified. The client-level fixture now matches GL framebuffer alpha precision, but retains known failing strict cross-driver pixel assertions; it is not reported passing. Evidence: `/tmp/quake3-sign-actual-H7XPx6/REPORT.md`, `/tmp/quake3-residual-actual-vOO1a1/REPORT.md` and `/tmp/quake3-functional-join-qHlpx2/`.

CPU batching is integrated in EngineClient. Its client-owned pool survives renderer, map and product changes, then closes during final disposal. Independent review accepts the batch/error ordering and host lifetime; 92 focused cases pass with two unrelated GL skips. Repeated q3dm1 640×480 render-pass medians improve from 76.989/77.463 ms serial to 66.853/65.516 ms parallel, with identical color and depth bytes. User CPU cost rises from about 80 to 133 ms per pass. GL scalar packing separately improves its matched medians 6.70–8.45%. World preparation removes one duplicate Set allocation per view without a measured speed claim. These are render-pass measurements, not full-game FPS. Evidence: `/tmp/quake3-cpu-batch-implementation-jKzkUz/REPORT.md`, `/tmp/quake3-gl-preparation-DGzCpz/REPORT.md`, `/tmp/quake3-scene-preparation-0vkPxo/REVIEW.md`.

Final gameplay exposed and corrected a stale filesystem borrow in server pure verification during reconnect-time `vid_restart`. The pure host now obtains the current filesystem view, preserving retired-view guards and valid/invalid checksum behavior. Root reran its regression: one case, 13 assertions. All eight final raw/compiled product/backend cases pass authoritative movement/fire before and after a managed drop, confirmed fresh admission, video restart, screenshots and normal exit with empty stderr. Their sixteen recordings contain 432 active snapshots. Root inspected the final logs, source/build agreement and all four compiled post-restart images; independent workers inspected both images in their assigned cases. Different wall clocks produce different views, so agreement is behavioral, not framebuffer identity. Corrected runner and generated artifacts are retained with the release; early-reconnect evidence remains `/tmp/quake3-fs-restart-trace-Hk6U5q/`.

The latest measured compiled timedemos are from 14:37, not the new 15:32 build. At 640×480 they measure median CPU/GL 9.0/36.9 FPS for Quake 3 and 8.2/28.8 FPS for Team Arena. These short recorded-client replays include presentation but exclude live server/bot/network work. One warmup and three measured replays use identical per-product recordings and default-quality settings; GL runs on RTX 5060 Ti. Previous 13:48 medians were 9.0/35.8 and 8.1/26.9; sequential measurements do not isolate individual changes. CPU is still too slow. Worker-count tuning, record traversal, weighted bands and fused tessellation copying showed no demonstrated gain and were removed. Further optimization remains active without reducing rendering quality.

The 13:48 CPU change halves transmitted triangle records by filtering each worker's row band, while retaining original sample order and serial failure recovery. Two matched render-pass medians improve 2.61% and 3.21%, with exact color/depth agreement. GL removes two validation arrays per source vertex with unchanged getter order; matched gains vary from 1.10% to 8.50%, with exact pixels. Cvar runtime getters avoid snapshots, and canonical names avoid repeated normalization without a cache. Root reviewed all four diffs and passed 21 combined CPU/GL cases with 304 assertions. Existing settings and cvar selections pass 35/383 and 36/185. CPU has independent review. Details and scope limits are in RELEASE.md and `.artifacts/releases/2026-09-10-1348/optimizations/`.

The owned-worker decoder avoids descriptor materialization on the fixed worker's native-cloned messages. Public parsing and main-side admission remain strict; all scalar, bounds and storage validation remains. Root's focused check passes six cases/119 assertions, and independent review confirms unchanged validation bodies and scheduling. One matched q3dm1 pair improves median 66.418 to 63.384 ms, 4.57%, with exact color/depth agreement. This is render-pass evidence, not an isolated full-game FPS claim. Evidence: `/tmp/quake3-owned-decoder-oTqmmJ/` and the current release directory.

The 12:55 build contains four small changes: lazy per-event-loop packet scratch with independent nested lifetimes; one private CPU schema-entry list instead of rebuilding it per triangle; indexed GL validation of private packed float32 arrays; and fresh culling cvar scalar reads without full snapshots. Root's event checks pass nine cases/48 assertions, including a new nested packet-borrow check. CPU checks pass five/65; existing culling live-toggle checks pass one/seven. Actual GL checks pass 39/369, and two matched render-pass pairs improve 4.11%/4.39% with identical pixels. The other three changes have allocation evidence, not isolated FPS claims. Root reviewed exact diffs; independent review confirmed event/CPU/GL changes and current cvar lifetimes. Evidence: `/tmp/quake3-event-buffer-nRXwSo/`, `/tmp/quake3-schema-allocation-QE3INL/`, `/tmp/quake3-gl-validation-WBNmzq/`, `/tmp/quake3-renderer-cvar-reads-nsaWcf/`.

## Earlier function-by-function source work

The dated records below explain earlier omissions, corrections and rejected experiments. Their open-item, serial-only and executable statements are superseded by the current summary above and the milestone table. They do not establish new blockers.

The earlier September 10 03:02 UTC executable corrected the user's startup failures. Its predecessor lacked the implemented Team Arena shared type-data parser. Source also rejected basedir `.` during numeric conversion; the terminal-dot case now returns numeric zero while preserving the path string, an explicit policy for an undefined source read. Root's 40-case/126-assertion parser run, typing and policy passed. Both products/backends started with `--data .`; these were startup checks, not full-match acceptance. Logs: `/tmp/q3-startup-fixed.1Is3vK/`. The help leads with the client launch command. Current compiled post-map screenshot captures succeed; the old missing-command report is no longer an open item.

Latest source corrections remove three invented restrictions: JPEG's 16-megapixel ceiling, bot weights' depth-128/65,536-node limits, and UI scripts' depth-32/65,536-token limits. Existing source dimension, allocation, script-buffer and menu limits remain. Actual boundary cases decode a 4,097 by 4,096 image, allocate 65,537 separators, and execute finite nested UI scripts. Root reviewed the production diffs. Its combined key/JPEG/weight run passed 111 cases and exposed a stale retail diagnostic expectation. Source inspection confirmed that Cadaver's undefined FS_ARMOR produces two errors in sequence; the corrected existing test checks both. Root's independent retail rerun passes 5,584 assertions across both products in `/tmp/quake3-root-weight-retail-diagnostics.log`. Root's independent UI runtime/menu run passes 153 cases and 1,409 assertions. Whole-project typecheck13 and policy6 pass after the production changes.

The added 4,096-entry UI resource-path and item-state limits are removed. Root's two actual-loader cases pass39 assertions: shader-name aliases use one shader, and three normal ui_load cycles retain4,098 offsets before a later real UI_Alloc exhaustion still fails. Independent review found no defect and passed three retained-pointer cases.

Runtime file reads, config journals and bot scripts no longer impose an invented128MiB quota. ZIP directory loading reads individual records and accepts the valid65,535-entry ZIP32 count. Source-valid TGA output above64MiB decodes; real signed-size and allocation boundaries remain. Root's TGA/journal selection passes17 cases/63 assertions, and independent combined file-boundary checks pass12/69. Diagnostic archive readers keep their separately documented limit.

The shared CPU pixel kernel and optional private-band worker controller are implemented and independently reviewed. Actual renderer replacement reuses the same worker pool, and fault checks preserve exact partial writes or retry unpublished work serially. The first dispatch strategy is rejected for normal use: q3dm1 median81.20ms serial versus94.45ms parallel, with identical pixels and1,120 measured worker jobs. EngineClient remains serial. The extraction itself showed no regression in a separate matched comparison. Batching and mailbox alternatives are being compared under `/tmp/quake3-cpu-dispatch-redesign-jbuL9H/`; measurement evidence is `/tmp/quake3-extraction-performance-HxFoTU/`. No worker speedup is claimed.

CommonCdKeyState's startup and configuration callers are now connected. Independent review caught the dedicated build's unconditional validator and short/NUL string behavior; these are corrected. Root inspected the actual source and passed the combined synthetic selection. No real credentials were read, copied, logged or included in a build. Game checks use copied PK3-only data outside the repository, with separate private home and CD roots.

The settings-only AAS accessor change is accepted as a bounded setup optimization after independent structural review and root's 59-case/1,193-assertion AAS/routing run. Two clean comparisons reduced the delayed setup frame from 731.067 to 682.276 ms and 745.526 to 708.960 ms. Map-end allocation/routing/match results match. Steady frame p95 is mixed; no general frame-speed improvement is claimed. Evidence: `/tmp/quake3-settings-shared-gTbFhs/REPORT.md`, `/tmp/quake3-root-settings-shared-join.log`.

An uninstrumented, quiet 640 by 480 q3dm1 comparison measures CPU render-pass median/p95 76.652/78.802 ms and hardware NVIDIA GL 12.176/14.975 ms. GL includes GPU completion. Both use the same camera, settings and 20 warmup/40 measured frames. This is roughly 13 versus 82 render passes per second, not full-game FPS. Fresh system samples average 87.7% idle. CPU remains slow without broad system saturation. The direct profile attributes about 77% of rendering samples to triangle, sampling and LOD work. Evidence: `/tmp/quake3-renderpass-compare-kKkLhf/RESULT.md`, `/tmp/quake3-direct-cpu-profile-IWSbBS/RESULT.md`.

Current TypeScript on the saved reference's Mesa driver matches q3dm1 exactly at 76,791/76,800 pixels, with only one-byte residuals. The CPU maximum-40 difference is traced to perspective-derivative mip thresholds; no universal driver sampler replacement is justified. Three mpteam1 blue-sign pixels retain maximum error 50 on CPU, NVIDIA and Mesa. Recorded compression/texture settings do not explain them; original per-draw inputs were not captured. M9 remains open. Reports: `/tmp/quake3-remaining-pixels-npL0fp/REPORT.md`, `/tmp/quake3-cpu-mesa-lod-cause-KiY6t0/REPORT.md`.

The examination pass is accounted for: 941 of 941 source files and all 56 build scripts, with no unmatched paths. [SOURCE_EXAMINATION.md](SOURCE_EXAMINATION.md) describes the methods and qualifications; [source-examination.tsv](source-examination.tsv) preserves the exact input-to-account join. This is 100% file-examination coverage, not 100% implementation or parity. Historical tools were examined, not turned into TypeScript applications; obsolete native platform profiles remain replacements or explicit exclusions.

M7's stated scene/material gate is complete for the selected runtime profile after root and independent reconciliation. Current shader corpus/reference checks pass8 cases/26,573 assertions. The retained57-map union now has current default-camera q3tourney4 CPU/GL captures and default test_bigbox world/model proof for both products. Root viewed q3tourney4's images and reran the corrected6-case/100-assertion image/model selection. The old test omitted source-tess-stage observations and left its model axis zero; both fixture errors are corrected, without a production renderer change. Evidence: `/tmp/quake3-root-m7-final-selection.log`, `/tmp/quake3-m7-final-default-UxpKy6/`, `/tmp/quake3-root-bigbox-final-join.log`. This does not close M9 pixel parity or M19 performance/release.

M6 is accepted after the original replay comparison added both event IDs and parameter slots. All 127 rows per product match, with 13 generated events and repeated ring wraps. Root's joined movement run passes 50 cases and 386 assertions without skips; independent review confirms the existing item and movement-mode criteria. Parameters are zero, matching original `PM_AddEvent`; arbitrary nonzero-parameter producers are not covered by this replay. Evidence: `/tmp/quake3-movement-events-XPPvtv/pmove-run.sh` and `root-tests.log`. M5's original water and actual-BSP stair comparisons remain in progress.

The latest implementation wave adds the six flare helpers and their actual ordered CPU/GL depth-read/draw path, source zero/one-vertex polygon handling, JPEG recovery and streaming row buffers, retained bot/VM byte copies, signed inventory indexing, cluster-zero AAS cache behavior, oversized UDP diagnostics, podium resets and the team-overlay prepass. The legacy reset menu and DEBUG-only team-goal helper are callable without changing their release-build activation. Root inspected the new source connections; focused tests are separate from full-port acceptance.

All four direct TypeScript module owners now print the original export IDs when VM debugging is enabled. Independent source review and the joined 24-test run pass with 319 assertions. The source syscall logger is implemented but remains dormant, matching its commented-out source caller. The invented bot weapon/projectile capacity limit is removed. An actual 24,904,456-byte configuration succeeds, and reached projectile-record clearing now rejects absent storage before parsing. Its focused suite passes 31 tests and 303 assertions. Root's separate actual GL flare/menu join passes 14 tests and 148 assertions; network/server/command checks pass 50 tests and 1,154 assertions.

Independent UI review found and corrected six defects: float-to-int padding conversion, retained list-style callback ordering, fresh edit-cvar lookup, Team Arena's distinct reciprocal coordinate scaling, retained cinematic-name resolution, and live menu-count traversal after callbacks. Existing cgame division and base UI aspect adjustment remain distinct. The source reproductions pass after correction. Root's game/menu/font/coordinate join passes 94 tests and 946 assertions with three explicit GL skips. Whole-project typecheck12 and policy5 exited zero after the final image/model observer corrections. Published executables are unchanged.

The latest eight full gameplay runs passed and closed all hosts: both products on CPU/GL reached menus, movement/fire, natural intermission, ready input and pure next-map admission; base CPU/GL earned two-client kills, respawns and scoreboards; Team Arena CPU/GL completed two-client One Flag pickup, delivery, reset and intermission. Reports are `/tmp/quake3-base-final-refresh-eitwyX/RESULTS.md` and `/tmp/quake3-ta-refresh-Xo0H2V/REPORT.md`, from source loads around September 10, 00:58–01:03 UTC. Root inspected the fresh base comparison sheet and Team Arena carrier/intermission images; the Team Arena worker inspected all 14 new images. Assistance uses ordinary positioning commands, never direct health/score mutation. Different clocks, views and animation phases prevent pixel-equivalence claims.

Separate fresh 1280×720 Team Arena CPU/GL runs exercise the corrected live coordinate profile, actual menu navigation and movement/fire. Evidence is under `/tmp/quake3-ta-wide-ofk0oB`. Its `GUN-FOLLOWUP.md` records a visibly rendered settled GL machinegun plus actual cgame model submissions, ready state and refdef; no persistent missing-weapon defect reproduced. Root viewed the settled image. The initial uninstrumented pose's precise cause remains unproven. New short CPU/GL sessions for both games exercise actual UI/GAME/cgame trace IDs, restore quiet operation, move/fire and close. Reports are `/tmp/quake3-base-vm-trace-ErxtIU/RESULTS.md` and `/tmp/quake3-ta-vmtrace-YvR1kN/REPORT.md`. Root viewed all four active/settled screenshots, including Team Arena at1280×720.

JPEG non-table marker length/error ordering is implemented and independently source-reviewed by root. The preceding joined decoder/weapon check passes93 tests and1,419 assertions. The wider loader selection passes5/60 after correcting its omitted source missing-image warning expectation. Latest source-limit and key-file work is recorded above. Final combined verification, original-renderer tolerances, reciprocal stock-peer interoperability, performance acceptance and executable release remain open. Runs are hidden/offscreen with dummy audio and private homes. Original-engine/retail-QVM permission remains unanswered; no physical output devices or credentials were used.

## Earlier gate evidence

September 9 framebuffer-alpha correction: the q3dm1 lightmap blend incorrectly used retained CPU destination alpha while the actual GL framebuffer has zero alpha bits. SoftwareRenderer now distinguishes RGB presentation from RGBA offscreen storage. Game, viewer and benchmark callers select RGB; the comparison selects the actual GL alpha precision. Source texture alpha remains available to alpha testing and blending. Root reviewed the exact rasterizer diff, passed 34 focused cases/144 assertions and whole-project typing, then reran both products against saved original world captures without diagnostic overrides. q3dm1 CPU-original mean/max error fell from 0.238607/119 to 0.168898/40; mpteam1 mean fell from 0.222522 to 0.202604. The torch brightness defect is corrected, but remaining RGB differences are not classified as acceptable driver variance. Evidence: `/tmp/quake3-framebuffer-alpha-root-OXqsda/`. All runs were background-only with explicit offscreen video, dummy audio and absent desktop variables. No original engine or retail QVM ran. Published executable unchanged.

M7 evidence recovery located all 57 merged map probes at `/tmp/q3-mission-world-cpu-GY78wj/summary.json`, four repaired patch-map passes at `/tmp/q3-mission-world-cpu-xevKfD/summary.json`, and two alternate-profile passes at `/tmp/q3-mission-world-cpu-6dMV8O/summary.json`. The last pair used an alternate q3tourney4 spawn and test_bigbox vertex lighting; they are not default-profile proof. Both shader corpuses and retained original material traces pass in `/tmp/quake3-shader-line-root-join.log`. Independent reconciliation recommends M7 acceptance; root's final evidence reconciliation remains pending. M7 is not closed by this note.

M3's remaining compiled-input requirement passes. The existing tools/sdl-smoke.ts now includes the actual SourceInputState, SdlGameInput and ClientKeys owners, including restart, window replacement and cleanup. Root independently reran final raw and privately compiled dummy-SDL CPU versions; their output matches. It retains all 1024 exact framebuffer-byte checks. The program rejects a missing isolation environment before acquisition. Root inspected the command-dispatch connection separately and passed current typing plus the migrated async smoke caller. Final logs: `/tmp/quake3-m3-smoke-root-raw-final.log`, `/tmp/quake3-m3-smoke-root-compiled-final.log` and `/tmp/quake3-m3-routing-root-typecheck-final.log`. The private final smoke executable is `/tmp/quake3-m3-typed-smoke-sVX8c9/sdl-smoke-final`. Earlier GL evidence remains separate; no physical device or full game rebuild was needed.

The latest production change reuses the portal routing timing view only between a previous-value read and its successful assignment. Root reviewed the absence of intervening callbacks/writes and passed 57 joined routing cases with 749 assertions and eight existing optional skips. Log: `/tmp/quake3-portal-relaxation-root-joined.log`. No speedup is claimed. A separate bilinear texture-word experiment retained exact pixels but had mixed timing and was reverted exactly. Evidence: `/tmp/quake3-cpu-bilinear-words-PwUFhp/RESULT.md`. No rejected CPU candidate remains in source.

M14's stated gate is accepted after independent review and root's map-set reconciliation. All seven modes have natural completed matches. Autonomous navigation now covers all 55 shipped BSP/AAS pairs. The latest 44 raw CLI runs initialized retail AAS, admitted source bots and closed cleanly after 6000 frames. Forty-two reached a natural fraglimit or capturelimit. mpteam5 and mpq3ctf3 ended scoreless with enemy-flag pickups and combat; those runs do not prove a complete capture-return journey. q3ctf1's separate new run supplies the bot evidence missing from its earlier human-only records. It earned red 1–0 at 0:52. The corpus contains 57 BSPs and 56 AAS files: test_bigbox and texturegrab have no AAS, and q3ctf5.aas has no corresponding BSP. They are not silently counted as played maps. The finite CPU/memory and reviewed ownership evidence below meet the resource clause without claiming universal frame or RSS bounds. Release performance and original-engine equivalence remain open.

New navigation logs are `/tmp/quake3-nav-*/console.log` and their product games.log files. The exact map-to-log index is `/tmp/quake3-navigation-wave-TsFhkr/accepted-maps.tsv`; the unchanged run contract is WORKER.md in that directory. Root compared the complete logged map set with the intersection of `/tmp/quake3-retail-geometry-soG2NU/missionpack.json` and `.artifacts/aas-owned-audit.json`, with no differences. Missing-target warnings were checked against decoded retail entities and original game source. Absent jump targets, deliberately freed info_null targets and a portal camera without a target explain them; no spawn fix was justified. Evidence: `/tmp/quake3-target-warnings-egFiOw/results.jsonl`. All runs were background-only, without physical display or audio use.

M18's four current-source lifecycle runs pass: base CPU `/tmp/quake3-current-base-cpu-98r4iI/run-2.log`, base GL `/tmp/quake3-current-base-gl-PxrCsZ/run-2.log`, Team Arena CPU `/tmp/quake3-current-ta-cpu-L4eary/run-4.log`, and Team Arena GL `/tmp/quake3-current-ta-gl-0IKPPW/run-5.log`. Each used real menu input to start one-human FFA, moved/fired, reached natural intermission at 61000 ms, remained unready for its minimum, and used SDL ready input to reach a fresh pure next map. All hosts closed. The GL runs used offscreen NVIDIA, not assumed Mesa. Team Arena synthetic motion used in_mouse1 with grabbing disabled. Earlier failed runner attempts remain labelled and retained. Root inspected the control flow/results and independently reconciled peer, bot, demo and authored-mod evidence. These satisfy M18's stated gate, not full-port parity.

Team Arena CPU printed a second timelimit message while loading the next map. Its recorded demo shows command3 at old-map time60000, a distinct command6 at old-map time66650, then the new gamestate and fresh map. Original ExitLevel clears intermission after queuing nextmap; SV_Frame may run another residual step before common command execution. Root inspected the matching source/port order. This is not a premature new-map limit or duplicate reliable-command execution, and no code was changed for it.

Current source accounting maps 271 of 283 core files and 297 of all 473 inventory files. The 12 core files without targets are explicitly inactive ranking/menu code or the empty ui_util.c; GROUNDING.md records their disposition. Header and syscall-wrapper mappings now point to actual typed owners. Native JIT entries point to the interpreter replacement. The existing updater validates every target/evidence path and regenerated the inventory successfully. These are partial mappings, not completed-file or port percentages; no runtime behavior changed in this accounting update.

Source shader registration now copies and reparses complete tcMod lines and skips compiler/fog tails as raw bytes. It preserves consumed braces, quoted argument reparsing, LF accounting and reached image callbacks. Source-buffer overflow rejects explicitly. GL strip emission now closes an active primitive when a managed draw rejects, preserving the error and incomplete draw phase. A subsequent readback and fresh draw work. Root inspected both implementations and passed 83 parser/material cases with 26,121 assertions, plus 24 isolated offscreen GL cases with 466 assertions. Typing and policy pass. Logs: `/tmp/quake3-shader-line-root-join.log` and `/tmp/quake3-gl-strip-root-recovery.log`. Five unrelated GL framebuffer-alpha expectations still disagree with the zero-alpha framebuffer; they were not weakened. No build or new test framework was added.

M8's six stated criteria have evidence. Root's current analytic rasterizer run passes 29 cases and 119 assertions. Existing scene-ordering, retail-map and repeated-frame results supply the other criteria. The current q3dm1 640×480 baseline is 68.406110 ms renderer median, 69.885444 ms p95 and 86.207209 ms instrumented full-pass median. Evidence: `/tmp/quake3-cpu-dispatch-performance-HZrdtx/baseline.log`. No FPS threshold was specified for M8. Its earlier remaining-work row conflated measured timing with M19 release performance and M9 original-renderer parity. Those downstream requirements remain open; this acceptance claims no speed improvement.

M14 has finite CPU/memory measurements for four source bots across q3dm1→q3dm2 and mpteam1→mpteam2. Both products completed 6000 fixedtime-50 frames with moving/attacking bots retained across replacement and every zone heap check passing. Whole-process CPU cost averaged 9.6–14.9 ms/frame by map; frame p95 was 13.8–20.9 ms. Sampled RSS peaked at 1007.6/1359.0 MiB during replacement, then fell to 833.5/901.1 MiB. Hunk usage was constant within each map. These finite observations do not prove indefinite allocation bounds. Base's post-close reporting failed after successful shutdown; Team Arena recorded released hunk and disposed server. Root inspected the helper and raw logs in `/tmp/quake3-m14-bot-cost-hlwszt`. Those processes are closed. Universal performance and allocation claims remain M19 work.

The 2.427-second mpteam2 frame is now traced to the source bot setup countdown reaching alternative-route generation, not deferred AAS initialization. Steady-play route-cache misses also cause long frames. BotMemory now reuses its payload view while checking ownership on every access and clearing the cache on free. Root inspected the change and independently passed its six cases and 52 assertions. Equal-length profiled runs reduced the setup frame from 2427 to 1662 ms wall and 3117 to 2041 ms CPU. Sampled subarray cost fell from 966 to 22 ms. These are exploratory measurements: the original run did not record wall-clock RNG seeds, and individual bot outcomes differed. Both runs ended red 1–0 at the same game time and released their hosts. Evidence: `/tmp/quake3-m14-stall-9TBpLt`. Remaining record-view hotspots are being reduced; navigation and sustained allocation acceptance remain open.

Root integrated view reuse for AAS lumps, alternative-route scratch, collision BSP records and patch records. The same finite background workload then observed a 1242 ms setup frame, 1562 ms process CPU, and 11.05/17.86 ms mpteam2 median/p95. It completed 4000 frames with four bots, passed its heap check and released the host. Its unseeded match ended 0–0, so this is not a deterministic performance or outcome comparison. Evidence: `/tmp/quake3-m14-stall-9TBpLt/events-views.jsonl`. Subsequent route-cache and AAS entity view reuse plus the token byte-comparison loop are integrated but not included in that timing. Root's two focused joins pass 97 cases/1156 assertions and 126 cases/1301 assertions, with six and three existing optional skips respectively. Final typing and policy pass. No new build or test framework was created.

Additional natural dedicated matches reached q3ctf4 CTF red 1–0 at 0:51, mpteam4 Harvester red 1–0 at 1:53 and q3ctf2 CTF blue 1–0 at 2:29. mpterra3 CTF had six kills, four enemy-flag pickups and two own-flag returns, but no capture. Each 6000-frame raw CLI run closed at 5:00 simulated time. Root inspected game and shutdown logs in `/tmp/quake3-base-space-navigation-EXBn2h`, `/tmp/quake3-m14-mpteam4-ECM55g`, `/tmp/quake3-base-water-navigation-xQJ6F4` and `/tmp/quake3-mission-terrain-navigation-hPrCvM`. No scores or positions were injected. These establish finite objective navigation, not full-map trajectory coverage.

A separate observed mpterra3 match reached a natural blue 1–0 capture and intermission at 153850 ms. Visor's 69 carrier samples crossed 67 AAS areas with active commands and ordinary nearby-goal detours. No return-route defect was demonstrated. Root inspected the executed helper, capture and heap-check records in `/tmp/quake3-mpterra3-carrier-trace-QpcLRl/trace.jsonl`. The process exited1 because final reporting queried the retired console after successful host.close and stdin cleanup. This is successful navigation evidence with a failed observer tail, not an exit0 run or a reproduction of the earlier scoreless match.

Movement-state fields, AAS links and bot actions now reuse allocated DataViews. Bot actions also retain one private wrapper per active client allocation. Lifetime checks and callback ordering remain at every original borrow. Root independently reviewed the changes, corrected stale AAS metadata access after a buffer shrink, and passed 81 joined cases with 785 assertions and five existing optional skips. Whole-project typing and policy pass. Log: `/tmp/quake3-bot-record-view-root-join.log`. The current-source dedicated run completed 4000 frames across mpteam1→mpteam2, with four bots retained, red 1–0, a passing heap check and clean server/hunk shutdown. mpteam2 median/p95 were 7.588/12.969 ms, but setup still took 774.680 ms. Evidence: `/tmp/quake3-bot-record-live-Ydv0U7/events.jsonl`. RNG seeds differ from earlier runs; no attributable speedup is claimed.

The scoped bot allocation review found fixed ownership bounds for route registries, update records, entity history and 64 reusable bot states. Production route-cache eviction follows the original free-zone threshold, not the unused max_routingcachesize setting. Its separate cumulative managed-ID exhaustion is now fixed: retired and unpublished IDs recycle without aliasing live or pending allocations. Source allocation residue, linked-list order and thrown failures remain unchanged. Root inspected the implementation and reran 37 routing cases/426 assertions, with six existing optional skips. Typing and policy pass. This fixes a lifetime limitation; it does not establish universal RSS or frame-time bounds.

A later CPU triangle sampling specialization preserved all seven captured scene framebuffers but worsened q3dm1 median from 67.421 to 74.140 ms in a noisy comparison. It was reverted, and root confirmed exact baseline source bytes with `cmp`. Evidence: `/tmp/quake3-cpu-triangle-sampling-j2CgFC`. No optimization is retained or claimed from that experiment.

Cache lookup now uses the same pointer walk without allocating a generator. The area-cache relaxation loop also borrows its fresh cache arrays within the callback-free kernel; loaded/existing cache aliases remain unchanged. Root inspected the call/initialization order, passed 37 cases/426 assertions with six existing skips, then typing and policy. The latest unprofiled 4000-frame run passed its heap check and closed its host, but its setup frame still took 768 ms. Earlier runs were profiled and RNG seeds differ, so no isolated speedup is claimed. Evidence: `/tmp/quake3-routing-borrow-VqrbEZ/events.jsonl`. The CPU repeat-address experiment showed only 0.08% median improvement and was reverted exactly; accepted rasterizer code is unchanged.

M10's legacy reference crash is now diagnosed from the existing system core for PID1769428. The preserved SIGSEGV frame shows an unaligned movaps destination in __glDispatchCheckMultithreaded, reached through original QVM→C→glXSwapBuffers. Its final SIGABRT followed recursive signal-handler shutdown. This establishes an original-runtime stack-alignment failure, not a TypeScript packet or pure-checksum rejection. The earlier debug crash was non-executable JIT storage. No original client was rerun; the temporary extracted core was deleted and systemd's retained core is unchanged. Reciprocal interoperability still needs a successful run. New native reference execution remains excluded pending the user's answer.

M12's current controlled retail matches pass all 175 gates and nine independent seeded replays. Both products cover FFA and CTF; the other five modes cover their applicable product. Real GameRuntime operations exercise scoring, flags, obelisk damage, death-generated cubes, intermission delay, ready input and next-map or tournament restart requests. Reports: `/tmp/quake3-m12-current-VdKXTZ/{baseq3,missionpack,modes}.json`. These runs do not execute queued engine commands or establish original whole-match equivalence. Existing native/QVM death, weapon and flag-transition fixtures supply reference state/event/score evidence; retained hosted matches supply actual restart and fresh-combat evidence. Independent gate review and root inspection found no remaining stated M12 gate.

M15's remaining render coverage passes independent review. Actual event/local-entity paths render the obelisk explosion and invulnerability shield in `/tmp/q3-special-render-qWzjMD`. Retail player presentation renders 56 states, 18 base and 38 Team Arena, in `/tmp/quake3-player-overlays-F8BLAh`; this includes flags, powerups, awards, tokens, medkit, shield phases, breath and actual retail dust surfaces. Root inspected the helpers, results and images. These are seeded CPU fixtures, not earned gameplay. Together with existing both-renderer gameplay, demo/recovery checks and M17's complete HUD definitions, they meet M15's stated gates. Full original-renderer comparisons remain M9.

M11's literal server gates pass independent source review and root's current combined run: 313 cases, 3,690 assertions, zero failures across server engine, frames, snapshots, client lifecycle, world, pure validation and downloads. This includes both retail products, actual local peers, map changes, retained resource ownership and independent instances. The first run exposed an obsolete filesystem-startup interception in an RCON test; correcting that connection preserved its cancellation and ownership assertions. No server production change was needed. Evidence: `/tmp/quake3-m11-current-tmPG9V/server.log`. The old remaining-work row incorrectly assigned full matches and physical-device acceptance to M11.

M17's remaining definition and navigation checks are complete. The Team Arena render union covers all 45 UI definitions and all 23 HUD definitions. Fourteen CPU fixtures cover both HUD selections, selected-player/Everyone, voice, duplicate score branches and both scoreboards: `/tmp/quake3-m17-hud-remaining-3i4Few/REPORT.md`. Actual offscreen UI renders Fight, CD-key and postgame; EngineClient input returns to Skirmish and starts active mpteam2 through Next: `/tmp/quake3-ui-remaining-15Msoy/run.jsonl`. The postgame seed uses the existing source debug command, not an earned match. Actual SDL Enter/F8 input changes Forward, retail Back saves it, and private config reload/reopening restores F8: `/tmp/quake3-binding-flow-HH3TeY/flow5.log`. Root inspected the actual helpers, result values and images after independent review. The prior base settings/start/join/team/postgame evidence supplies the other product flows. All owned probes closed.

M13's service and corpus gates pass. Fresh existing checks cover all AAS, characters, chats, weight configurations and exact weapon choices for both products, plus actual BotLibrary setup/map/frame/shutdown and independent RNG/actions. Seven stale token-lifetime assertions were corrected against source unread-token allocation and consumption; root reran all seven, 98 assertions passing. Two default-timeout corpus failures passed unchanged with `--timeout 120000`. The earlier eight-file run passed 136 cases; the two timeout reruns and seven corrected cases resolve its nine failures. Reference evidence remains `.artifacts/routing-time-sweep-O2-root.log` for 1,635 native queries, `.artifacts/bot-movement-integrated-root.log` for 1,180 route/physics/action records, and `.artifacts/loaders-commands-integrated-root.log` for native LTG/NBG and seeded avoidance. Root inspected those retained records and the current source allocation/free order. Broader game-bot navigation remains M14.

M16's literal timing and device-lifecycle gates pass independent review. Current retained movie playback drives actual EngineSound and dummy-SDL queued PCM while video advances. Source clock, EOF, loop and PCM-offset fixtures join existing delivery/repaint/starvation evidence and complete retail WAV/RoQ decode. All nine missing cinematic diagnostics are implemented with ordinary output separate from live developer-gated output. Root's combined audio/cinematic/demo/picture run passes 253 cases and 61,958 assertions, one retail picture case skipped; typing and policy pass. Physical speaker latency and native DMA identity are not claimed by the SDL replacement.

Demo fallback now publishes the opened file to the actual connection before the success diagnostic, leaving disconnect responsible for cleanup if printing aborts. The new focused check failed before the fix on the premature close; the joined demo selection passes 19 cases and 291 assertions.

Cinematic audio now writes complete chunks immediately into the source masked raw ring. Decoder backpressure and pending music remainders are removed; INFO dimensions and frame pointers publish at their reached callbacks. Bot script/AAS filenames use ordinary common diagnostics, route-cache writing preserves count-before-filename ordering, and AAS prediction handles raw presence and interrupted map initialization. Demo and cinematic filenames now use the Linux 4,096-byte limit and warning-before-open behavior. The actual client, viewer and diagnostic callers are connected. Root's joined audio/cinematic/demo/picture selection passes 249 cases and 61,927 assertions, with one retail case skipped. The joined AAS selection passes 91 cases and 1,077 assertions, with five optional cases skipped. Whole-project typing and policy pass. These changes are newer than the executable.

The menu probe exposed a verification-isolation mistake: setting DISPLAY alone left SDL free to select the user's Wayland desktop. Earlier claims that those menu captures used private Mesa were wrong; their logs identify NVIDIA. All affected owned processes are closed. Helpers now require explicit driver/display isolation. Corrected X11/Xvfb Mesa llvmpipe and offscreen NVIDIA RTX 5060 Ti both visibly render the real localhost server-status rows and FindPlayer result. Evidence: `/tmp/quake3-menu-render-completion-HuwJ1Q/status-search-mesa-alpha` and `status-search-nvidia-offscreen` in the same directory. The earlier RTX 3090/Wayland transparent-overlay observation remains unresolved under M9; later driver successes do not prove it fixed. No speculative material change was applied. The CPU derivative-length shortcut was slower and restored exactly; accepted performance is unchanged.

Common VM registration now reaches actual UI, cgame and GAME entrypoints, loading diagnostics, owner reuse and retirement. Typed CD-key callers invoke the actual UI callback and never read credential files. Root's combined VM/client/UI selection passes 177 cases and 2,016 assertions; whole-project typing and policy pass. Source input starts before the window and supports `in_restart`, including shared joystick releases across focus loss and window replacement. Audio starvation timing, source cgame errors, opt-in GL call logging and server ban commands are integrated. Root's muted audio/drawing selection passes 15 cases with one GL-only skip.

The current base GL client passes real Controls input/save, movement, active `in_restart`, `map_restart 0` and `vid_restart`. Input and game registrations retain the intended lifetimes. An actual `devmap` run creates a 1,413,654-byte private GL log with draw calls and 38 swap markers; `r_logFile` correctly remains cheat-protected during ordinary play. Evidence: /tmp/quake3-root-vm-input-gl-SAO8qa/devmap-run. Team Arena CPU independently passes movement/fire before and after both restart commands, preserves the server game registration across map restart and frees retired UI/cgame owners: /tmp/quake3-private-ta-vm-input-5C8Q4u. Both runs closed every owned window. Their earlier output-isolation claims require explicit SDL-driver evidence, not DISPLAY alone.

Fresh dedicated source-bot matches finish TDM 5–1 on q3dm7, Tournament 3–0 on q3tourney2, One Flag 1–0 on mpteam1 and Harvester 1–0 on mpteam3. Logs: /tmp/quake3-tdm-completion-MZrHxl, /tmp/q3-tournament-completion-ytstyX, /tmp/quake3-oneflag-natural-NCwVyt and /tmp/quake3-harvester-natural-UmI34F. The tournament's separate `succession-complete.log` records a 2–1 timelimit match, intermission at 121000 ms, real local spectator ready input and fresh combat after warmup/restart. The 3–0 fraglimit result belongs to `cli.log`, not that succession run. TDM and objective logs establish natural score/exit behavior; their intermission state timestamps were not sampled. Earlier selected-map wins cover all seven modes. The mpteam2 Overload 600-second non-win is a map-coverage qualification, not an identified code defect: its interrupted attacks and regeneration follow the original source. No AI rule was changed to force a winner.

New source carries raw mod integers through QVM records, protocol deltas, server frames and retained client snapshots, with checked conversion only at the retail cgame consumer. Independent review found one ping-publication ordering defect; it is corrected. Renderer changes retain failed-model storage, defer MD3 payload reads to actual backend/portal execution, preserve partial shader/deformation parsing, and apply exact raw GL state in both backends. Model registration now synchronizes actual queued renderer commands before loading. Bot movement preserves raw presence values until the reached AAS query. These changes are newer than the published executable.

Root reran actual both-product CPU client-level after the transport/renderer changes: three cases and 397 assertions pass. Private Mesa GL and CPU runs passed menu rendering, movement, shooting, bot admission, video restart and map restart; both GL-recorded demos replayed on CPU. Actual base CPU and Team Arena GL menu input reached settings, CTF creation and team changes. Base CPU additionally reached its real timelimit, game-produced postgame and q3dm2 through Next. Both product browsers joined real private localhost servers through Favorites and actual SDL input. Evidence includes /tmp/quake3-private-client-base-mesa-iEVvEs, /tmp/quake3-private-client-mission-mesa-dowWLS, /tmp/quake3-postgame-next-rKc24x and /tmp/quake3-browser-join-n5Wngt. All used private displays and muted dummy or disabled audio, and closed their owned processes. Full competitive matches and non-Favorites discovery remain separate gates.

Combined typing and policy pass. The platform allowlist includes libc tcgetattr, tcsetattr, sigaction, localtime_r and tzset; arbitrary libraries and symbols remain forbidden. Host calendar conversion replaces the partial timezone interpreter. Source drop/fatal corrections for collision, items, cinematic slots and sound registration are integrated, as are bot weight parser and callback-abort corrections. Root's latest calendar, weapon and world-image union passes 51 cases/553 assertions; two calendar helper tests run only through their child fixtures. No new build or published executable is part of this integration.

Current merged retail decoder passes cover every visible BSP, MD3, JPG, TGA, WAV and RoQ for both products, with zero failures. Base counts are 36/372/1132/787/614/11; missionpack counts are 57/515/1132/2224/1700/49 in that order. Shader/menu/animation/skin and bot corpuses also completed. Shipped Cadaver's invalid weight expression now follows the source failure return instead of aborting the host. These results do not establish visual or audio/video timing parity.

Actual all-map CPU rendering exposed three defects: self-stitching patch lifetime, q3tourney4's negative leaf area, and test_bigbox's generated second lightmap. All three production corrections are implemented and their affected maps render. Root independently reran default world/model frames for test_bigbox in both products. Their bounded memory profiles are recorded in COMPATIBILITY.md.

The last accepted executable is the September 9 00:14 UTC build (September 8, 7:14 PM CDT), 103,475,328 bytes. Its base game ran 75 simulated seconds with source-bot combat and clean exit; Team Arena CTF reached blue 1–0 capturelimit at 91 seconds and exited at 200 seconds. Evidence: /tmp/quake3-source-order-base-vhRjr7 and /tmp/quake3-source-order-ta-f7w3ki. The build, source map and preceding executable remain in /tmp/quake3-source-order-build-cZhf7L.

The published executable's q3dm1 640×480 CPU measurement is 79.51 ms median renderer time. Accepted source measured about 68 ms after the bilinear-sampling change, with unchanged pixels. Paired sampling runs improved by 1.05% and 5.57%; the smaller result is the conservative measured gain. Evidence: /tmp/quake3-cpu-sampler-performance-Pogw78. The scene has 55 batches and 1,743 triangles. Further CPU performance work continues, along with complete GL/device acceptance, full map/mode coverage, interoperability and release parity.

M0's acceptance gate is complete: the reference commit is clean and matches the 473-entry inventory, strict policy and typing pass, five selected policy-rejection/CLI cases pass, and the compiled artifact runs. The old table incorrectly placed final behavior-ledger closure under M0; that belongs to M19. This does not turn an open downstream acceptance gate into a reason to wait before implementing its available dependencies.

M1 acceptance: root passed 64 current numeric/binary/math/shared cases with 1,049 assertions. Independent review covers all 204 definition/trajectory enum values and both stat schemas. Direct comparison against `/tmp/quake3-current-port-build-7vGmAm/main.js.map` confirms that its already-built executable contains the exact current math, numeric, binary and definition files, including the bounds fix. Its existing base and Team Arena run logs are `/tmp/quake3-current-base-DjchrC/run.log` and `/tmp/quake3-current-ta-hvqKoO/run.log`. This closes the recorded M1 integration item without a new build. M5's separately recorded water/stair reference evidence remains open.

M2 acceptance: all stated retail mount, precedence/security/malformed-input and config-replay gates pass. The current worker selection passed 384 cases/7,063 assertions without failures or skips. Root reran both-product mounts, package checksums and actual config/startup/touch-file behavior: 12 cases/102 assertions. A stale CRC test now uses genuinely malformed DEFLATE, preserving the source's disabled payload CRC behavior. No production change remains necessary for these gates.

M4 acceptance: the six unlimited corpus reports in /tmp/quake3-retail-geometry-soG2NU, /tmp/quake3-m4-installed-images-Zbe0h1 and /tmp/quake3-audio-cinematic-audit-Ecmw36 contain 8,629 unique per-product entries, content hashes and package provenance, with zero decoder failures. Base/missionpack script traversal covers 30/59 shader files, 288/394 skins and 29/33 animation files. Cross-reference traversal checks 58/66 MD3 headers and 1,131/1,287 animation ranges. Eight old Tim skin references have four absent retail targets and retain source fallback. Shader source rejections are explicit: 89/97. All 49 Team Arena menu files produce 68 menus and 1,640 items. Root reran `bun test tests/material.test.ts tests/animation.test.ts tests/menu.test.ts`: 95 pass, 19,882 assertions. Independent format/corruption selections pass 73 cases/1,709 assertions and 90 cases/374 assertions with overlapping MD4 coverage. Script evidence is retained worker stdout with aggregate corpus hashes, not per-file JSON. A larger renderer-registration output was truncated and is not used to establish M7 parity.

## Earlier checkpoint: September 8, 2026, 19:14 UTC

Production additions since 11:55 include progressive JPEG, ADPCM/wavelet, loose CD-file copying, mod-directory switching, retained filesystem buffers and actual common hunk/zone ownership. Bot configurations, character profiles, fuzzy weights, chat state/data, goals, movement, weapons, BSP/AAS entities, loaded/generated navigation and temporary reachability records now consume allocated bytes. External GAME initialization, recursive commands and restart pass authored-module cases through ServerEngine. Retail modules remain direct TypeScript.

The active dependency is M2/M4/M5 → M7/M11/M13/M15/M16/M17 → M18. Client/server collision and snapshots share actual common storage. Main-zone bot allocations, the 512 KiB cvar/command small zone, routing graph/cache/head/link/crossing storage and game AI player/text records are connected. PK3 uses TypeScript DEFLATE with bounded incremental input/output. Console/packet events and journal replay share main-zone payloads. Lexer storage, preprocessor source/hash/conditional allocations, reached chat cleanup and the routing warning are implemented. Team Arena now parses incrementally through the engine's shared script handles and globals. Memory touching and renderer image-frame accounting are implemented; image creation binds before preparation. Pointer representations, token/define records and additional managed allocations remain qualified.

Source-ordered collision and renderer BSP loaders consume their allocated records. External GAME connection, snapshots, fast restart and reconnect pass an authored-module engine run. Stereo sequencing and CPU/GL draw-buffer commands are implemented; hardware stereo remains unverified. Allocated renderer state, source sorting, shader-index fixups and backend skipping are integrated. Retained visibility and r_drawworld/r_novis/r_showcluster/r_lockpvs now control traversal, including the corrected positive combined leaf-index alias. Live ambient, directed and rail cvars replace defaults. SF_ENTITY retains its source identity without an eager mesh; actual backend or retained portal dispatch generates geometry. The unused batch planner and four helper-only tests are removed. All workers in this batch released their files. No new framework or staging cycle was added.

Latest actual dedicated runs after BSP integration: base q3dm1 ended 3–2 at 60.25 simulated seconds; Team Arena mpteam1 ended with a red flag capture, 1–0 at 177.20 seconds. Both retained their bots across q3dm2/mpteam2 replacement, passed heap checks and closed after 6,300 fixedtime-50 frames. Homes: /tmp/quake3-source-match-baseq3-M0a8ln and /tmp/quake3-source-match-missionpack-vHvKZd. No networking or devices were used. Root's combined CPU client-level, command, settings and hunk selection passed 76 cases with three device-only skips, alongside whole-project typing and policy. The authored external GAME run passed 43 assertions. These results precede the active backend-memory implementation.

Published executable: September 8 19:13 UTC, built directly from src/main.ts. Root's combined CPU renderer/client selection passed 199 cases and 4,179 assertions with 17 skips. A separate actual retail CPU case passed retained sprite/core/rings/lightning portal dispatch and long-rail overflow. Whole-project typing and policy passed; independent final source review found no defects within scope. The compiled CLI starts without engine or device initialization. Gameplay evidence for this source is raw CPU, not a new compiled gameplay run. /tmp/quake3-visibility-rail-checkpoint-WGwyXt preserves the candidate and preceding 18:29 executable/build record. The preceding binary passed both compiled dedicated products with source bots. Full graphical acceptance and full-port completion are not claimed.

The source/hash/conditional unit passed 78 focused cases; the image-binding/frame-use unit passed 125 with four GL-only skips. The incremental parser consumed all 49 shipped Team Arena menu files, 68 menus and 1,640 items. Subsequent shared-handle integration passed 36 pure cases. These do not establish device-based UI or renderer acceptance. Retained RoQ reset and filesystem debug behavior are implemented. Broader renderer, gameplay, mod compatibility, performance and release work remains. Full-port completion and milestone closure remain unclaimed. Source mapping is 219/283 core files and 244/473 inventory files; these include partial mappings and are not completion percentages.

## Earlier checkpoint: September 8, 2026, 11:55 UTC

Implementation continues without SDL, virtual displays, audio, input capture, native reference execution, or retail bytecode execution. The executable remains the 05:44 build; current changes are source-only.

| Dependency | Current source state | Remaining connection |
| --- | --- | --- |
| M4 → M18: client modules | External UI and cgame connected; shared client-active lifetime, marks, calendar, and bot adapters reviewed | Broader compatibility and existing memory-accounting profiles |
| M4 → M18: server modules | Shared entity memory, game-data tables, synchronous/awaited calls, command/frame/snapshot consumers, and GAME loader/dispatch implemented | Authored-VM engine integration and final combined review in progress |
| M13 → M14 → M18: bots | Existing direct TypeScript play retained; command/chat/movement callbacks now support reentrant GAME calls | Verify the complete external GAME connection |
| M19: executable | Last published executable unchanged | Build only after source integration; device runs require permission |

Root's six-suite server/client/lifetime selection passed 287 cases and failed three obsolete ownership assertions. Independent source review corrected those assertions; the isolated lifetime suite now passes 45 cases. Component runs also pass command suspension, VM entity borrowing, calendar, bot callbacks, and snapshots. This is not yet combined acceptance of the external GAME connection. No milestone gate or full-port percentage is newly closed; source-accounting ratios below are mapping coverage, not completion.

## Earlier adapter checkpoint: September 8, 2026, 10:30 UTC

The M4 → M18 QVM compatibility prerequisite is implemented in source: image decoding, all 60 opcode identities, prepared-code execution, memory/stack ownership, recursive asynchronous syscalls and data restart. Scalar math traps and 24/208/468-byte user-command/entity/player records now operate through real VM pointer arguments. Root's combined selection passes 66 cases and 4,330 assertions; source, async ownership and record-layout reviews are clear. The decoder read all 27 installed QVM entries without executing them. External-mod engine adapters remain open; retail remains direct TypeScript. These source files are not in the published 05:44 executable.

The actual server-to-client download/install/remount/new-gamestate path passes with a locally authored package. The reviewed production fix routes download opens through common filesystem callbacks and handles, retaining containment and stale-borrow protections. Root's combined selection passed 330 tests/3320 assertions; independent review is clear. The executable has not been rebuilt. The bounded mpteam2 Overload recheck found no established port divergence or winner within 600 seconds; its original AI quirks remain unchanged. No physical audio or desktop devices are used.

Common, filesystem, renderer-resource/2D, sound, client-state and key VM adapters now use actual engine owners. Client server-command parsing publishes into the common command buffer; user-command sensitivity and overstrike retain their source values. Independent filesystem, renderer and client-state/key reviews are clear within the documented profiles. Root's combined 48-suite check passes 834 tests and 15,447 assertions, with three device-only cases skipped and no failures. Cinematic VM calls also passed independent review, with the unchecked retained-header reset still explicitly unsupported. These checks used no devices.

Collision, font and UI browser VM adapters are implemented and independently reviewed. Source collision traces retain the actual temporary-model state and trace bytes. Root corrected box handle 255's unused angle-pointer read; the combined collision selection passes 38 cases. Font registration now drains the actual render queue before reads, cache hits and capacity returns.

M7 shared scene storage is integrated into actual cgame, HUD and both products' menu callers. Independent review is clear, and root's eight-suite CPU selection passes 70 cases, including both products' GameRuntime packets driving client-level rendering. Three device-only cases were skipped. Frame-wide entity/poly/light budgets, add-time fog and successful-scene completion now have one owner. Sound playback also reloads nonresident registered rows through the actual bank, with 12 VM-to-mixer cases passing. All these checks were device-free.

Numeric VM scenes now use that renderer owner, with deferred model, shader, and skin lookup and full retained pose fields. Independent review cleared the corrected bad-reType error ordering and ignored flag bits. Root's later client, menu, and font selection passed 85 cases with three device-only skips. World-loading, entity-token, and visibility calls are implemented. Their joined review corrected PVS storage to retain the source collision allocation instead of copied rows.

Parser handles and VM dispatch passed independent review after corrections to number parsing, include boundaries, macro arguments, and diagnostic positions. Root's combined parser and actual retail BotLibrary selection passed 39 cases. World-loading, entity-token, and allocation-backed PVS review is also clear within the documented profiles.

Bot action, character, weapon, and UI key adapters passed independent review. Goal adapters are implemented; review corrected inventory bounds and weight-loading publication. Movement, chat, AAS, and bot-library lifecycle adapters are in parallel implementation or review. These helpers do not yet establish an external game-module launch.

External UI bytecode now loads through EngineClient and shares its actual services. Its asynchronous lifetime, recursive calls, retained configuration, and shutdown ordering passed independent review. Root's UI and module-acquisition selection passed 12 cases. Recognized retail modules still select TypeScript. UI memory accounting and full calendar time remain explicitly unsupported; external cgame and game lifecycles are unfinished.

Whole-tree typing and policy passed at the preceding parser checkpoint. A later typing run found a chat input-type leak, now corrected by its owner; final combined typing and policy must follow the active changes. The published 05:44 executable is unchanged. No physical audio, desktop windows, focus changes, or input capture are authorized without explicit user permission. Current work uses in-memory checks without SDL or virtual displays.

Source bots now play real base-game and Team Arena matches. The M13 → M14 → M18 bot integration edge is implemented and exercised, not waiting for a BSP-entity prerequisite or permission to write TypeScript.

| Dependency | Verified behavior | Remaining work |
| --- | --- | --- |
| M4 → M18: QVM compatibility | Interpreter, shared owner adapters, parser handles, and external UI loading/lifecycle; retail stays TypeScript | Remaining bot/server calls, cgame/game lifecycles, memory/calendar profiles, and joined runtime verification |
| M13: BSP entities → AAS → BotLibrary | AAS lifecycle, generation, clustering, optimization, writing and persisted RCD routing caches; corrected mpteam2 portal indexing | Broader map navigation and source qualifications |
| M14: game AI → user commands | Bots completed all seven multiplayer modes on selected maps; mpteam2 One Flag and Harvester replays now finish | mpteam2 Overload did not finish within 600 simulated seconds; broader navigation and parity remain |
| M18: server/client integration | Actual human-ready SDL input after bot-earned intermission reaches a new pure map with retained bots on base CPU and Team Arena GL | Broader competitive, objective and compatibility coverage |
| M5 → M7 → M8/M9 | Shared collision debug ownership, actual renderer callbacks, CPU and GL polygons, live collision cvars, hyperspace cull and eye-space clip retention | Broader renderer parity remains |
| M6 → M12 | Source float stores for shallow-water movement; jump-pad sqrt syscall and trigger cooldown ordering corrected | Broader gameplay parity remains |
| M19: executable | Published 05:44 executable includes these changes; compiled base CPU and Team Arena GL debug-mode, bot, movement/fire and restart runs pass | Full map, mode, compatibility, performance and release gates remain open |

Autonomous base matches reached fraglimit 3 and intermission: FFA q3dm1 at 28.50 seconds, Tournament q3tourney2 at 170.65 seconds (Visor 3–2, wins/losses updated), and TDM q3dm7 at 81.40 seconds (red 3–0). Four balanced Team Arena bots reached capturelimit 1 and intermission on mpteam1 in all objective modes: CTF 118.10 seconds, One Flag 230.65, Overload 217.55 and Harvester 42.60. These are bounded dedicated matches with stored AAS and fixed 50 ms frames, without forced movement, damage or scores. Bots-only intermission does not prove human-ready next-map behavior. Evidence: `/tmp/q3-base-modes-0GwFoe/` and `/tmp/q3-team-objectives-DWXVf2/`.

The mpteam2 routing crash is corrected. One Flag finished at 45.05 seconds and Harvester at 44.70 seconds. Overload ran 600 seconds without a crash, with 47 kills and damage to both obelisks, but no winner. Human-ready runs on base q3dm1 and Team Arena mptourney2 separately reached bot-earned intermission, accepted actual SDL attack input, and entered q3dm2 and mptourney3 with four retained bots and fresh pure admission. Evidence: `/tmp/q3-base-modes-0GwFoe/ready.ts` and its product logs.

Actual play drove fixes for empty bot IP filtering, bot channel delivery, disabled-bot map reconnect, fixed-position jump-pad sounds using entity -1, and terminal renderer/base-UI cleanup. Independent reviews also corrected aim-vector aliasing and integer decision deadlines. Joined typing/policy passed; root's focused selection passed 146 cases, migrated chat suites passed 28, and actual cleanup passed both products on CPU and GL.

`dist/quake3-ts` is the 05:44 direct Bun compile. Compiled base CPU q3dm7 and Team Arena GL mpteam1 pass debug modes, movement/fire, two bots and video restart. Both checks use private Xvfb, disabled game sound and dummy audio. Root inspected the captures; all owned processes closed. Evidence: `/tmp/q3-compiled-debug-base-XIIFD0/` and `/tmp/q3-debug-compiled-ta-5RMX1k/`. The previous routing executable remains `/tmp/q3-live-bots-Gl1hwB/quake3-ts-routing`; metadata is in `dist/build.json`.

The preceding routing executable passed dedicated and graphical save/reload in both products, with a 12,434-byte base cache and a 365,759-byte Team Arena cache. Filesystem traces confirm all Team Arena bytes were read. Evidence: `/tmp/q3-compiled-routing-base-9e23zf/` and `/tmp/q3-compiled-routing-ta-cBMHWh/`.

Forced regeneration of optimized retail AAS cannot recover stripped ground faces; those runs prove generation/write lifecycle, not viable regenerated navigation. Actual play uses stored retail reachabilities. RCD compatibility uses the source i386 little-endian layout with documented repairs to source reader offsets and managed cache ownership.

New source draws actual collision polygons during human-plus-four-bot play in all four raw product/backend profiles. Runs exercise modes 0/1/2, actual SDL movement and attack, video restart and clean shutdown. These runs used audio before the user's device restriction; future checks must use private virtual displays and disabled sound. The source release bot `Test` body returns zero, so mode 2 correctly produced no additional polygons. Evidence: `/tmp/q3-debug-surface-live-A0vBxy/`. Shared collision controls and both hyperspace fixes are independently reviewed. The final combined selection passes 268 cases with 8 skips; typing and TypeScript policy pass. Compiled captures prove mode changes and continued play, not independently visible debug polygons.

The three existing client-frame CPU/GL pixel failures reproduce byte-for-byte through the previous rendering entry point. Most aggregate error is alpha comparison against a GL framebuffer with zero alpha bits; remaining RGB maxima of 40 and 68 still exceed the existing tolerance. Those renderer differences remain open, without relaxed assertions. Evidence: `/tmp/q3-client-frame-cause-wUmVV0/`.

The RGB discrepancies are now attributed to mip selection on Sarge's HUD head and additive cigar stage, not screen01. Private coarse-quad sampling experiments improve Mesa frames but worsen NVIDIA pixels; no universal CPU sampler change was justified. Original assertions remain unchanged. Reports: `/tmp/q3-head-rgb-mNM73A/REPORT.md` and `/tmp/q3-hud-cause-lEsDfd/HANDOFF.md`. Both workers closed their owned displays and helpers.

Source accounting is **219/283 core files mapped (77.4%)** and **244/473 total files mapped (51.6%)**, including the current codec, memory and VM source mappings. These are partial source mappings, not a full-port completion percentage. No complete milestone gate is newly closed. Work uses separate live-owned files and focused checks, not per-leaf staging/build cycles.

## Historical checkpoints

Assessment: September 8, 2026, 01:55 UTC. This is a dated report, not an automatically updated dashboard. The [port plan](../PORT_PLAN.md) defines scope, dependencies and acceptance gates. Historical sections below retain the acceptance limits of their dated snapshots.

## Overall position

The accepted 01:50 executable includes signed-demo sequence handling, retained current player state and source-ordered EOF header publication. The two reviewed joins changed 15 unique paths. Root's final eight-suite selection passes 137 cases; the real CPU snapshot-zero/expired-history input and console regression also passes. The final Team Arena GL recording/replay cycle reproduces all 37 live snapshots, reaches normal EOF, closes the playback owner and enters a fresh pure map. Both compiled base CPU and Team Arena GL pass movement/fire, queued sound, video restart and 20 valid pure recorded snapshots; matching-source decoding observes every header and the -1 terminator. These ordinary compiled recordings do not prove the special snapshot-zero input case. Exact root stages: `/tmp/quake3-port-stage-zj2MgF/` and `/tmp/quake3-port-stage-EdIaZv/`. Final raw EOF evidence: `/tmp/quake3-demo-ta-gl-final-crggf5/`. Build/run evidence and recoverable 01:21 executable: `/tmp/quake3-demo-eof-build-NL21zJ/`. Root verified all 1,437 live/build inputs before this documentation update. No full milestone closes.

The accepted 01:21 executable adds the splash-damage float32 subtraction correction, source MAXCOUNT output, the real developer error command and sound-loading developer warnings. The combat/server/common join passes 393 root cases; the sound join passes 30 cases with native-reference branches explicitly excluded. Both joins pass typing/policy and actual sound-enabled raw CPU/GL clients. Both compiled products initialize queued SDL audio at 48 kHz, preserve source warning behavior, render image modes, restart video, move/fire, pass pure admission and record 20 valid snapshots before clean exit. Root inspected compiled captures and verified exact live/build input agreement. Source evidence: `/tmp/quake3-port-stage-XwJR9r/` and `/tmp/quake3-port-stage-UBWjyT/`; build/run evidence and previous executable: `/tmp/quake3-combat-sound-build-QzO9ai/`. MAXCOUNT is no longer missing. The grenade-profile regression proves 53 damage rather than the former 54; compiled runs are not claimed as that exact combat scenario.

The preceding ten-file signed-demo join passes 88 root focused cases, the actual CPU snapshot-zero/expired-history centerview and notification regression, typing/policy and sound-enabled raw base CPU/Team Arena GL runs. Its EOF follow-up is now accepted in the 01:50 executable as recorded above. Signed overflow, detached parse-entity storage and invalid negative payload lengths remain qualified. The bot prerequisite permission request remains unanswered, so that stopped branch has not resumed.

The accepted 01:00 executable adds `r_showImages` and unconditional renderer registration completion through the real catalog, command queue and both backends. Root integrated 80 reviewed paths, passed 90 combined cases and typing/policy, then verified actual raw and compiled base CPU/Team Arena GL startup, modes 1/2/0, video restart and restored gameplay. Each compiled demo contains 20 valid snapshots with pure admission, movement/fire and normal termination. Root inspected raw and compiled captures and verified exact live/build input agreement before this documentation update. Source evidence: `/tmp/quake3-port-stage-RRxfm5/`. Build/run evidence and the recoverable 00:32 executable: `/tmp/quake3-show-images-build-RynPfr/`. Existing source-indeterminate CPU attributes and common memory-touch accounting remain qualified.

Both products also pass actual compiled GL disconnect/reconnect against matching raw CPU listen servers on the preceding 00:32 executable. Each connection passes pure admission; post-reconnect demos show movement/fire and advancing snapshots. Evidence: `/tmp/quake3-compiled-gl-reconnect-3XN6JH/`. Physical input and full competitive/objective matches remain open.

The burst ended at 01:00 UTC as requested. Normal parallel work continues. Combat/MAXCOUNT/common-error, sound-loading warnings, signed-demo sequence handling and EOF publication are accepted as recorded above. No full milestone closes; source mapping remains 186/283 core files and 208/473 total.

The accepted 00:32 executable adds actual stable server sector membership and `sectorlist`. Independent implementation review is clear. Root's combined 13-suite game/server selection passes 378 cases with no skips or failures, followed by strict typing/policy. Both raw and compiled base CPU/Team Arena GL clients verify all 64 slots, retained heads after shutdown, unchanged repeated shutdown and rebuilt membership on q3dm2. Both compiled recordings retain 20 valid snapshots, movement/fire and pure admission. Root verified exact live/build agreement. Source/run evidence: `/tmp/quake3-port-stage-R0dNrB/`. Build/run evidence and the previous 00:10 executable: `/tmp/quake3-sector-build-13lK0E/`. This preserves actual membership cells without retaining retired map/game resources. Existing missing MAXCOUNT diagnostics remain qualified. The parallel image-grid work is private and not in this executable.

The accepted 00:10 executable adds `serverinfo`, `systeminfo`, `dumpuser` and dedicated-console `say`. Root integrated five exact reviewed paths, with all 1,429 other inputs unchanged. Shared `Info_Print` now serves actual client and server callers. Root's combined selection passes 206 cases, strict typing and policy. Raw base CPU and Team Arena GL clients execute info commands before and after video restart while moving/firing, checking audio and passing pure admission. Both actual raw dedicated entrypoints pass remote userinfo updates, console output, reliable chat and kick disconnect over localhost UDP.

Both compiled graphical products pass info commands twice, existing renderer controls, captures, movement/fire, pure admission, video restart, clean shutdown and 20 valid recorded snapshots. Base CPU moves 270.56 units and consumes ammunition 100 to 87; Team Arena GL moves 212.79 and consumes 100 to 93. Both compiled dedicated products pass six snapshots, acknowledged userinfo, exact chat and disconnect delivery, and normal exit with empty stderr. Root inspected the graphical captures and verified exact live/build agreement before this documentation update. Source evidence: `/tmp/quake3-port-stage-xuMnf8/` and `/tmp/quake3-port-stage-IUGELA/`. Build/run evidence and the preceding 23:47 executable: `/tmp/quake3-server-info-build-pbTsRO/`. Dedicated evidence: `/tmp/q3-server-console-live-oNPIvW/`.

The earlier sector and image-grid queue is now accepted as recorded above. Source mapping remains 186/283 core files, 65.7%, and 208/473 total, all partial or in-progress.

### Preceding accepted work

The accepted executable was built at 23:47 UTC and includes the debug-control and server-kick integration. Compiled base CPU and Team Arena GL both pass live debug controls, protected-host kick commands, source primitive modes, all six renderer commands, video restart, captures, movement/fire, pure admission and clean shutdown. Each demo has 20 valid snapshots and its normal terminator. Base CPU moves 284.75 units and consumes ammunition 100 to 87; Team Arena GL moves 216.43 and consumes 100 to 93. Root inspected both compiled JPEGs and verified exact live/build input agreement before this report update. Evidence and the preceding 22:49 executable are in `/tmp/quake3-debug-build-GdcM5d/`. Source accounting maps 186 of 283 core-runtime files, 65.7%, and 208 of 473 inventory files to 302 target paths. These counts include partial mappings and newly recorded older server code; they do not measure full-game completion.

The 23:43 source integration adds `r_debugSort`, triangle outlines and retained normal drawing through the source frontend, command queue and both renderers. It also adds real server `kick` and `clientkick` handlers. All 26 changed paths match the reviewed frozen join, with 1,408 borrowed inputs unchanged. Root's combined actual-GL and retail-server selection passes 314 tests and 13,285 assertions; strict typing and policy pass. All four raw product/backend combinations present 131 frames, exercise live debug controls and sort cutoff, move/fire, verify queued sound, pass pure admission and restart video. Root inspected the base CPU and Team Arena GL restart captures. The passed-vote server case disconnects an actual remote client through the shared command buffer and delivers its reliable disconnect over UDP. Independent source, CPU, GL and server reviews are clear. Evidence: `/tmp/quake3-port-stage-8jIGnt/`, final source `/tmp/quake3-port-stage-nF5mVg/test-gJRkt2`, server handoff `/tmp/quake3-port-stage-VxTe3u/HANDOFF.md`. The accepted 23:47 executable includes this source. The next server unit is `serverinfo`, `systeminfo`, `dumpuser` and dedicated-console `say`.

Three review corrections are included: empty text deformations still execute material effects before debug flags; CPU portal-created outline edges are not clipped twice; GL validates only attributes consumed by the selected debug draw, after its state prefix. Existing source-undefined discrete multitexture, eager frontend publication and unsafe numeric failure qualifications remain. Bot-backed `allbots` removal is not proven. No whole milestone gate closes from these units.

The preceding executable was built at 22:49 UTC. Both compiled products pass live `r_primitives` modes 1, 2, -1, 7 and 0, all six renderer commands before and after video restart, invalid-start correction, TGA/JPEG/levelshot capture, movement/fire, pure admission and clean shutdown. Each demo contains 19 valid snapshots and its normal terminator. Base CPU moves 248.97 units and consumes ammunition 100 to 89; Team Arena GL moves 154.16 and consumes 100 to 95. Root inspected both JPEGs and verified that live inputs exactly matched the build before this documentation update. Build/run evidence and the preceding 21:51 executable remain in `/tmp/quake3-primitives-build-m4ob0d/`. This is a bounded renderer checkpoint, not complete milestone acceptance.

Source primitive selection is integrated through 33 reviewed paths. Both backends now execute the source strip, indexed and defined discrete paths, with source-stage scratch ownership and live `r_primitives` sampling after bindings. Direct geometry remains separate. Independent source, CPU and GL/join reviews are clear after correcting one measurement-count regression. Root passes strict typing/policy, 161 actual CPU/GL cases with two isolated-gamma skips, and the corrected measurement/command and constant-depth selections. A separate 179-case caller selection has one framebuffer-alpha failure that reproduces unchanged before this feature. No assertion was weakened. Both actual retail clients present 121 frames while switching modes, moving/firing, checking queued sound, passing pure admission and restarting video. Root inspected both final JPEGs. Evidence: `/tmp/quake3-port-stage-RJsOsC/` and final corrected source `/tmp/quake3-port-stage-MwqXSi/`. The accepted 22:49 executable includes this source. Root's live seven-suite rerun passes 87 cases and 925 assertions. Paired discrete mode, source short-index allocation and direct retained-attribute qualifications remain explicit.

The source now includes `imagelist`, `shaderlist`, `skinlist`, `modellist`, `modelist` and `gfxinfo` through the actual renderer owners and client console. Independent component and final wiring reviews are clear. Root's combined command branch passes 91 cases with four optional skips; its final mode-list successor passes 32 cases with two fullscreen skips. Actual CPU and GL clients execute the commands, replace their owners at `vid_restart` and unregister before shutdown submission. Model records retain failed loads, original names, allocation sizes and LOD identities. Evidence: `/tmp/quake3-port-stage-ikdKS8/`, `/tmp/quake3-port-stage-nrTdN9/` and `/tmp/quake3-renderer-wiring-review-JiAgQc/`. These changes are included in the accepted 21:51 executable. Early registration/color-mapping timing and safe skin storage remain explicit qualifications in [COMPATIBILITY.md](COMPATIBILITY.md).

All eleven previously reported failures in the affected renderer selection are resolved by correcting stale fixture observations. Root's preceding combined selection passes 93 cases with two optional fullscreen skips, plus the separately selected built-in-image case. SDL RGB readback remains exact; its alpha plane must either retain submitted alpha or be entirely opaque, depending on the window target. Production SDL code is unchanged. Evidence: `/tmp/quake3-port-stage-U2IVHb/` and `/tmp/quake3-port-stage-K11610/`.

The 21:09 executable also includes the reviewed cached-gametype correction and signed CPU framebuffer word view. Actual clients start with invalid modes, reach active play, move/fire, pass pure admission and restart video. Root's earlier combined selection passes 156 cases with 18 optional GL skips. The CPU change preserves complete color/depth buffers and improves the measured lightmap workload; retail-map timing is unchanged and process-level timing varies. Evidence: `/tmp/quake3-port-stage-jJbB0w/` and `/tmp/quake3-port-stage-UjaAgi/`. A separate nearest-texture word-load experiment was rejected after two repeatable regressions and is not integrated.

The previous subjective 50% overall estimate and milestone percentages are withdrawn. They did not measure accepted source behavior or predict remaining work. Reported test counts below certify bounded implementations, not a percentage of the full game.

The plan and dependency map are complete. No implementation milestone has passed its complete acceptance gate: **0 of 20 gates closed**. Substantial tested implementations exist inside those open milestones.

Accepted renderer/audio source, 17:22 UTC: audio diagnostics/retained channel volumes, Upload32 error consumption, developer cvar tracing, terminal server/download disposal, SDL windowed fallback and ordered sky frontend/backend operations are integrated. The exact combined tree before this report is `/tmp/quake3-port-stage-OJ1UrY/test-1OvedK`, manifest `01ed4eb3fb6df772cba8017ccc477d904e4bbeaa9207c7fdb6e53871c3dbaad4`, 1,418 inputs. It matches the independently reviewed and root-tested candidate. These additions are included in the 17:24 executable below.

Root's final sky/SDL join passes typing/policy, 73 targeted CPU/native cases and both actual products: base CPU and Team Arena GL each present 104 frames, toggle live sky depth, verify actual SDL-queued tone samples, move/fire, pass pure validation and restart video before clean closure. CPU executes 821 nonempty stencil volumes and consumes ammunition 100 to 53; GL executes 1,012 and consumes 100 to 89. The viewer reports 1,743 triangles after correcting its missed source-stage caller; root inspected its actual frame. Evidence: `/tmp/quake3-port-stage-vnT3Z1/`. Earlier failed probe logs remain: q3dm1's retail `skyparms - 384 -` correctly has no outer box, so the corrected helper verifies cloud depth transitions and zero outer-box operations. Independent retail tests separately cover outer-box and empty-stage execution. Existing alpha/remap baseline failures and eager failure-publication qualifications remain open.

Latest accepted source, 18:54 UTC: PCX/BMP image loading, exact filename dispatch and retries, and synchronous renderer diagnostics now reach the actual filesystem, image catalog and client console. The independently reviewed 62-file join passes root typing/policy and 107 focused cases. Actual base CPU and Team Arena GL clients each present 104 frames, move/fire, verify queued sound, pass pure admission and restart video before clean closure. Eleven broader renderer failures reproduce on the unchanged baseline and remain open. Source and run evidence: `/tmp/quake3-port-stage-zT9qIE/`; detailed image handoff: `/tmp/quake3-port-stage-Kkzs80/HANDOFF.json`. Build/source fingerprints remain in those evidence records. Previously refused bot and audit branches remain stopped.

The final journal/cinematic source remains preserved at `/tmp/quake3-port-stage-Ep7g50/test-lXn76x`, manifest `7f89d8069ce33e476e4aafdf475dfba15ee76e5db9cb3a1d4d3fc735f993e1d3`. Its sound-enabled natural startup reaches Main without input or movie skips: base CPU 98.898 seconds, Team Arena GL 14.816 seconds. Root inspected both actual Main captures. Journal ABI and native allocation qualifications remain explicit.

The preceding executable was rebuilt at 21:51 UTC and includes all six renderer commands and the preceding accepted source. [dist/build.json](../dist/build.json) records its identity. Compiled base CPU and Team Arena GL execute every command during active play and after video restart, correct invalid startup gametypes, capture TGA/JPEG/levelshots, move/fire, pass pure validation, record 19 valid snapshots and quit cleanly. CPU movement is 267.48 units with ammunition 100 to 88; GL movement is 176.32 with ammunition 100 to 95. Root inspected both JPEGs and verified unchanged source. Evidence and the preceding 21:09 executable: `/tmp/quake3-renderer-commands-build-Kpu4FM/`. Initial runs completed gameplay but failed a helper parser that omitted the source echo command's trailing space. Those logs and helper remain preserved; the corrected helper passes both runs with strict typing and policy.

A separate ordinary raw base CPU menu run starts Main → Multiplayer → Create → q3dm1, enters time limit 1 / frag limit 0 in actual menu fields, moves/fires through SDL, reaches normal-speed intermission at 61,000 ms, stays unready through 66,100, and accepts Mouse1 ready before pure q3dm2 admission. Root read the complete helper and logs and inspected the Options capture. Demo readback records 1,168 valid snapshots, 105 sustained intermission snapshots and both gamestates. Evidence: `/tmp/quake3-normal-menu-KVivss/`, source manifest `2a3ea25bb9cc0be002f9cfacba764f773d48d7ee981ac0c4f5a1d1161730a3e9`. One human, bots/audio disabled, ordinary Escape past the known intro bug and decoded SDL text delivery remain explicit qualifications. This does not close the both-product full-match gate.

All four raw product/backend ordinary menu flows now pass through movement/fire, normal one-minute intermission, SDL ready input and pure next-map admission. Team Arena GL evidence is `/tmp/quake3-ta-normal-5YPTcU/`. The final base GL and Team Arena CPU crossings use the exact final 7f89d806 source; they record 2,694 and 371 valid snapshots, including 215 and 30 intermission snapshots. Evidence: `/tmp/quake3-final-menu-crossings-4IX7KN/`. An initial base Main capture was premature and black; it remains preserved, and a fresh seven-frame run captured the presented menu. These are one-human FFA flows with bots/audio disabled and ordinary movie skips, not full competitive or objective-match acceptance.

The preceding executable was rebuilt at 16:08:08 UTC. It includes the accepted single-player postgame/podium implementation, CPU/GL r_nobind handling, channel receive/drop diagnostics, native BeginFrame checking and synchronous portal correction, plus the previous MD4/font/menu/capture integrations. SHA-256: `392b52b5ce4b5a803061973bf572d605ec73f07b0328679f809a925c04245d31`. Frozen source `.artifacts/snapshots/build-h0ihuE` has manifest `09e316eee765aab823ef8abb7fd5f0c864d89790e817f38be9f45d1df5d2ea9e`, 1,418 inputs. Build typing, policy and drift checks pass. Actual compiled base CPU and Team Arena GL clients capture TGA/JPEG/levelshots, restart video, move/fire, record and quit. Both recordings contain 19 valid snapshots and normal terminators with pure validation. CPU movement is 297.02 units with ammunition 100 to 87; GL movement is 133.14 with ammunition 100 to 95. Root inspected both captured JPEG frames. Evidence and the recoverable preceding executable are in `/tmp/quake3-sp-binding-build-ERSleE/`. The preceding 15:06 binary and its evidence remain preserved at `/tmp/quake3-md4-font-build-WrYCXr/`. No full-match or full-port acceptance is claimed.

The final combined MD4/font/menu source review is clear, including an independent actual Team Arena GL movement/fire/stencil/restart run. Root's live typing/policy and 15-case native selection pass. The subsequent portal-timing fix is integrated through 13 exact postimages after independent source and merge review. Its combined font/portal snapshot passes typing/policy, 51 focused cases and six retail CPU/GL cases. Root's actual Team Arena GL client moves/fires, executes 726 nonempty stencil volumes, presents 89 frames, passes pure validation and restarts video normally. Evidence: `/tmp/quake3-port-stage-ea0orG/`, manifest `493bdfb4b7ec5993676fa5cf1e45ca44d25a6b6bfc302559d2b4f5599ab1d201`. This correction postdates the 15:06 executable. The private CPU sampler optimization was rejected for multi-workload regressions.

Native `RE_BeginFrame` error checking and actual client/server receive/drop diagnostics are independently reviewed, integrated and included in the 16:08 executable. The GL check passes 60 joined cases and an actual Team Arena GL move/fire/restart run with `r_ignoreGLErrors=0`. The network join passes actual loopback and local UDP cases for both products, plus a base CPU client run with real client/server receive traces. Typing and policy pass for both joins. Evidence: `/tmp/quake3-port-stage-KBYYZo/` and `/tmp/quake3-port-stage-f5ewfe/`. The separate five-file Upload32 error-consumption implementation is now integrated after independent review and actual client checks; it postdates the 16:08 executable.

Game-owned single-player postgame/podium behavior and all caller migrations are now integrated through 36 paths. Independent source and final caller reviews are clear. Root's joined typing/policy and 289 game/server/client cases pass. An actual base CPU client enters `spmap q3dm1`, reaches timed intermission at 61,000 ms, opens the postgame menu and executes real linked podium entity thinks. Winner celebration starts at 63,000 and stops at 65,300. The source rejected `timescale 20`, so the one-minute match ran at normal speed. One human and `bot_enable=0` intentionally yield duplicate source-ranked podium bodies; no bot match is claimed. Evidence: `/tmp/quake3-sp-client-SJLOJQ/` and `/tmp/quake3-port-stage-caI9g2/`. Exact integrated source before the following documentation update is preserved at `/tmp/quake3-port-stage-Uy6LWg/test-gfUhQb`, manifest `46dc6b74d286986d604c94d856568b605171a47852fdc557beb5122f59328b6e`, 1,418 inputs.

The 14-file `r_nobind` CPU/GL join is independently reviewed, integrated and included in the 16:08 executable. Root's 108 focused and seven native cases pass, as do strict typing and policy. Actual Team Arena GL and base CPU projected-shadow clients toggle the live cvar, move/fire, pass pure validation and restart video. CPU stencil shadows plus the override reach an existing coordinate rejection. The first volume pass rejects conservatively despite masked color writes; the later color-writing finish does depend on source-indeterminate coordinates. No guard was suppressed. Source stage and logs: `/tmp/quake3-port-stage-Xf5Gjo/`. The combined source is preserved at `/tmp/quake3-port-stage-n3SNvO/test-6U9n7K`, manifest `770dd1b00368c801ed8028bc0a7e3a28587bb707537a3936290cb1702db7e5ff`, before the documentation correction. Audio diagnostics, Upload32 error consumption and the ordered sky consumers are now accepted source additions, and are now in the 17:24 executable.

The graphical application joins both product controllers, cgame, sound and the listen server. Actual Team Arena menu input starts active CTF on CPU and OpenGL, followed by an alive Red player moving and firing. Projected model shadows and dynamic lights, patch stitching/distance LOD, portal/mirror views, retained music and RoQ streams, SDL layout-aware keys and the CPU sampler optimization are integrated. Filesystem pure/config restart and actual download/authorization/demo/MOTD owners are integrated. A source collision correction allows two human clients to reach flag capture and intermission; combined CPU and GL runs pass actual server pure validation through supported retail module selection. The compiled TypeScript client now also passes the original native server's pure gate and plays on both products. Reciprocal native-client acceptance, bots, full matches, renderer parity and shared memory accounting remain open.

| ID | Milestone | Gate | Principal remaining work |
| --- | --- | --- | --- |
| 0 | Inventory and strict tooling | Complete | Accepted September 9; final behavior-ledger closure belongs to M19 |
| 1 | Shared types, numeric and binary behavior | Complete | Independent numeric/byte and product-mapping evidence; accepted September 9 in source |
| 2 | Filesystem, commands and cvars | Complete | Accepted September 9; retail mounts, precedence, malformed input and config replay pass |
| 3 | SDL2 platform | Complete | Actual window, input, CPU/GL readback and cleanup evidence plus current raw/compiled typed input-owner smoke; physical device behavior remains separately qualified |
| 4 | Asset formats | Complete | Accepted September 9; both-product corpuses, cross-references and malformed inputs pass |
| 5 | Collision and visibility | Complete | Root's unchanged-original retail water/stair sweeps and point-contents probes pass; existing curve/transformed/numeric qualifications remain explicit |
| 6 | Shared movement and definitions | Complete | Both-product original 127-row replay now includes event IDs/parameters; root's 50-case movement run and independent literal-gate review pass |
| 7 | Scene and material frontend | Complete | Accepted September 10 for the selected runtime profile: original scene/material evidence, 57-map visibility union and current shader corpus; pixel tolerances remain M9 |
| 8 | CPU renderer | Complete | Stated analytic, edge, ordering, retail, deterministic-frame and measured-cost gates pass; release performance and original-renderer parity remain separate |
| 9 | OpenGL renderer | Complete | Selected-profile driver/readback, source scene/material/model paths and original screenshots with explicit rounding, scene-input and sampler qualifications; see COMPATIBILITY.md |
| 10 | Protocol, channels and demos | Complete | Original-client pure gameplay, movement/fire, map change and reconnect pass both products in root reruns; retained packet/channel/demo and reverse-direction evidence meet the stated gate |
| 11 | Production server | Complete | Independent source/gate review and current combined retail/local-peer run: 313 cases, 3,690 assertions; stated server gates pass |
| 12 | Authoritative game and Team Arena rules | Complete | All seven scripted modes pass current gates; retained original state/event/score fixtures and hosted lifecycle evidence meet the stated acceptance |
| 13 | AAS and botlib | Complete | All stated services, corpuses, reference routes/goals and independent-context gates have evidence; broader game-bot map navigation belongs to M14 |
| 14 | Game bot AI | Complete | Natural matches in all seven modes, navigation across all 55 shipped BSP/AAS pairs, objective use and measured CPU/memory with reviewed ownership bounds; release performance remains M19 |
| 15 | Client gameplay and presentation | Complete | Demo/recovery, defined effects and HUD states, and actual both-renderer gameplay gates pass |
| 16 | Audio and cinematics | Complete | Sample/spatial, complete retail decode, source A/V timing and actual dummy-SDL queue/lifecycle gates pass under the declared SDL adaptation |
| 17 | Menus and shared UI | Complete | All retail definitions parse/render; actual settings, start, join, team and postgame navigation pass, including private Team Arena binding persistence |
| 18 | Complete game integration | Complete | Four current-source lifecycle crossings plus retained actual network, bot, demo and authored-module integration meet the stated gates |
| 19 | Final parity, performance and release | Complete | Accepted September 10 against the recorded profiles; new parity discoveries and further optimization are separate follow-up work |

## Dependency position

### Earlier joined source and executable evidence

The MD4 registration/scene/writer/stencil join is now integrated through 44 further files, following its earlier four resource/entity predecessors. Separate registration, publication, stencil and root-composition reviews are clear within their documented bounds. Corrected private input `/tmp/quake3-port-stage-9r0diC/test-sJdIdr` has manifest `4d11f2f896ee31b0fca5bce32050594e7e7c8a1330d82579ceb0e0058d707041` (1,416 inputs). Root drives actual base CPU and Team Arena GL clients through ordinary movement/fire, pure validation, nonempty shadows, video restart and clean shutdown; both present 89 frames. Source tess indices remain unchanged; draw publication includes only referenced vertices in original slot order. Two native image comparisons remain failures reproduced identically on the unchanged baseline: MD3 retained color maximum difference 32, and scoreboard icons mean 3.2107063802083333/maximum 126. Logs and the current-interface client driver are under that stage parent. The earlier driver failed because its observer used the retired `drawShadow` method; the corrected observer uses actual `drawImmediate`, passes strict typing/policy and completes both runs. Inherited deferred portal probing is being corrected separately, not accepted as source-order parity.

The 17-file shared-font join and two-file immediate menu-to-map repair are also integrated. Font review independently passes 25 actual CPU/GL cases; the eleven migrated UI/HUD consumers pass 92 cases. A real renderer owns the shared six-font capacity, and renderer replacement resets it even when SDL retains the window. Menu/map author and reviewer reproduce actual CPU demo EOF followed immediately by active pure map entry; this deliberately repairs the source's null-UI sequence. Root's live five lifecycle cases pass. After all three integrations, joined typing/policy pass and the live MD4/font/map native selection passes 15 cases with 210 assertions. Evidence: `/tmp/quake3-port-stage-7cx4AF/`, `/tmp/quake3-port-stage-ZUJZD7/live-root.log`, and `/tmp/quake3-port-stage-9r0diC/live-joined-{native,typecheck,policy}.log`. These additions postdate the 12:20 executable; no full milestone is closed.

The 12:20:00 executable has SHA-256 `a6ebcd058d104cedb747fb4f76c16d379b70a5fb5cb3ace4e5ce241842eb9b0a`, frozen source `.artifacts/snapshots/build-q8dgcB`, snapshot `a62fb7fec6b2396d75e2d04fb01563dde311ecff8cff7da5192acce66bd2aad8`. Build typing, policy and drift checks pass. Compiled base CPU/dummy and Team Arena GL/offscreen produce decoded 320x240 TGA/JPEG captures and 128x128 levelshots with nonblack content, followed by actual movement/fire and normally terminated pure recordings. Root repeats CPU: 255.63 units moved, ammunition 100 to 89, 19 valid snapshots. Evidence: `/tmp/quake3-capture-build-0kIe12/{build,compiled-root-base-cpu}.log` and `/tmp/quake3-compiled-capture-9VfwGb/{baseq3-cpu,missionpack-gl}.log`; derivative helper `compiled-capture-input.ts` there has SHA-256 `2026cb3342edc8ef8122df75382372e6927ef7d8fe1afc3adaf0383284bb668f`. The prior executable remains recoverable as `/tmp/quake3-capture-build-0kIe12/previous-quake3-ts`. Capture functionality does not establish general image parity or physical gamma behavior.

The 11:09:17 executable has SHA-256 `fa6d424421412e999f82b6f01929cadc9d2c94961932c291a8a7a98441b56dbf`, frozen source `.artifacts/snapshots/build-CMlaDX`, snapshot `226af8964f3d3abba71172354a28a30b36ae5877c9cb2a2aa31b31895d240e43`. Build typing, policy and drift checks pass. Compiled base CPU and Team Arena offscreen GL each record 20 records and 19 valid snapshots, retain pure gamestate, move/fire after `vid_restart`, then stop recording and quit normally. Root's CPU repeat moves 263.66 units and consumes ammunition from 100 to 89. These checks do not inspect compiled drawable dimensions or texture formats. Evidence: `/tmp/quake3-port-stage-whwqg4/{build,compiled-baseq3-cpu,compiled-missionpack-gl,compiled-input-root-base-cpu}.log`; helper `compiled-demo-input.ts` there. The prior executable is preserved as `previous-quake3-ts` in that directory. Newer source-only filesystem/JPEG changes are not in this binary.

The historical 09:38:37 executable has SHA-256 `f33e13086404ef1f43b2e4f74a6d18b75d9cb8bb709ddf2b63266488936eaa4c`, frozen source `.artifacts/snapshots/build-Sz2vKQ`, snapshot `61f9fe98322499bbafd02f8cf0893b36acc6ab82edc17764efa882163cddd32a`. Build typing, policy and drift checks pass. Evidence: `/tmp/quake3-port-stage-kaUfPg/build.log`; compiled stdin driver `/tmp/quake3-port-stage-2g7QSK/compiled-demo-input.ts`; author base CPU and Team Arena GL logs there; root repeat `/tmp/quake3-port-stage-kaUfPg/compiled-input-root-base-cpu.log`. Root's 19 snapshots show 241.697 units of displacement and ammunition decreasing from 100 to 91. Compiled drawable dimensions were not measured by this driver.

Portal entity/model prepasses and state-inheriting immediate operations are integrated through 16 exact postimages. The source now preserves retained entity pose, beam/axis execution, entity depth-range transitions and pre-view ordering. Independent review found no remaining defect in this unit. Root's joined actual CPU/GL selection passes 117 tests and 11,327 assertions, including retail models and portal views. Evidence: `/tmp/quake3-port-stage-18gtql/portal-native-final.log`; integrated snapshot `6acb279780998e8f0d245f9a3e755556922398d53efbde0f1b4ccaa830300ca1`, 1,395 files. Coordinate-dependent reads of source-indeterminate retained texture attributes remain explicitly qualified.

Image preparation/catalog, CPU and GL mip/filter consumers, two-phase cvar registration, world registration, SDL color/depth/refresh selection and actual Graphics Apply are integrated through 95 exact postimages. Final image candidate: `/tmp/quake3-port-stage-zy7p63/test-6UEbWE`, manifest `79dd4bbee070537b9847932f52480a441f92917de574501f929f70129615214f`. Separate source and caller reviews are clear. Actual SDL menu input on CPU and GL changes the drawable from 320x240 to 400x300 and a world texture from 256-square RGB8 to 128-square RGB5 with trilinear mips. Movement, firing, pure admission and valid-filter retention across map flush and `vid_restart` pass afterward. Root independently repeated CPU. Evidence: `/tmp/quake3-port-stage-BT5Svk/graphics-{cpu-final,gl-final,root-cpu}.log`. The final rendering selection passes; three SDL context-loop tests exceeded its five-second timeout, then the unchanged isolated suite passed five cases with a 30-second limit. Logs: `/tmp/quake3-port-stage-zy7p63/{renderer-native-final,pixel-native-timeout}.log`. Four stale implicit-name/framebuffer-alpha test assumptions were corrected after independent review. Mesa child storage after native level-zero resizing, driver internal-format precision, absent physical gamma proof and baseline X11 CPU readback discrepancies remain explicit qualifications.

Earlier flare checkpoint: the source-empty surface handler, startup shader registration, three cvars and retained submission state passed the 60-case CPU/GL join in `/tmp/quake3-port-stage-whwqg4/flare-joined-native.log`. The later source-completion wave supersedes its dormant-algorithm omission: all six helpers in `src/render/flares.ts` are implemented, with ordered rendering connected in `src/render/world.ts`. The original disabled dynamic-light producer remains disabled.

Silent RoQ audio dispatch now bypasses the source-gated PCM decode/body validation while retaining container and video validation. Four reviewed files are integrated. Root's live decoder/playback selection passes 56 cases and 54,881 assertions. Evidence: `/tmp/quake3-port-stage-KKpeeR/live-root-cinematic.log`. The preceding audible pre-INFO sound-update ordering is preserved.

Filesystem console commands (M2) and source floating-point JPEG decoding (M4) are now integrated after independent review. Root's live filesystem selection passes 48 cases with one deliberate retail skip; JPEG passes 17 selected cases, including an exact source comparison through the mounted filesystem. Evidence: `/tmp/quake3-port-stage-ZIm4I5/live-root-console.log` and `/tmp/quake3-port-stage-lmosfj/live-root-jpeg.log`. JPEG's binary32/LP64 profile and existing extensions are recorded in COMPATIBILITY.md. Progressive JPEG remains correctly rejected: this pinned build disables it.

Actual LOD/near-plane controls are integrated through eight reviewed files. Parent and deferred portal-child views retain the same prepared cvar values. Source-clamped positive-infinite LOD scale is accepted at the reached conversion. Root's joined CPU/GL selection passes 53 cases and 757 assertions, with typing and policy clean. Evidence: `/tmp/quake3-port-stage-PcvUvY/live-{root-lod,typecheck,policy}.log`. The renderer publishes the loaded world's source basename through its existing resource owner.

The writable filesystem now implements source home/current-game `FS_FileExists`, without VFS searches, pack references or common-handle allocation. Independent review is clear and root's live 24-case selection passes. Evidence: `/tmp/quake3-port-stage-GiSiIc/live-root-exists.log`. Existing contained-Linux path and special-file qualifications remain. These source-only units postdate the 11:09 executable.

The source JPEG encoder is integrated through two independently reviewed files. Root's live six-case run includes 65 exact pinned-source stream comparisons, and independent review adds 364 exact comparisons. The MD4 skeletal model reader is also integrated through two reviewed files. Root's checksum/model selection passes 42 cases; existing checksum code and tests are unchanged. Joined typing and policy pass. Evidence: `/tmp/quake3-port-stage-ecyhSA/live-{root-encoder,joined-typecheck,joined-policy}.log` and `/tmp/quake3-port-stage-nAvWZC/live-root-reader.log`. These source-only units are not in the 11:09 executable.

TGA/JPEG screenshots, source levelshots and AVI capture are integrated through five exact reviewed files. Both initial review findings are corrected: AVI rereads its cvar after the screenshot command, and failed file opens emit the source warning. Separate TGA/lifecycle and JPEG-consumer reviews are clear. Root's corrected candidate run passes 27 cases with two fullscreen skips; the live screenshot/command run passes 23 cases and 265 assertions, followed by typing and policy. Evidence: `/tmp/quake3-port-stage-GIQE66/{root-native,live-root-screenshots,live-joined-typecheck,live-joined-policy}.log`. Actual GL uses NVIDIA 610.57.04, not inferred Mesa. Hardware gamma and post-swap buffer contents remain driver-qualified. These files are not in the 11:09 executable.

MD4's allocation/animation resource and retained scene-entity owner are integrated through four exact independently reviewed files. Root's live runs pass 52 resource/reader cases and 21 entity/lighting cases, with joined typing and policy clean. Evidence: `/tmp/quake3-port-stage-n8iAvI/live-{root-resource,joined-typecheck,joined-policy}.log` and `/tmp/quake3-port-stage-Lua2hg/live-root-entities.log`. These four files postdate the 12:20 build. Registration/bounds/tags, allocated-slot publication, the actual MD4 writer, retained stencil adjacency and root's production scene composition remain in private stages. The selected design is `/tmp/quake3-md4-design-8OphZ3/SYNTHESIS.md`; no MD4 rendering or complete milestone is accepted yet.

Usage-limit interruptions were recovered after the user's refresh at 14:19 UTC. Six workers resumed their existing stages and reviews; frozen predecessors and the live executable were preserved. Root's unfinished composition is `/tmp/quake3-port-stage-hYAkYa/test-jRnrwU`, not the live source.

The first MD4 joined snapshot is privately frozen at `/tmp/quake3-port-stage-ZUOFjJ/test-0YlYW1`, manifest `d0476d302cb4a08ff2150ef5e25bb6f694c6d222d9a64f6de28a42114736ac21` (1,415 inputs). Joined typing and policy pass. Root's affected native run passes 109 cases and fails one existing retail MD3 color comparison; the unchanged original baseline reproduces the same maximum difference of 32. Both logs are in that stage parent. Six new actual queued MD4 scene cases pass CPU and GL in a separate private successor. Retained stencil adjacency also passes independent source review and actual NVIDIA/Mesa runs. These results do not accept the join: author and reviewer independently reproduced a valid MD4 sequence whose unreferenced retained vertex makes dense batch publication reject a draw. A publication-only correction is underway; raw source tess indices and active counts must remain unchanged. The menu/map ordering and renderer-global font lifetime branches have separate private owners.

Text0 through text7 deformations are integrated through eleven exact reviewed postimages. Both world and picture drawing consume the actual retained tessellation cells and refdef text. Text survives world-to-2D transitions; repeated deformations preserve source inactive-cell reads. Valid source rows contain at most 31 bytes before NUL; undefined overreads reject explicitly. Root's joined retail CPU/GL selection passed 59 cases and 11,123 assertions, with typing and policy clean. The post-integration native selection passed 21 cases and 181 assertions. Evidence: `/tmp/quake3-port-stage-DS4xdl/{render-native,live-text-native,typecheck,policy}.log`, frozen joined manifest `cb1028add82d93d698e8f60987f8e0e07af490b638638ca1db4d803d1e132b78`. Independent review found no remaining defect in this unit. The 09:38 executable includes this unit.

Actual ClientHost recording, movement/fire, stop, disconnect, playback to normal EOF and subsequent pure map admission pass on base CPU, base GL and Team Arena CPU. Each profile recorded 38 records and replayed 37 snapshots matching its live session's time, command time, position, velocity, weapon, ammunition, health and entities. Files close after recording, EOF and final disposal. Evidence: `/tmp/quake3-port-stage-2g7QSK/{baseq3-cpu-idle,baseq3-gl-idle,missionpack-cpu-idle}.log`, external driver `client-demo-live.ts`. The initially failing immediate map-after-EOF sequence was subsequently fixed: ServerEngine consumes Main's pending sv_killserver request after map validation and before map startup. Root re-inspected that current code and the retained driver, baseline failure and successful immediate pure admission in `/tmp/quake3-port-stage-ZUJZD7`. The driver adds no idle frame or cvar reset. This deliberate source-order repair is documented in COMPATIBILITY.md; the earlier claim that it remained unfixed was stale.

Video-mode selection/fullscreen and pre-INFO cinematic audio ordering are now integrated through the reviewed 71-file join, manifest `619e9e4795fcb6bfeaba1a6f5fe8d446a0426105660936d2b3e7560bae4e675d`, 1,394 files. The cinematic caller change is 65 existing callback wrappers across 59 files, not new test infrastructure. Root independently verified every live postimage. Joined typing and policy pass; actual X11 video/stencil/Graphics tests pass 20 cases with two gated skips, and the joined retail cinematic/client run passes 86 cases with three gated skips. Review caught and corrected custom video cvar precedence. Pre-INFO stereo now invokes the real EngineSound update and fresh mixer reset before decoding; diagnostic mixer-only callers reject that unsupported operation explicitly. Evidence: `/tmp/quake3-port-stage-WoL8Rg/video-native.log`, `/tmp/quake3-port-stage-kaUfPg/{cinematic-client,typecheck,policy}.log`, `/tmp/quake3-video-review-Lfk156/REVIEW.md`. These changes postdate the 08:57 executable.

Portal entity/model prepasses are integrated. Source image preparation and settings now have CPU/GL implementations and caller joins under verification. Full Graphics Apply still awaits the image and pixel-selection joins; live guards have not been bypassed.

The 08:57:09 build uses `.artifacts/snapshots/build-8q3qJJ`, snapshot `cef119083e30397b965b2f39646f1cb33cf550f81bd26ed0b47bcb902fd3f51e`, executable SHA-256 `9e13390e4ade4b1ad6bcd988ffb463e7b0178cb90d3deb71cbcc2227e2fb14a1`. Its frozen typing and policy pass. Compiled base CPU and Team Arena X11 GL clients run 60 frames with `r_stencilbits 8`, `cg_shadows 2`, `cg_thirdPerson 1` and `sv_pure 1`, enter q3dm1 and close normally. Root's post-integration raw clients also repeat actual movement/firing and `vid_restart`, observing 638/730 nonempty volumes and 89 presentations each. Logs are under `/tmp/quake3-port-stage-sgj9kN/`: `build.log`, `compiled-base-cpu.log`, `compiled-ta-gl.log`, `live-client-base-cpu.log`, `live-client-ta-gl.log`. The prior binary is preserved as `previous-quake3-ts` there. Text deformation, video-mode selection and pre-INFO audio ordering are integrated in source; none is in this executable.

Separate actual two-client runs now cover base FFA machinegun damage, two kills, attack-button respawn and fraglimit intermission on CPU and GL. Both pure clients receive scoreboards; the remote cgame prints both obituaries. Team Arena One Flag and Harvester also reach objective delivery, correct scores and capture counts, acknowledged scoreboards and both-client intermission on both backends. These runs use frozen raw input `bba0bdc9e2046d7ba5b4cf2badb279abfc19f5f3519e068d763f26a56ce7ad5d`; no gameplay-code change was needed. Evidence: `/tmp/quake3-base-combat-AZ4KUe/{cpu2.log,gl.log}` and `/tmp/quake3-port-stage-w8VTtr/REPORT.md`. Source `devmap`/`setviewpos` assists positioning. Harvester uses the peer's `kill` command, not a combat-earned cube. The original already-opened free-slot guard can suppress cube creation and remains unchanged. These are bounded gameplay probes, not complete competitive matches.

The compiled TypeScript client passes actual native `sv_pure 1` admission, cgame initialization, authoritative movement, demo recording and normal disconnect for both products. Baseq3 recorded 76 active snapshots and 74 deltas; missionpack recorded 77 active snapshots and 76 deltas. Both demos have normal terminators. Native logs contain the actual `cp` command followed by `CS_ACTIVE` and `ClientBegin`, not just a synthetic checksum exchange. Evidence: `/tmp/quake3-stock-pure-oycGZ0/{baseq3,missionpack}-native-server/{server.log,snapshot-evidence.json}`. The pinned 1.32b native reference was independently built with recorded portability changes; it is not a byte-identical retail executable. Native game execution uses installed QVMs. Team Arena's 2000 artifact identity does not establish equivalence to the 1.32b source baseline.

The reverse baseq3 connection reaches challenge, connect and pure gamestate, but the native graphical client crashes before `cp` or active play. Its source pure mode forces the x86 JIT onto non-executable heap memory. One bounded process-local legacy-permission retry passes UI compilation, then crashes during cgame initialization. Neither run is accepted; the source signal handler's zero exit code is not success. Reciprocal missionpack was not attempted after that reference blocker. Logs are under `/tmp/quake3-stock-pure-oycGZ0/baseq3-ts-server-tty-debug/` and `baseq3-ts-server-tty-legacy/`. All runs used isolated local networking, fresh homes and read-only mounts of explicit PK3 files. No credential files or public services were accessed.

The 06:16:56 build uses frozen source `.artifacts/snapshots/build-yxbiSI`, snapshot `64b15d7981e2d8556d01fe6b3c7f9510f2e3ff809b84b14435ffa2ef7356d9c4`, executable SHA-256 `80fe2164ee57ca1b2e90df120d41b1bf659815f02443e6b5a2cc3b745159d8df`. Build typing/policy passed. Root drove the compiled remote peer through actual localhost UDP, pure validation, flag possession, Red 1–0/personal capture 1, capturelimit intermission and scoreboard acknowledgment on CPU and X11 GL. Source `devmap`/`setviewpos` shortened positioning; these are not ordinary complete matches. Abrupt peer `quit` reproduced a reported UDP refusal without crashing the surviving host. Logs: `/tmp/quake3-client-root-6tpbcY/parallel-wave-build.log`, `ctf-compiled-peer-cpu.log`, `ctf-compiled-peer-gl.log`.

M15 MOTD request/response is independently reviewed and integrated through actual EngineClient connect/frame handling. Client-static state retains source challenge/address/info lifetimes; requests use actual renderer/version fields and native random/common clock. Root's joined MOTD/admission/network run passed 44 tests and 1,135 assertions. A separate actual ClientHost/SDL/UDP probe passed 18 assertions: source request framing, wrong-address/challenge rejection and accepted publication through real frames. DNS is mapped only to a controlled localhost update peer; no public service or credential file was accessed. Logs: `motd-udp-live.log`, `motd-joined-typecheck.log`, `motd-client-host-live.log` under the same evidence directory. This postdates the binary.

M7 projected world/inline lights are independently reviewed and integrated. Source masks, transforms, float32 projection/attenuation, additive/modulative blending, depth equality, before-fog ordering, sky iterator gates, tess overflow timing and culling controls reach both backends. Review found and closed MD3 `r_nocull` propagation and portal-child light-transform reset defects. Root reran 23 source cases/147 assertions plus 12 actual CPU/GL world cases/10,779 assertions; typing/policy passed. q3dm1 emits 11 projected passes/489 indices and changes pixels on both backends. Logs: `dlight-source-live.log`, `dlight-world-live.log`, `dlight-typecheck-live.log`, `dlight-policy-live.log`. The source 32-light undefined shift uses an explicit full-mask repair. This also postdates the binary.

A separate reproducible native GL problem remains: one test ordering makes the first sky draw write ordinary depth near 0.99776 despite queried range [1,1]. Reordering one state call did not fix it; duplicating the range call helped but is not an accepted correction. Frontend clearance and the expanded green world suite do not close that issue. Frozen reproduction: `/tmp/quake3-port-stage-rfXxbN/test-hrFH4t`, `tests/world.test.ts` selection `mirror views render first|world and rotated inline projected lights|retail projected dynamic lights` with actual X11 GL.

M7 -> M8/M9 stencil work is integrated through 93 exact reviewed postimages. The ordered draw/volume/finish contract includes the 72-file ordinary caller migration, actual client startup/restart precision and source `r_finish` at all four frame consumers. Root's combined native run passed 111 cases with 11,385 assertions. Final review closed internal-marker normal writes, lost initial portal geometry and duplicate retained-marker flushing between ordinary views. The corrected frozen join is `/tmp/quake3-port-stage-sgj9kN/test-Rk39d0`, manifest `a378fb70aa180026a9b9b92308d096d57cbf4cf4122b06b2e050ee4505ae0b56`, 1,393 files. Root's final portal and marker selections passed four cases with 93 assertions; typing and policy passed. The independently reproduced duplicate changed the affected pixel from source 200 to 120; the final join produces one volume and 200.

Actual ClientHost/cgame runs for both products and backends now render hundreds of nonempty stencil volumes, present real frames, move and fire from an ordinary q3dm1 spawn, pass pure validation and restore rendering after `vid_restart`. No cheat positioning is used in these runs. The external driver and all four commands are in `/tmp/quake3-client-shadow-live-BxAG9o/HANDOFF.md`. Root independently repeated base CPU and Team Arena GL against the joined tree, observing 634 and 729 nonempty volumes respectively, with 89 successful presentations each. Counts depend on actual frame timing. This proves the live source path, not complete image parity or a match.

The separate stencil GL failure was isolated to a native stencil-clear transition using a small direct SDL/GL program with no project renderer. NVIDIA turned subsequent textured draws black; Mesa did not. The private correction revalidates only currently enabled texture units after stencil clear, preserving bindings, selectors and logical state. All 41 stencil cases pass on both drivers; a complete CPU/GL shadow image matches byte-for-byte. Evidence: `/tmp/quake3-gl-rootcause-AI6Vhf/` and `/tmp/quake3-port-stage-ykXd0b/`. This does not fix or close the first-sky depth issue.

### Earlier dependency evidence

Current work connects milestones 7, 11, 15, 16 and 17. The graphical application receives actual server packets and renders maps with CPU or GL. Both product controllers are integrated. Actual Team Arena Main -> Start Server -> Create Server input now reaches an active q3ctf1 CTF map with cgame/HUD initialization on both renderers. A subsequent console team command produces an alive Red player, followed by movement and firing. These bounded runs do not establish complete objective matches or physical SDL input acceptance.

Milestone 11 now has a real `ServerEngine` owner composing filesystem restart, server statics/world, game initialization, connections, packet processing, frame scheduling, full map changes, fast restarts and shutdown. Both-product loopback and UDP tests reach actual gamestate, client priming and decoded snapshots. These use human-only hosting with `bot_enable 0` and `sv_pure 0`; client priming is not full cgame initialization or a match. Common startup configuration ordering and its event loop have bounded acceptance. The new raw `server` entry point has independent CLI review and real external-process tests. Independent CLI/verifier review and the combined raw/compiled checkpoint are accepted for this human-only dedicated-command slice.

Renderer command execution separates scene preparation from backend execution. World scenes and pictures consume canonical finished shader passes. Source-ordered registration and shared sky state have landed. Both backends now consume explicit immutable image identities and preserve actual texture-object bindings independently from the source bind cache. One engine-lived queue serves cgame, menus and raw cinematics through a shared cinematic owner. The broad caller migration, client failure fix and diagnostic-tool corrections now pass a frozen whole-tree check, matching build and all eight retail viewer probes. This accepts that integration unit, not complete renderer parity.

All 40 active bot movement functions now have implementations, including travel handlers and `BotMoveToGoal`. Root's integrated movement rerun passed 178 tests and 7,536 assertions across seven files, with the recorded native-reference executables enabled. That does not compose game bot AI or prove complete bot matches. M13/M14 integration and whole-match gates remain open.

Common startup/configuration/console extraction is independently reviewed and root-rerun: 135 common/core/server tests, 43 platform/network tests, and 569 native parser/filter comparisons passed. The dedicated event loop and callable host now have independent acceptance for their bounded scope. Review caught an unrelated command entry that could stop an accepted frame's map, and a missing initialization print that delayed logfile activation. Both fixes have permanent failing-before/passing-after tests. Root reran seven adjacent suites: 111 tests and 878 assertions passed, including real both-product UDP lifecycles through the host frame loop. Independent tests also drove a Bun process through EOF continuation, queued SIGTERM, natural exit and port rebinding. The raw and compiled dedicated entry points now pass the combined checkpoint plus real CLI and UDP acceptance for their bounded scope. Developer-only cvar and packet traces remain recorded gaps.

The unfinished paths into milestone 18 are:

```text
2 Filesystem/commands + 10 Protocol
  -> 11 ServerEngine component [tested]
  -> common startup [focused verification passed]
  -> common loop [bounded independent review passed]
  -> dedicated CLI [raw tests and independent review passed]
  -> verifier + matching compiled checkpoint [passed]
  -> listen/client ownership + raw graphical CLI [integrated; CPU/GL runs passed]
  -> complete production-hosted matches [open]

13 Botlib foundations -> travel + BotMoveToGoal [focused verification passed]
  -> LibVars + omitted-origin time query [integrated, independent/native checks passed]
  + 12 Gameplay -> 14 Game bot AI [synchronous design selected, not integrated]

7 Materials + 8 CPU + 9 GL + 16 Audio
  -> explicit images + shared cinematics [implemented, focused tests passed]
  -> diagnostic tools + caller migration [full checkpoint passed]
  -> 15 Client + 17 Product controllers [integrated; complete flows/parity open]

All branches -> 18 Complete game [bounded base-client play; full game open] -> 19 Final acceptance
```

The earlier missing dependency on the client branch now has an implementation:

```text
M2 actual common-lived file handles + ordered bounded listing [integrated]
  + source COM_ParseExt [integrated, focused tests]
  -> M17 arena/bot catalog and SP progression [integrated, retail verified]
  -> SP difficulty/start and Add Bots [integrated and independently reviewed]
  -> Start Server / Server Options / Bot Select [integrated and independently reviewed]
M2 filesystem/parser + source interpolation
  -> actual player preview -> Controls [integrated and independently reviewed]
  -> Player Model / Player Settings [integrated and independently reviewed]
Both menu branches -> Level Select + progression -> Postgame [integrated and independently reviewed]
  -> base and Team Arena product controllers [integrated]
  -> M3/M15 SDL delivery, client/listen, screen/audio ownership [integrated]
  -> M18 base CPU/GL map/demo play and Team Arena menu-to-map [bounded runs passed; full matches open]
```

Root superseded the earlier incremental-inflater-first design constraint with the already-permitted qualified retained PK3 reader. Actual open captures the entry and size without decoding; the first nonzero read inflates/verifies the whole entry. Incremental allocation and CRC/error timing remain parity gaps. Node directory enumeration is unsorted and omits dot entries; symlinks are excluded. The earlier decoder audit remains stopped, and no refused operation was retried.

The bot branch also has an earlier prerequisite. Original `AAS_LoadFiles` always calls `AAS_LoadBSPFile` before reading AAS data, and `Export_BotLibLoadMap` then initializes level items and brush-model types. The stopped bot BSP-entity prerequisite therefore blocks canonical AAS/BotLibrary load composition. An existing inspection parser or fabricated ready flag cannot substitute for that owner. Root read the bounded caller map and verified those decisive source calls; this is a dependency finding, not a completed botlib audit or an approved new implementation. Evidence: `/tmp/quake3-bot-path-contract-SS5fPU/CONTRACT.md`.

Milestone 12 is ahead of 11 because controlled tests supply much of the game host. The new composed-server tests extend that evidence but do not close whole-match acceptance. The stock-peer probe tests protocol interoperability with pure mode disabled, not a complete new server.

## Parallel implementation wave, September 7

The nine-file filesystem successor and the actual client download, authorization and retained-demo owners are integrated as a reviewed 33-file unit. Independent review caught and confirmed a missing cgame post-await retirement guard. Root verified exact postimages; joined typing passed, 107 focused tests passed with two retail-dependent skips, then 47 explicitly retail-enabled cases passed without skips. Actual record/reopen/natural demo completion is verified. The subsequent module-selection unit below supplies actual package provenance. Contained, no-overwrite download finalization and malformed-message partial publication remain qualified.

Retained RoQ and all constructor callers are now integrated. Production playback opens unique handles through the actual current common filesystem and reads bounded chunks with source lookahead and loop timing. Final retirement drops borrowed handles without callbacks into a closed common owner. Whole-entry PK3 inflation and pre-INFO stereo sound-update behavior remain qualified. Portal/mirror child views execute before the primary view on both backends, with homogeneous clip planes, mirror culling and source sky depth. Independent reviews covered real q3tourney6 mirrors, q3dm0 and mpteam4 remote views. Source portal-sorted entity/MD3 prepasses remain explicit unsupported branches. Root's combined world/portal/render-command/cinematic run passed 54 cases with actual OpenGL and clean typing. Actual live `cinematic idlogo.RoQ` reached cinematic state 9 and exited normally. Evidence: `/tmp/quake3-client-root-6tpbcY/portal-cinematic-live.log` and `joined-retained-cinematic-live.log`.

The two-human CTF run exposed a valid source prediction transition: a new entity's next solid admits it to the collision list while its current zero solid decodes into inverted temporary box planes. The correction preserves those source planes and prepares capsule targets only for capsule models. Production prediction and moving-query/BSP validation are unchanged. Independent review passed 73 cases. The combined collision/module run passed 81 cases. Actual uninstrumented private CPU and GL runs reached flag pickup, Red 1–0/personal capture 1, the 1000 ms capturelimit delay and both players' intermission/scoreboard acknowledgement, then exited normally. Source `devmap`/`setviewpos` positioning and Blue `cl_timeNudge 30` qualify these as objective probes, not ordinary full matches.

`acquireRetailClientModule` now reads the actual pure-filtered UI/cgame artifact at module creation. Its full-byte identity selects the supported compiled TypeScript product/role before construction, retaining real filesystem package-reference effects. Unknown or mismatched artifacts reject without a retail fallback. This is an explicit replacement policy, not QVM execution or native VM-mode parity. Four identities were verified in both installations; the Team Arena 2000 artifacts are not proven equivalents of the 1.32b source baseline. Root's current CPU and X11/OpenGL two-process CTF runs with `sv_pure 1` recorded `gotCP=true` and `pureAuthentic=true` for both actual server clients, capture/intermission and clean exit. Evidence: `/tmp/quake3-client-root-6tpbcY/ctf-objective-pure-live-cpu.log` and `ctf-objective-pure-live-gl.log`. Stock-peer pure acceptance remains separate.

The peer-quit UDP correction is integrated after independent review. Actual native send errors print and return without throwing, preserving source `Sys_SendPacket`; validation, lifetime and programming errors still propagate. The baseline reproduced `ECONNREFUSED`; the corrected real socket survived 32 sends with 16 reported failures and remained usable. Receive quiet-error behavior is unchanged. Bun diagnostic wording and errno-free backpressure remain qualified. Dynamic lights and client MOTD subsequently integrated; stencil shadows remain active private work.

Root's first current-tree Team Arena mpteam1 CTF run reached game entry and its deferred input marker, then failed final resource disposal. A second actual run traced the error to `EngineSound.close -> BackgroundMusic.stop -> MusicStream.close` attempting to read the common filesystem after normal source shutdown had closed it. The correction is now integrated: callback-free final retirement drops the music borrow while actual handle cleanup remains common-owned. Source stop and shutdown behavior is unchanged. Root's 19 sound cases passed, and repeated CPU 130-frame and GL 110-frame CTF map/input runs both exited 0 through normal shutdown. Failed and corrected logs remain under `/tmp/quake3-client-root-6tpbcY/`: `parallel-disposal-trace.log`, `music-retirement-live.log`, `parallel-live-cpu-ctf-retirement-fixed.log`, and `parallel-live-gl-ctf-retirement-fixed.log`. These command runs do not inspect objective scores or prove a complete match.

The M2 retained-unique-read prerequisite is integrated. Music now opens and reads through the actual common file table, retains its handle across filesystem restart, and opens loops at the source transition rather than preloading them. Root's live sound/UI/client-level selection passed 63 tests and 988 assertions. The source one-shot path leaves its unreachable handle in the common table until filesystem disposal; whole-entry PK3 inflation also remains qualified. Retained demo and RoQ consumers follow this same M2 dependency, not substitute readers.

Projected model shadows use the actual source shader, lighting, orientation and geometry owners on both renderers. Independent source and actual CPU/GL review passed; root's live selection passed 58 tests and 374 assertions. Patch subdivision, stitching, recursive shared-error propagation and view-distance LOD are integrated after separate algorithm and renderer reviews. All 57 supplied maps load and select near/mid/far patch geometry. An actual rotated Team Arena inline model selected 228 near indices and 96 far indices on CPU and GL while preserving full geometry for marks and world allocation. One original reverse-edge stitching lookup reads beyond its allocated error array: this port intentionally uses that matched segment's midpoint error only for the undefined lookup. Defined in-bounds source lookups are unchanged; this is a documented compatibility repair, not exact native heap behavior.

SDL input uses adjacent text records for actual Shift/layout symbols and retains the pressed scancode binding for release. Real X11/SDL probes passed US and French layout cases; root's live selection passed 22 tests and 136 assertions. Missing adjacent text, IME/dead-key and some Control/layout combinations remain qualified. The CPU bilinear sampler changed four signed word loads without changing output bits. Three paired existing benchmark runs measured an 8.4–12.8% median improvement in that workload; seven captured scene outputs matched byte-for-byte. This is not a whole-game performance claim.

The 04:32 executable passed separate CPU and GL Team Arena CTF map/command/shutdown runs, including correctly quoted deferred input commands. Those runs did not inspect authoritative movement/ammunition state. The executable predates this parallel wave. Evidence is under `/tmp/quake3-client-root-6tpbcY/` (`projected-shadows-live.log`, `patch-lod-live.log`, `music-streaming-live.log`, `sdl-layout-live.log`, `parallel-first-integration.log`, `team-compiled-cpu-ctf-quoted.log`), `/tmp/quake3-team-compiled-gl-8Tp1WX/compiled-ctf-quoted-independent.log`, and the frozen component stages. No new whole-tree checkpoint or full-port gate is claimed.

## Graphical application checkpoint, September 7

The raw `client` command is integrated. Root ran the live checkout against read-only q3a assets with fresh external writable homes:

- CPU q3dm1, recording, playback and q3dm7: exit 0. A separate timedemo reached natural completion, reporting 21 frames in 2.011 seconds, then returned to the menu.
- OpenGL q3dm1, `vid_restart` and q3dm2: exit 0 using an owned Xvfb wrapper.
- Base menu startup with the local key-prompt-check cvar set for the test: exit 0. No real key was read or imported.
- Actual client `serverstatus`: printed q3dm1 and its connected player. The existing browser suite also passed real loopback and UDP responses.
- Joined session/message/UI-state/browser/common checks: 156 passed, 0 failed. Strict typing and policy passed for the client integration; typing also passed after the status-command wiring.

Logs and runtime homes are under `/tmp/quake3-client-root-6tpbcY/`. `cpu-client.log`, `gl-client.log`, `timedemo.log`, `menu.log`, `client-serverstatus.log` and `joined-client.log` distinguish the actual runs. The first run correctly rejected changing cheat-protected `timedemo` mid-match; the separate demo-only startup enabled it before playback. These runs do not prove a complete human match, audible device timing or full renderer parity.

Live runs exposed and fixed source shutdown ordering, a stale `sv_killserver` request before map startup, same-window GL renderer replacement, pending loopback packets during demo playback, and source cinematic/menu lifetime ordering. Independent lifecycle review found loading cancellation could access a retired connection; its explicit cancellation path is code-reviewed and covered by an actual encoded-packet case, but full SDL Escape-during-loading reproduction remains open. Managed cancellation skips the retired packet's remainder rather than reproducing C-global reuse.

The client build from snapshot `8cf86bb9c917e4647a7454b3e57fa97c9fb627f5126747ef1dd84f80ee24e130` produced executable SHA-256 `3b01eea4a47a8c6d2d0504278b515cf7c89c1d659f917b328b2f0f2fd3c20a63`. Compiled CPU playback completed 21 frames in 2.066 seconds; compiled GL entered q3dm1. Both exited 0. This build passed typing/policy and these application checks, not a new full-tree checkpoint. The older full-check binary remains in `.artifacts/client-audio-executable-8dv4Xc/`.

Subsequent raw cgame/sound integration replaced mandatory mixer/music arguments with actual EngineSound services. Root entered q3dm1 and exited 0 both with `s_initsound 0` and with a deliberately unavailable SDL audio driver; the six existing audio/cgame suites passed 20 cases. Logs are `no-sound.log`, `no-device.log` and `cgame-sound-live.log` in the same root-run directory. Music remains device-independent. Whole-file music decoding/loop preload and missing/malformed-track error behavior remain qualified. One-argument `music` playing once matches the source, which explicitly clears its loop name.

An additional actual ClientHost input probe moved the authoritative player. Its initial firing expectation was wrong: holding attack from the first active frame retained source `PMF_RESPAWNED`. A neutral-command interval cleared that flag; subsequent attack delivered 14 machinegun shots and reduced ammo from 100 to 86. No production correction was needed. This is movement/firing evidence, not a multi-player combat match.

Process cleanup was limited to proven ownership. Two root-launched callback-check processes were terminated after a failed input setup and a test-promise deadlock: PIDs 1446385 and 1447825. Exact commands, parent processes, checkout paths and dedicated output-log descriptors matched the launches. Their corrected replacement passed. Other recorded client/test processes exited naturally. Three older Xvfb servers had no conclusive launch provenance and were left untouched; no unrelated processes or files were removed.

Team Arena startup prerequisites subsequently integrated: shared UI string/allocation quotas, game/team metadata, arena/bot catalogs, `COM_Compress`, deferred map-preview handles and `UI_BuildQ3Model_List`. Root read the installed missionpack in place: catalog checks loaded 60 arenas and 45 bot definitions; model checks registered actual Sarge portrait shaders. The three parser/metadata/catalog suites passed 33 cases, and four model-list cases passed separately. Independent source review found no remaining defects in those bounded units. Joined typing/policy passed after correcting one new test's checked-index typing. Logs: `team-arena-catalog-live.log`, `team-arena-models-live.log`, `team-ui-types.log`, `team-ui-policy.log`. These changes postdate the current executable.

The next startup units are also integrated and independently reviewed:

- Cached-server persistence uses actual home-root server handles and retained 152-byte records. The joined browser/writable/server-file run passed 64 tests and 821 assertions, including retained bytes, partial reads, failed writes and source address comparisons.
- Saved scores preserve the 64-byte record, source cvar publication order and actual file reads/writes. Selection updates original map/character rows. The joined score/metadata/model/catalog run passed 24 tests and 230 assertions.
- Team Arena resources register actual retail pictures, fonts, sounds and cinematics. Font no-write outcomes preserve prior UI/cgame destinations; the root five-suite retail run passed 81 tests and 533 assertions, with one GL-only skip.
- Mod/movie/demo lists use the actual bounded common listing API; six tests and 42 assertions passed, including retail content. The player list reads actual snapshot identity/configstrings; two tests and 16 assertions passed.

Logs are `server-cache-live.log`, `team-ui-joined.log`, `team-ui-resources-live.log`, `team-ui-font-no-write-retail.log`, `team-ui-lists-live.log` and `team-ui-player-list-live.log` under the root-run directory above.

By 02:12 UTC, shared parser/runtime memory and postgame calculation/active-menu transitions are integrated after independent review. Root's joined seven-suite run passed 94 tests, 804 assertions, strict typing and policy (`team-ui-postgame-joined-live.log`, `team-ui-postgame-types.log`, `team-ui-postgame-policy.log`). Source NULL `open` clears focus and closes cinematics; protected/latched UI cvars now receive actual forced writes. The same real UI pool backs retail parsing and runtime script reads.

By 03:00 UTC, retained menu loading/input, browser filtering/sorting/refresh, status/player search, connection/download drawing and actual engine-slot cinematic traps are integrated after component review. Visibility and graphics/name/rate/pitch/match-limit callbacks also passed source review. Root's eight-suite run passed 97 tests and 980 assertions, including the existing mission-pack HUD, retail RoQ/font/sound assets and actual loopback/UDP status replies (`team-ui-async-joined-after.log`). Feeder selection/painting and owner-key callbacks now await their actual asynchronous work, with all current callers migrated; the independent combined callback review is clear. Strict typing and policy passed. Source menu reload also follows the actual string reset, game/arena parsing, menu loading, close and focus-restoration sequence; its reached uninitialized-name case was corrected after review, with empty and unnamed-only sets completing normally. No new full-tree check or build covers this batch. Full product-menu orchestration, owner drawing/key dispatch, feeders and scripts remain incomplete.

By 03:36 UTC, actual feeder data/selection and Team Arena player previews are integrated after independent source review. Source reload, refresh/FPS/cursor ordering, console commands and menu movie/sound caching are also reviewed and integrated. The final independent five-suite run on the combined tree passed 64 tests and 793 assertions, with no failures or skips; strict typing and policy passed. Actual CPU menus/models, retail RoQ/sound assets, shader remapping and UDP status owners are exercised. Owner drawing/key dispatch, menu scripts and full product initialization/EngineClient composition remain the active successors. The executable still predates this source wave.

A real CPU ClientHost run exposed a fractional `timelimit` bug: game composition read the float instead of the source VM integer. The corrected game uses the integer, with a focused failing-before/passing-after boundary case. The live client then reached timed intermission on q3dm1, sent ready input and entered q3dm2. Its final level time was 68,950 ms, new start time 66,550 ms, with no queued/active intermission (`timed-match-fresh-level-verified.log`, exit 0). A repeated client print initially looked like another premature exit; the actual new-map state disproved that suspicion, so no second production fix was made. The same OpenGL flow passed with final level time 69,050 ms, start time 66,650 ms and no queued/active intermission (`timed-match-gl-verified.log`, exit 0); its owned display wrapper exited normally. These human-only, accelerated, sound-disabled runs establish this bounded match flow on both renderers, not all-mode or both-product match acceptance.

By 03:58 UTC, owner-key dispatch is independently reviewed and integrated; root's actual key/feeder run passed 12 tests and 245 assertions. Drawing callbacks are in final review, scripts are implemented in a private stage, and full UI initialization/client composition is underway. These successors are not yet accepted as a working Team Arena menu controller.

A separate live CPU client moved, fired its machinegun, received a delivered `kill` command and respawned from attack input (`fire-respawn-spawn-qualified.log`, exit 0). Ammo fell from 100 to 95; the actual server spawn count advanced from 1 to 2 and health returned to 124. An earlier bounded attempt did not reach its ammo predicate; its cause is unconfirmed, and no production fix is claimed for it. This was accelerated, human-only, sound-disabled base-game play, not general combat acceptance. All logs and generated homes remain outside the project.

By 04:18 UTC, owner drawing, menu scripts, the full Team Arena UI initialization sequence and EngineClient product/HUD wiring are independently reviewed and integrated. Root's drawing/script/key run passed 19 tests and 373 assertions. Shared music ordering and callback-free UI/HUD retirement passed an independent 67-test retail CPU run with 1,066 assertions. Root's real CPU application loaded both menu sets and closed normally; an independent application run also completed `vid_restart`, reloaded the menus and saved the actual 661,520-byte server cache. The combined code passes strict typing and policy. The current executable predates this work.

The longer menu-to-match run exposed a renderer bug during a retail menu fade: `SetColor` rejected a small negative alpha that the original byte-color conversion accepts. The integrated correction retains source float32 colors at the frontend and converts to bytes when the command executes. It does not clamp or change the fade. Out-of-unit values use the documented bounded truncating int32-to-byte profile, not a universal native C conversion claim. Actual CPU/GL drawing checks passed 20 tests with 1,362 assertions and no skips.

By 04:32 UTC, root CPU and independent OpenGL runs both passed retail Main -> Start Server -> Create Server -> active q3ctf1 CTF -> UI report -> normal shutdown. Each reported game type 4, spawn count 1 and client frame 75. Input reached actual EngineClient handlers through awaited command execution. The temporary helper initially registered async handlers with the synchronous API; correcting it to `registerAsync` resolved the closed-context error without a production change. These sound-disabled, human-only runs do not prove alive team participation, physical SDL input, objectives or a complete match. Evidence: `/tmp/quake3-client-root-6tpbcY/team-menu-to-match-cpu-input-awaited.log`, `/tmp/quake3-port-stage-gTHsOK/team-menu-match-gl-awaited.log`, `/tmp/quake3-port-stage-gTHsOK/gl-drawing-tests.log`. The executable still predates this work.

The follow-on source runs passed actual Red team joining, neutral input clearing `PMF_RESPAWNED`, firing and movement while the missionpack HUD rendered. GL machinegun ammo changed 100 -> 92 -> 85 and the player moved about 236 units. CPU stationary fire changed ammo 100 -> 75, then movement covered about 703 units and picked up a shotgun. Different frame durations and spawn selections make these distinct behavior checks, not a deterministic parity comparison. Both remained alive in `PM_NORMAL` and exited 0. No production correction was needed. Evidence: `/tmp/quake3-client-root-6tpbcY/team-alive-fire-cpu.log`, `/tmp/quake3-port-stage-gTHsOK/team-alive-fire-gl.log`.

Bun rebuilt the source at `2026-09-07T04:32:01.840Z`, snapshot `84b7798224009481dd22ef63ac1b7b4e03eed3ad59684db17be016e0bab9df0b`, executable SHA-256 `7b142f64675e762be129a4e8ec26ab7039a9788a3b14b0cdd3299e9d818bb50b`. Strict typing and policy passed. The compiled CPU application loaded missionpack `mpteam1` with CTF enabled, accepted the team and held-input command sequence, printed its completion marker and exited 0. An earlier command-line sequence lost literal quoting and ran inputs prematurely; those earlier runs establish map loading only. The corrected invocation preserves literal double quotes around the `port_probe` cvar passed to `vstr`. These compiled logs do not inspect authoritative movement/ammo as the source runs do. Evidence: `dist/build.json`, `/tmp/quake3-client-root-6tpbcY/team-ui-build.log`, `/tmp/quake3-client-root-6tpbcY/team-compiled-cpu-ctf-quoted.log`.

## Verification boundaries

### Earlier checked input and integration history

Latest implementation: Main, Setup/In-game, ArenaServers, Graphics/Driver Info/Display, client userinfo updates, source packet writing and CL_SendCmd for actual sessions are live. The command-send check exercises retained movement across a throttled frame, actual packet decoding, local pause and demo/cinematic suppression. Main renders the retail banner and handles the local key prompt and error recovery. ArenaServers consumes real browser replies and shares the existing child menus. Source commands generated by these menus still require the normal client application to execute them.

Base UI controller, engine screen composition, sound startup, cgame drawing/presentation separation and actual demo recording are integrated. One SDL screen now draws the retail Main/Setup menus, connecting and loading overlays, and console through the shared actual owners. The seven-suite joined run passed 41 tests; later engine-lived screen/common changes passed 83 focused tests and strict typing. Cinematics now resolve the actual live sound output, allowing renderer initialization before sound and avoiding stale audio after restart. Client cvar registration and independent console commands are also integrated. Logs are under `/tmp/quake3-port-stage-eht4Y1/` and `/tmp/quake3-port-stage-2nvTzK/`.

That graphical-client edge subsequently landed and passed the bounded application runs described above. Stereo, complete renderer begin/end behavior, Team Arena controller and existing memory/network/parity qualifications remain open. Tests remain scoped to changed behavior; no new infrastructure or repeated whole-tree checkpoint cycle is underway.

The later full check/build attempt used input `37f225ab0ef84c6e88e666d5b6876c8ec29bd5c5cff28e71e70fcf84dc977251`, 1,298 files. Both were terminated by SIGTERM and exited 143 before completion. They are not accepted results. Partial logs are `/tmp/quake3-port-stage-4dTTdC/graph-wave-check.log` and `graph-wave-build.log`. The previous executable and build metadata remain backed up in `.artifacts/client-audio-executable-8dv4Xc/`; that attempt did not replace `dist/quake3-ts`.

The latest full check began at `2026-09-06T21:40:02.381Z`, input `6d3ea9c69f98fd2fbf958e32b019bd81916dbdf73cc58acc81cef953f2edac27`, 1,282 files. Main: 4,510 passed, 84 skipped, zero failed, 525,273 assertions across 304 files. Selected actual GL: 473 passed, two skipped, zero failed, 9,493 assertions across 41 files. Strict typing, policy, all command exits and frozen/live input agreement passed. The GL selection repeats coverage. Evidence: `.artifacts/checks/2026-09-06T21-40-02.381Z.json` and `/tmp/quake3-port-stage-oBHtwZ/live-wave-full-check.log`.

The matching build at `2026-09-06T21:50:53.411Z` published `dist/quake3-ts`, SHA-256 `ec31820080b048e2ab5b3dd731696d1f55017daef2b97148852cf06e4eb96928`. Frozen raw source and this executable passed eight retail viewer profiles and eight dedicated UDP profiles. Both products, CPU/GL, raw/compiled and quit/SIGTERM are covered where applicable. Raw/compiled images match for every product/backend pair. UDP runs reach admission, fast/full restart, final disconnect and port reuse. Logs and captures: `/tmp/quake3-port-stage-oBHtwZ/wave-*`. These are map-viewer and dedicated-host runs, not normal product-menu or gameplay runs. The previous menu executable is preserved in `.artifacts/menu-executable-6qV4bh/`.

This checked input includes the complete integrated menu chain through Postgame, inert UI cvar allocation and explicit registration, the actual SoundOutput buffer owner, shared server-relative reads/mod discovery/Mods and SDL input. The separate root X11 run passed 18 tests and 97 assertions, including native keys, focus transfer, capture and held-key release. Shifted punctuation/layout binding parity and physical joystick validation remain unfinished. The input producer is not yet wired into a normal graphical client.

The later 64-file client/audio integration is live after independent component reviews. It contains:

- Post-resolution client admission using real Unix I/O and loopback, source challenge/resend behavior, address checks and shared reliable ownership. Actual loopback and localhost UDP tests reach the existing session. Whole connect/disconnect orchestration, public authorization, MOTD and graphical cgame lifecycle remain absent.
- Connect Screen using actual client records, source download text and CPU drawing. Review found that value-only configstrings lost the difference between missing and explicitly empty server info. Root reproduced the incorrect frame, fixed presence at the session owner, and verified both products. Unchanged configstring commands retain presence; rebuilding changed configstrings removes empty entries as source does.
- Deferred sound-channel starts, source allocation clocks, LIFO channel allocation and repaintable PCM ranges. All 110 mixer constructions and 15 SoundOutput start calls migrated. Review corrected retained raw-ring gap bytes and free-slot detachment before the second clock sample. The broken native duplicate scan uses the explicitly qualified bounded correction; its out-of-bounds listener fallback and overwritten raw history still reject.

Root's combined live run passed 1,083 tests, 41 skips, zero failures and 155,382 assertions across 82 files, followed by strict typing and policy. Evidence: `/tmp/quake3-port-stage-WdhvK1/live-client-audio64.log`, `live-client-audio64-types.log` and `live-client-audio64-policy.log`. This integration is outside the 21:40 check/build. The updated source ledger maps 178 files, all still in-progress; this is not a completion percentage.

The next outgoing-packet prerequisite is also integrated after independent review. EngineClientSession now sends through the actual ChannelDelivery API instead of collecting datagrams before transport. It publishes the send clock before sending, preserves send/trace/fragment-progress order and stops on transport failure or operation retirement. Both direct callers migrated. Root's private five-suite run passed 178 tests and 4,497 assertions; independent review passed 113 tests and 1,959 assertions including real UDP/loopback admission. Evidence: `/tmp/quake3-port-stage-2BIP8r/delivery-final.log`, `delivery-before.log` and `delivery-live.log`. Source packet-rate gating is now integrated too: download/nonactive thresholds, LAN/loopback ordering, actual cl_maxpackets clamp and previous packet clock. Complete CL_SendCmd orchestration, send diagnostics and demo recording remain separate work.

The following reviewed dependencies were integrated by 22:46 UTC, after the last full check/build:

- Engine server-browser lists, commands and ping ownership through real Unix I/O. Root's live five-suite run passed 63 tests and 1,558 assertions, including actual ServerEngine getinfo replies through admission into browser getters. Evidence: `/tmp/quake3-port-stage-TuQku4/browser-live.log`. ArenaServers is now in implementation; cache persistence and status queries are not supplied by this owner.
- Actual CPU/GL configuration and exclusive, focused single-display gamma acquisition. Root's isolated Xvfb CPU/GL/input union passed 63 tests, one expected skip and 349 assertions. Evidence: `/tmp/quake3-port-stage-4Ripqp/configuration-live.log`. Installed SDL actually reports gamma setting unsupported; positive physical gamma and move/replug continuity are not verified. Only scalar gamma consumption is ported here, not all color-mapping tables or renderer initialization.
- Common-owned local CD-key bytes, validation and the base menu. Root's six-suite live run passed 83 tests and 1,046 assertions, followed by strict typing and policy. Evidence: `/tmp/quake3-port-stage-zW0aNJ/cd-key-live.log`. No real credential files were accessed or copied. File persistence and public authorization are not implemented by this unit.
- Sound-frame scheduling and actual SDL queue replacement, separating delivered sound time from repaint time. Independent review passed 54 focused tests, 17 actual SDL cases and seven existing Doppler tests. Defined negative paint ranges are preserved; invalid native reads are rejected at access. SDL's queue-size API cannot identify already hardware-buffered playback or silent underrun progress. Those qualifications and native DMA-wrap behavior remain open. Root's combined live run passed 160 tests and 1,680 assertions across 13 audio/filesystem/client/browser/key suites, followed by strict typing and policy. Evidence: `/tmp/quake3-port-stage-4dTTdC/combined-live.log` and adjacent types/policy logs.

The source ledger now maps 179 files, all still in-progress. Active graph edges are browser -> ArenaServers and renderer configuration -> Graphics/Driver Info/Display, followed by actual product controllers, screen and graphical-client ownership. GPT-6 Astra high owns both bounded implementation assignments. No new verification framework was added. The full-port goal remains active.

### Filesystem batch and parser, earlier integration

The 26-file M2 batch is integrated and independently reviewed. It supplies the single common-owned 63-handle table, real loose/PK3 cursor reads, bounded ordered listing, shared writable ownership, remount/final-disposal behavior and source-loader/inspection caller migration. Independent combined review found post-close bot-log acquisition; the fix and its causal test passed review confirmation. Root also corrected empty-entry CRC validation and restored the bot script allocation cap.

The existing full check began at 2026-09-06T19:14:07.511Z on input 7c14d2f3c2e07c5b84c64b0217d2b89cb29bec3d5b2b79f4d1fdd3ce938ccf44, 1,242 inputs. Main: 4,264 passed, 83 skipped, zero failed, 522,092 assertions in 285 files. Selected actual GL: 473 passed, two skipped, zero failed, 9,493 assertions in 41 files. Strict typing, policy, command exits, snapshot integrity and candidate input agreement all passed. Evidence: /tmp/quake3-port-stage-sqqkQe/full-check.log and test-l7WWes/.artifacts/checks/2026-09-06T19-14-07.511Z.json beneath that same directory.

The checked candidate's raw and compiled runs passed eight dedicated UDP profiles and eight retail viewer runs, covering both products, CPU/GL, quit and signal shutdown. The four image hashes match the previous accepted captures. UDP snapshot counts can vary with scheduling; all runs reached admission, fast/full restart, final disconnect and port reuse. The compiled candidate hash is c18365ca799e16b192737a2832d3a343750ff3197c16ac6be301d02b981b0d88.

After exact source integration, the then-current live build published dist/quake3-ts from the same 7c14d2f3 input. Its hash is 27d1eedf29034b0a8333c167073cc793f44a63201c9933dcb0404adc1788a076; embedded build paths account for a separate artifact identity. That build is now preserved in .artifacts/filesystem-executable-mKgw6B/. Checkpoint twenty-two remains recoverable under .artifacts/checkpoint22-executable-sogULV/. Later parser and documentation changes are outside this older build.

The source parser and game-info consumer are integrated. Independent review found a short-read case that rejected a complete quoted token before any missing byte was read. Root moved the check to actual byte access; review confirmation and root each passed 25 tests and 252 assertions. The live union with UI foundation and the preserved interpolation pair passed 89 tests and 676 assertions, with strict typing and policy. Evidence: /tmp/quake3-port-stage-rHZdk0/root-final-tests.log and live-ui-combined.log. Retail initialization loads 24 ordinary SP arenas, training/final and 32 bots through the actual common filesystem. The source parser and RNG are owned once by BaseUiState.

The next integrated consumers are real player previews, Difficulty/start, Add Bots, Controls, Remove Bots and Demos. Independent review corrected Difficulty's null-arena recovery and Controls' complete pre-cache reset. Root's first eight-suite union passed 149 tests and 1,914 assertions. The later Controls/preview/Remove Bots/Demos union passed 33 tests and 365 assertions, followed by whole-project typecheck and policy. Evidence: /tmp/quake3-port-stage-JYF4qv/live-menu-combined.log and ui-next-combined.log. These overlapping counts are not additive. Actual CPU previews render models, skins, weapons and chat; menu tests drive real keys, cvars, audio, common files and protocol configstrings. Original undefined muzzle-light storage and uninitialized/overflowing source-buffer domains remain explicit qualifications.

Player Model/Settings and Start Server/Server Options/Bot Select are independently reviewed and integrated. Reviewers confirmed the corrected fractional precache/out-of-range color conversions and source bot-icon overflow diagnostics. Root's live player/menu union passed 84 tests and 660 assertions, with whole-project typecheck and policy. Evidence: /tmp/quake3-port-stage-mOQG0h/live-player-union.log. The 20:33 full check/build now covers these additions. Their downstream Level Select and Postgame integrations are described above; no product controller or normal playable client is claimed.

### Historical integration after checkpoint twenty-two

Team Orders and the source-disabled Save Config leaf are integrated as four new source/test files. At that historical checkpoint boundary, the next full check/build had not run. The filesystem batch above now includes both leaves. At `18:32:19.055Z`, live input exactly matched independently reviewed candidate `7589c2fac9830443ec82e9397e0ea5390ac1df454caae7b43bd1ad6fb9abac03`, 1,235 inputs. Root's final sixteen-suite union passed **317 tests, zero failures or skips, 6,787 assertions**. Later ledger/status changes are separate. Evidence: `.artifacts/saveconfig-post-live-tests-root.json` and `.artifacts/saveconfig-integrated-live-root.log`.

Team Orders passed independent source review and root's fresh reproduction: nine independent tests with 352 assertions, then 244 adjacent tests with 5,894 assertions, no failures or skips. Root's current-dependency and live twelve-suite runs each passed 258 tests with 6,230 assertions. The exact two additions and all 1,231 borrowed entries matched after integration. Root inspected both actual 640-by-480 CPU captures. This preserves the source roster/index behavior and pending command bytes; it does not accept bot service integration or chat delivery. Evidence: `/tmp/quake3-port-stage-rAzphm/REVIEW.md`, `/tmp/quake3-teamorders-review-replay-cpXvhd/`, `.artifacts/teamorders-post-live-tests-root.json` and `.artifacts/teamorders-candidate-captures-root.jsonl`.

Save Config's independent current-combined review and root reproduction each passed seven independent tests with 462 assertions and 303 adjacent tests with 6,451 assertions, no failures or skips. A separate fresh patch reconstruction/application exactly reproduced its original candidate. Root's current candidate passed 296 tests with 6,663 assertions; that fifteen-suite selection differs from the reviewer's and includes the depth regression. The final live union above contains both selections. Root read every changed source/test file, report and helper, checked complete input inventories, and inspected the actual filename/Back CPU captures. Repeated suite counts are not additional unique coverage. Evidence: `/tmp/quake3-port-stage-A27vh2/REVIEW.md`, `/tmp/quake3-saveconfig-review-root-UwNxCZ/`, `/tmp/quake3-port-stage-ql10zB/`, `/tmp/quake3-port-stage-VvXkpx/` and `.artifacts/saveconfig-candidate-captures-root.jsonl`.

Save Config remains source-disabled in Setup and UI cache registration. Tests invoke the compiled leaf explicitly, type or paste into the actual field, and inspect pending `writeconfig` bytes without executing them. No player-reachable route or configuration persistence is claimed. The accepted checkpoint-twenty-two binary and build metadata were preserved under `.artifacts/checkpoint22-executable-sogULV/` before the next build. All previous frozen packages remain unchanged.

Checkpoint twenty-three's external helper review found a startup-evidence failure that could abandon an unidentified running test and a verifier that counted successful commands without validating their identities. Root corrected both and retained inherited stdin. Independent isolated verification and root reproduction each passed fourteen subprocess cases and ten corrected-verifier cases, and reproduced the original verifier's false acceptance. Root observed the original runner return 1,016 ms before its dummy probe completed; the correction returned four ms after completion and recorded the owned PID. All probes exited naturally, with no timeout or cleanup signal. Original/current/helper strict and policy checks passed. This is temporary-fixture proof, not a real checkpoint or GL run. Evidence: `/tmp/quake3-check23-helper-proof-Y4hVwl/REPORT.md`, `/tmp/quake3-check23-helper-root-replay-8jmYqa/` and `/tmp/quake3-check23-helper-run-T7EErv/`. That proposed helper sequence is superseded. The functional filesystem batch used the existing check/build/runtime commands directly; this temporary helper proof is historical. No full milestone, normal playable client or complete match is accepted.

### Checkpoint twenty-two

This earlier accepted combined checkpoint started at `2026-09-06T17:36:10.949Z`, input SHA-256 `301a522a2d4f1e8f9ff1acbcd490995adf34c9d542a7b867485574a7e87eb12e`, across 1,231 inputs. Main: **4,166 passed, 83 skipped, zero failed**, 520,816 assertions across 279 files in 350.03 seconds. Selected actual GL: **473 passed, two skipped, zero failed**, 9,493 assertions across 41 files in 94.29 seconds. The GL run repeats selected suites, not additional unique coverage. Strict typing, policy, frozen-snapshot integrity and live matching all passed. See the [twenty-two report](../.artifacts/checks/2026-09-06T17-36-10.949Z.json) and `.artifacts/checkpoint22-check-summary-root.json`.

The matching build at `17:45:06.911Z` used that exact input; executable SHA-256 is `9c308f4af13f63cce5c11223b1330c4fdb00add03146fa527a9dd88a06dce919`. All eight dedicated UDP profiles passed: both products, raw and compiled, quit and SIGTERM. Every case reached two gamestates, fourteen valid snapshots, distinct final message numbers 15 and 16, clean exit and port reuse. All eight retail CPU/GL viewer runs passed with the four previously inspected hashes unchanged. The compiled CLI suite passed eight tests and seventy assertions. At `17:46:37.242Z`, live source and binary still matched. Evidence: `.artifacts/checkpoint22-build-summary-root.json`, `.artifacts/checkpoint22-runtime-summary-root.json`, `.artifacts/checkpoint22-compiled-cli-root.log` and `.artifacts/checkpoint22-post-probes-snapshot-root.json`.

This checkpoint includes Specify Server, Cinematics, their corrected joined test, the client snapshot-number getter and the constant-depth CPU correction. Root verified that only five documentation/ledger inputs changed after the reviewed 61f02d4a integration; all 1,226 other inputs stayed unchanged, before and after runtime probes. Evidence: `.artifacts/checkpoint22-final-input-delta-root.json` and `.artifacts/checkpoint22-post-probes-input-delta-root.json`. The previous accepted binary and build metadata were preserved under `.artifacts/checkpoint21-executable-5gEle4/` before publication. Later status edits are outside this frozen checkpoint. Team Orders and Save Config were integrated afterward and are excluded from this checkpoint. No full milestone, normal playable client or complete match is accepted.

### Integrated after checkpoint twenty-one

The following integration units landed after twenty-one and are now covered by checkpoint twenty-two. Root reproduced Specify Server's independent review in a fresh external run: seven independent tests, 84 assertions, and 231 adjacent passes with four required oracle skips and 4,135 assertions. Current-dependency checks passed before its exact five-file integration. The live four-suite run passed 49 tests and 483 assertions. Evidence: `/tmp/quake3-specify-review-root-1AbAfy/run-KfGwOs/`, `.artifacts/specify-post-live-tests-root.json`.

Cinematics' independent component review and root reproduction passed five independent tests with 58 assertions and 124 adjacent tests with 1,712 assertions, with no skips. The final combined review found no production defect, but did find a weak root rendering assertion. Root preserved the original package, corrected the test in a fresh candidate, and required an overwritten framebuffer plus positive queue consumption before image equality. Independent combined review and root reproduction each passed six independent tests with 106 assertions, then 257 tests across nineteen suites with four expected oracle-only skips and 4,912 assertions. All helper/project strict and policy checks passed. These repeated suite counts are not additional unique coverage. Evidence: `/tmp/quake3-cinematics-review-bjRbad/REVIEW.md`, `/tmp/quake3-port-stage-qYzBVY/REVIEW.md`, `/tmp/quake3-port-stage-FbJIVZ/` and `.artifacts/cinematics-combined-independent-root.log`.

Root integrated the remaining two Cinematics additions and corrected joined test. The post-integration live run passed 55 tests with 1,060 assertions and no skips. At `16:49:08.760Z`, the live snapshot exactly matched corrected candidate `f3759adefe564a2ad1b097c6aa301c7a3adb77ca5c49a20edba289f0d8a3bf73`, 1,229 inputs, with all 1,226 borrowed current-baseline entries unchanged. Across both integrations there are eight changed source/test paths relative to twenty-one. Evidence: `.artifacts/cinematics-post-live-tests-root.json`, `.artifacts/cinematics-integrated-live-root.log`.

The actual 640-by-480 CPU captures then exposed visible corruption in Specify Server's address highlight. Root viewed both images and held checkpoint twenty-two. Independent Astra High diagnosis and root's fresh reproduction confirmed that identical triangle depths can interpolate to tiny positive values and fail against an earlier zero-depth write. In the blank highlight region, 1,428 of 4,403 pixels were incorrectly rejected. Two independent test-only state interventions removed those holes, but neither is an approved production fix. The actual rasterizer source matches twenty-one; the new menu exposed existing code behavior. No historical whole-input reproduction or earliest introducing change is claimed. Evidence: `/tmp/quake3-address-diagnostic-YI2UR6/DIAGNOSIS.md`, `/tmp/quake3-address-diagnostic-root-Du4Ril/`, `.artifacts/checkpoint22-menu-specify-server-cpu.png` and `.artifacts/checkpoint22-menu-captures-root.jsonl`.

Root approved the two-line constant-plane correction only after reading the actual failing regression. The final identical fourteen-test suite produced five passes and nine failures on original code, then fourteen passes on the correction. Independent renderer review and root's fresh replay each reproduced two independent original-code failures, six passing corrected probes, fifteen byte-identical varying-depth fingerprints and 122 adjacent passes with nine GL-only skips. Root viewed the corrected actual capture: all 4,403 blank highlight pixels are amber, with unchanged material depth state. The corrected Cinematics image remains byte-identical. This resolves the observed defect, not complete renderer parity. Evidence: `/tmp/quake3-constant-depth-review-rSlnIH/REVIEW.md`, `/tmp/quake3-constant-depth-independent-run-IwXsbr/` and `.artifacts/checkpoint22-corrected-menu-captures-root.jsonl`.

The client snapshot-number getter also passed independent review and root reproduction: six independent tests with 58 assertions and 173 adjacent passes with 4,138 assertions, no skips. Final combined review and root reproduction each passed thirteen independent tests with 177 assertions, then 337 tests across eighteen suites with eight GL-only skips and 6,192 assertions. The live integration reran that eighteen-suite union successfully. At `17:30:12.302Z`, the exact four getter/renderer postimages and all 1,227 borrowed inputs matched candidate `61f02d4aa780f14dd320175344abee8ec1ee4081e034670a5d7bc8c9083f7caf`, 1,231 inputs. There are now twelve changed source/test paths relative to twenty-one, plus separately recorded documentation/ledger changes. Repeated suite counts are not additional unique coverage. Evidence: `/tmp/quake3-port-stage-fuGrf1/REVIEW.md`, `/tmp/quake3-port-stage-pcD2kE/`, `/tmp/quake3-port-stage-rVewWt/REVIEW.md`, `/tmp/quake3-port-stage-6uPm4y/` and `.artifacts/depth-getter-post-live-tests-root.json`.

Checkpoint twenty-two and its matching build/runtime probes now cover this integration. The captures use direct menu fixtures, not a normal graphical client. Movie playback, actual network connection commands, and full UI flows remain outside this menu-leaf acceptance. No historical stopped renderer audit was resumed.

The reported possible Bun termination did not interrupt any identified Q3 verification. Root checked known handles and terminal results rather than starting duplicate runs. All identified runs exited successfully; no unrelated Bun process was killed. An unidentified process cannot be assessed without its PID or command.

### Checkpoint twenty-one

The preceding accepted combined checkpoint started at `2026-09-06T15:50:26.082Z`, input SHA-256 `a1df19699b10270bcdeb4d8bfda73aa9565a6c59879865ba672877d0a830a0a8`, across 1,225 inputs. Main: **4,096 passed, 83 skipped, zero failed**, 519,380 assertions across 275 files. Selected actual GL: **473 passed, two skipped, zero failed**, 9,493 assertions across 41 files. The selected GL run repeats suites, not additional unique coverage. Strict typing, policy, frozen-snapshot integrity and live-tree matching passed. This adds the seven independently reviewed Team, Server Info, Credits and shared-owner test files described below. See the [twenty-one report](../.artifacts/checks/2026-09-06T15-50-26.082Z.json) and `.artifacts/checkpoint21-check-summary-root.json`.

The matching build at `16:03:57.412Z` used that exact input. Root verified executable SHA-256 `7ccd15c92b18e7964e1a3cc5523b750c329958ed688a0a800840cba662c0467a`. All eight dedicated UDP profiles passed: both products, raw and compiled, quit and SIGTERM. Every case reached two gamestates, 14 valid snapshots, distinct final message numbers 15 and 16, clean exit and port reuse. All eight retail viewer runs passed and matched the four previously inspected CPU/GL hashes. The compiled CLI suite passed eight tests and 70 assertions. At `16:05:30.151Z`, the live input snapshot and binary still matched the checked build. Evidence: `.artifacts/checkpoint21-build-summary-root.json`, `.artifacts/checkpoint21-runtime-summary-root.json`, `.artifacts/checkpoint21-post-probes-snapshot-root.json` and `.artifacts/checkpoint21-compiled-cli-root.log`.

This accepts tested dedicated hosting, controlled client components, viewers and menu leaves. It does not establish a normal playable application, complete matches or full UI flows. Specify Server and Cinematics were integrated afterward and remain excluded from checkpoint twenty-one.

### Integrated after checkpoint twenty

Team, Server Info and Credits are live as six new source/test files, alongside one new shared-owner integration test. All component and combined independent reviews passed, and root reproduced them in fresh private copies. Team passed 15 tests and 222 assertions; Server Info passed 27 tests and 209 assertions. Each reproduction passed helper/project strict typing, policy and exact two-addition inventory checks, with all 1,218 borrowed inputs unchanged. Evidence: `/tmp/quake3-team-menu-replay-kw9zPW/` and `/tmp/quake3-server-info-independent-run-mth8y9/`.

Credits' independent review and root reproduction each passed five independent probes with 185 assertions, 12 submitted tests with 1,920 assertions, and 170 adjacent tests with one GL-only skip and 3,615 assertions. Those repeated suite counts are not additive unique coverage. Root read the full postimages, source bodies, report, runner and probes and checked helper typing/policy before copy-first execution. Evidence: `/tmp/quake3-port-stage-i0X1G8/REVIEW.md`, `/tmp/quake3-credits-independent-root-OijY1Z/root-replay.log` and `/tmp/quake3-port-stage-BUNDcf/`.

Root's joined 17-suite run passed 252 tests, one GL-only skip, zero failures and 5,112 assertions. Independent combined Astra High review and root's complete copy-first replay each passed 220 tests with 4,812 assertions and no skips. These comprise three independent probes and 13 permanent suites. Root read all review helpers and checked their typing/policy before execution. The exact seven additions and all 1,218 unchanged borrowed inputs matched before and after execution. Evidence: `/tmp/quake3-menu-combined-review-V3Vpvr/REVIEW.md`, `/tmp/quake3-menu-combined-run-1UpTtx/` and `.artifacts/menu-combined-independent-root.log`.

Root then verified that the current dependency tree differed from the reviewed baseline only in six recorded documentation/instruction files, integrated the exact seven postimages, and ran all four new live suites: 46 tests passed with 2,330 assertions. At `15:45:05.699Z`, the post-test live snapshot was `4390278cf0ad1918dff1c4437ac256135beeddb91dd33244d20feeb6bec891e2`, with 1,225 inputs and all 1,218 pre-integration inputs unchanged. Evidence: `.artifacts/menu-current-before-integration-root.json`, `.artifacts/menu-post-integration-root.json`, `.artifacts/menu-integrated-live-root.log` and `.artifacts/menu-post-live-tests-root.json`. The source ledger and this documentation were updated afterward. Checkpoint twenty-one and its matching executable/runtime probes now cover this integration.

These tests use actual UI/key/command/CPU owners and protocol-owned configstrings, but invoke menu entrypoints directly. They do not supply a product UI controller, network command host or actual process shutdown. Specify Server and Cinematics subsequently completed the integrations described above. The full-port goal remains active; engineering estimates and closed-gate counts are unchanged.

### Checkpoint twenty

The preceding accepted combined checkpoint started at `2026-09-06T14:10:40.309Z`, input SHA-256 `a4d0c3721a1a50cc91564f16bfd7e2528d364c8e66f53e4ebe96dbd8a7888fc4`, across 1,218 inputs. Main: **4,050 passed, 83 skipped, zero failed**, 517,050 assertions across 271 files. Selected actual GL: **473 passed, two skipped, zero failed**, 9,493 assertions across 41 files. The GL run re-exercises selected suites; its count is not additional unique test coverage. Strict typing, policy, snapshot integrity and live-tree matching passed. See the [twenty report](../.artifacts/checks/2026-09-06T14-10-40.309Z.json), `.artifacts/integration-full-check-20.log` and `.artifacts/checkpoint20-check-summary-root.json`.

This covers the reviewed server packet ordering, standalone entity trace and proximity callback identity changes, plus the canonical private-stage tool. Independent final input review confirmed exactly 20 source/test/tool paths and eight documentation/instruction paths against the recorded pre-integration baseline. Root reproduced the review's inventory and helper checks, including another input comparison after runtime probes. Evidence: `/tmp/quake3-live-input-confirmation-zmjUPw/ADDENDUM.md`, `.artifacts/checkpoint20-final-input-confirmation-root.json` and `.artifacts/checkpoint20-post-probes-final-input-root.json`.

The matching build at `14:19:16.332Z` used that exact snapshot. Root verified executable SHA-256 `2c6c1615a1d955e18859f77dc5069c0b53b3c6f2a729ce7fbce1db7fffddcf95`. All eight dedicated UDP profiles passed: raw and compiled, both products, quit and SIGTERM. Each reached two gamestates, at least 14 valid snapshots, two distinct final packets, clean exit and port reuse. The baseq3 raw SIGTERM case produced 15 snapshots and final message numbers 16 and 17. The other seven produced 14 snapshots and final message numbers 15 and 16. The minimum-14 gate was unchanged.

All eight retail viewer runs passed and reproduced the four previously inspected CPU and GL image hashes. Root's compiled CLI rerun passed eight tests and 70 assertions. At `14:21:10.047Z`, the live input snapshot and binary still matched the checked build. Evidence: `.artifacts/checkpoint20-build-summary-root.json`, `.artifacts/checkpoint20-runtime-summary-root.json`, `.artifacts/checkpoint20-post-probes-snapshot-root.json` and `.artifacts/checkpoint20-compiled-cli-root.log`. These checks accept the dedicated host, controlled client components and viewers, not a normal playable client or a full match. This status update and later documentation edits are outside the checked snapshot.

### Integrated after checkpoint nineteen

Three reviewed components were integrated as 18 changed source/test files, plus two new staging-tool/test files. Checkpoint twenty and its matching executable now cover all 20 paths. The 18-file combined independent review passed with no supported defect. Root read the report and reproduced its exact inventory and helper checks. The reviewer ran 474 tests with 99,365 assertions and eight additional actual dedicated-caller cases with 56 assertions. Root's earlier 915-test run covers the combined candidate. Evidence: `/tmp/quake3-combined-integration-review-4R8BF5/REVIEW.md` and `.artifacts/packet-trace-proximity-final-inventory-root.json`. The following paragraphs preserve component-level evidence before the full checkpoint. Overall completion remains about 50%, and no full milestone gate is closed.

The 13-file packet-delivery change makes the existing server channel send and trace each fragment before committing fragment progress. It preserves source ordering for normal packets and queued messages. Root reproduced premature advancement in both products, read the full patch and independent review, and reran the complete independent verification sequence. The current-dependency run passed 602 tests with 13,238 assertions. Five causal/independent cases passed 33 assertions, and three integrated live suites passed 134 tests with 1,412 assertions. At `13:31:46.825Z`, all 1,203 borrowed inputs were unchanged and the 1,216-input live identity was `cca727e740aa6226e33cabdc23e98d44731dbe9c205a309b3ee164cf40c6cbf2`. Evidence: `.artifacts/channel-delivery-independent-full-root.log`, `.artifacts/channel-delivery-current-combined-root.log` and `.artifacts/channel-delivery-post-integration-root.json`. The existing `ClientSession.transmit` collecting API is unchanged. Actual client packet delivery and connection ownership remain open.

The three-file entity-trace change adds `ServerWorld.traceEntity` through existing geometry and shares its clipping operation with full tracing. It preserves current entity storage, mask rejection, source zero fields and owned result copies without requiring a world link. Root reproduced the independent review's 109 tests and 92,047 assertions, then its four extra cases and 24 assertions against the current packet candidate. The combined packet/trace run passed 763 tests, with four optional native checks skipped, 106,958 assertions and clean whole-project typing/policy. Three integrated live suites passed 52 tests and 91,557 assertions. At `13:51:53.240Z`, the exact three changes and 1,213 unchanged borrowed inputs matched identity `673b094d1a4a053d04c48e19157a980eb5f83fb52d5236a141473162ae12f30d`. Evidence: `.artifacts/entity-trace-independent-full-root.log`, `.artifacts/entity-trace-current-combined-root.log` and `.artifacts/entity-trace-post-integration-root.json`. The actual AAS callback test does not establish production BotLibrary registration or full native ABI parity.

The two-file proximity change gives `MissileRuntime` one owner-bound touch callback and compares the entity's current callback identity. Root reproduced the real two-armed-mine identity failure before the change and its passing result afterward, six assertions each. The combined 30-suite packet/trace/proximity run passed 915 tests, with four optional native checks skipped, 109,588 assertions and clean whole-project typing/policy. Three integrated live suites passed 100 tests and 1,264 assertions. At `13:57:07.718Z`, the exact two changes and 1,214 unchanged borrowed inputs matched identity `ace300f1cb8087fa51000d3594c626d9c0ebba119a17a48929f0893500c52452`. Evidence: `.artifacts/proximity-causal-verified-red-root.log`, `.artifacts/proximity-causal-green-root.log`, `.artifacts/proximity-current-combined-root.log` and `.artifacts/proximity-post-integration-root.json`. Future game AI still owns entity eligibility and publication.

The proximity author's original snapshot omitted six canonical files and the `licenses` directory. Its original tests and verifier cover eleven selected roots, not a complete historical capture. The preserved author addendum and independent confirmation document this limit. Root integrated from a separate complete current snapshot. Evidence: `/tmp/quake3-proximity-identity-stage-RQOyuS/ADDENDUM.md`, `/tmp/quake3-proximity-identity-review-in6XUh/ADDENDUM.md` and `.artifacts/proximity-author-inventory-root.json`.

The canonical staging CLI is now integrated as two new files, and port-unit instructions require it for private work. It uses the existing snapshot owner, checks both independent copies against the source, rejects an inside-source parent and preserves failed stages. Root read both files and the independent report, reproduced its copy-first 28-test/231-assertion run, and reran the three independent cases against current dependencies. Current-candidate and live four-suite runs each passed 25 tests and 187 assertions, with clean whole-candidate typing/policy. At `14:05:41.578Z`, the exact two additions and 1,216 unchanged borrowed inputs matched the 1,218-input identity `9c5d5d3717ab394dfcf04654256d4f54e2c6b96d3225a9253ca8377aa2de588f`. Evidence: `.artifacts/create-port-stage-independent-full-root.log`, `.artifacts/create-port-stage-current-tests-root.log`, `.artifacts/create-port-stage-independent-current-root.log` and `.artifacts/create-port-stage-post-integration-root.json`. Later instruction/documentation updates are separate inputs. This tool does not retroactively repair the historical capture or make the snapshot an atomic filesystem freeze.

Display Options remains design-only. The actual SDL gamma lifecycle and the UI-initialization-time configuration owner do not yet exist. A fabricated capability flag would conceal that dependency. The frozen design is `/tmp/quake3-base-display-contract-mNvuca/CONTRACT.md`. This defers the Display Options unit, not the remaining port.

### Checkpoint nineteen

The preceding accepted combined checkpoint started at `2026-09-06T12:55:13.724Z`, input SHA-256 `8ec9ef293be4a01b10660c5e195dcb0408f5d7e5fe154d518ea70d11d0df7e70`. Main: **3,983 passed, 83 skipped, zero failed**, 516,106 assertions across 270 files. Selected actual GL: **473 passed, two skipped, zero failed**, 9,493 assertions across 41 files. Strict typing, policy, snapshot integrity and live-tree matching passed. This includes the six-file Unix resource extraction and all accepted checkpoint eighteen changes. Independent final review found no supported defect in the six-file Unix extraction. Root reproduced its two actual host lifecycle probes and 28 assertions, inventory checks, and private typing and policy checks. See the [nineteen report](../.artifacts/checks/2026-09-06T12-55-13.724Z.json), `.artifacts/integration-full-check-19.log` and `.artifacts/unix-io-final-combined-root.log`.

The matching build at `13:04:12.716Z` used that exact snapshot. Root verified executable SHA-256 `4a0ea9763b7959dcd998443af28f3598c20f1b047512e5ef71c9fcbc2ff4eabb`. All eight dedicated UDP profiles passed: raw and compiled, both products, quit and SIGTERM. Each reached two gamestates, 14 valid snapshots, two distinct final packets, clean exit and port reuse. All eight retail viewer runs matched the four previously inspected CPU and GL image hashes. Root's compiled CLI rerun passed eight tests and 70 assertions. At `13:06:22.244Z`, the post-probe live snapshot still matched across 1,216 inputs. Evidence: `.artifacts/checkpoint19-build-summary-root.json`, `.artifacts/checkpoint19-runtime-summary-root.json`, `.artifacts/checkpoint19-post-probes-snapshot-root.json` and `.artifacts/checkpoint19-compiled-cli-root.log`. This accepts the shared Unix owner for the existing dedicated caller, not SDL event delivery, a normal playable client or a full match. Later documentation edits and private packet-delivery work are outside this checked snapshot.

### Checkpoint eighteen

The preceding accepted combined checkpoint started at `2026-09-06T12:32:20.829Z`, input SHA-256 `f4830f2bd396212005615cc21b85aac5fd112b4edbcf6a296394d581919a1a61`. Main: **3,980 passed, 83 skipped, zero failed**, 516,091 assertions across 270 files. Selected actual GL: **473 passed, two skipped, zero failed**, 9,493 assertions across 41 files. Strict typing, policy, snapshot integrity and live-tree matching passed. This adds the six queue-timestamp and reliable-ring source/test changes described below. Independent final review found no defect; root reproduced its 18 joined cases and 430 assertions, complete inventory and private typing/policy checks. See the [eighteen report](../.artifacts/checks/2026-09-06T12-32-20.829Z.json), `.artifacts/integration-full-check-18.log` and `.artifacts/queue-reliable-final-combined-root.log`.

The matching build at `12:40:27.591Z` used that exact snapshot. Root verified executable SHA-256 `93db4d127ac04b1559cbef82929471e5975b9f7150726d9931e7390d7647cafc`. All eight dedicated UDP profiles passed: raw and compiled, both products, quit and SIGTERM. Each reached two gamestates, 14 valid snapshots, two distinct final packets, clean exit and port reuse. All eight retail CPU/GL viewer runs matched the four previously inspected image hashes. Root's compiled CLI rerun passed eight tests and 70 assertions. The post-probe live snapshot still matched across 1,215 inputs. Evidence: `.artifacts/checkpoint18-build-summary-root.json`, `.artifacts/checkpoint18-runtime-summary-root.json` and `.artifacts/checkpoint18-post-probes-snapshot-root.json`. These remain component/dedicated/viewer checks, not a normal playable client or full match. The private Unix extraction and future SDL/client-owner work are excluded. Later documentation edits are outside this checked snapshot.

### Checkpoint seventeen

The preceding accepted combined checkpoint started at `2026-09-06T11:51:26.012Z`, input SHA-256 `69c87171d8ee298bc16e07c41f19005d9e3e25139ff99ebe87a27788a7ef6c08`. Main: **3,965 passed, 83 skipped, zero failed**, 514,280 assertions across 270 files. Selected actual GL: **473 passed, two skipped, zero failed**, 9,493 assertions across 41 files. Strict typing, policy, snapshot integrity and live-tree matching passed. This adds Sound Options and the canonical session CommonError correction as three additions and one changed file. Independent final review verified this exact delta and 549 unchanged scoped code/test/tool/config entries, with no new finding. Root read the complete report, reran its inventory and adapter checks, and passed 107 combined tests with 3,492 assertions. See the [seventeen report](../.artifacts/checks/2026-09-06T11-51-26.012Z.json), `.artifacts/integration-full-check-17.log` and `/tmp/quake3-check17-independent-pqA6gR/REVIEW.md`.

The matching build at `11:59:52.225Z` used that exact snapshot. Root verified executable SHA-256 `67dda1752d0862b0209ecb434690442115b63fef0058056c7e243f8e04160795`. All eight dedicated UDP profiles passed: raw and compiled, both products, quit and SIGTERM. Each reached two gamestates, 14 valid snapshots, two distinct final packets, clean exit and port reuse. All eight retail CPU/GL viewer runs matched the four previously inspected image hashes. Root's compiled CLI rerun passed eight tests and 70 assertions. The post-probe live snapshot still matched across 1,215 inputs. Evidence: `.artifacts/checkpoint17-runtime-summary-root.json`, `.artifacts/checkpoint17-post-probes-snapshot-root.json` and `.artifacts/integration-build-17.log`. These remain component/dedicated/viewer checks, not a normal playable client or full match. The newly discovered queue-timestamp correction, reliable-ring ownership and Unix/SDL changes are excluded. Later documentation edits are outside this checked snapshot.

### Checkpoint sixteen

The preceding accepted combined checkpoint started at `2026-09-06T11:19:35.300Z`, input SHA-256 `f2765d44fcf37a678fc709381a06758d0d572af2d46eb75f283185152858d5ed`. Main: **3,869 passed, 83 skipped, zero failed**, 511,366 assertions across 268 files. Selected actual GL: **473 passed, two skipped, zero failed**, 9,493 assertions across 41 files. Strict typing, policy, snapshot integrity and live-tree matching passed. This adds Preferences, Network Options and the recovered client-input module as six source/test files. Independent final review verified exactly six additions and 541 unchanged source/test/tool files, with live matching the reviewed tree and no additional finding. Root read the report, reran its inventory and passed its six combined live cases with 76 assertions. See the [sixteen report](../.artifacts/checks/2026-09-06T11-19-35.300Z.json), `.artifacts/integration-full-check-16.log` and `/tmp/quake3-six-combined-review-PuRyEx/REVIEW.md`.

The matching build at `11:28:29.690Z` used that exact snapshot. Root verified executable SHA-256 `bf733ab476f25c6efdae057d448b27ecefba3be8c755b21810fec9f0ce88d34a`. All eight dedicated UDP profiles passed: raw and compiled, both products, quit and SIGTERM. Each reached two gamestates, 14 valid snapshots, two distinct final packets, clean exit and port reuse. All eight retail CPU/GL viewer runs matched the four previously inspected image hashes. Root's compiled CLI rerun passed eight tests and 70 assertions. At `11:31:11.942Z`, the post-probe live snapshot still matched across 1,212 inputs. Evidence uses `.artifacts/checkpoint16-*` and `.artifacts/integration-build-16.log`. These remain component/dedicated/viewer checks, not a normal playable client or full match. Sound Options and the separate session-error correction are excluded. Later documentation edits are outside the checked snapshot.

The preceding focused integration checks passed 561 tests with three GL-only skips and 6,577 assertions across 25 files; direct live checks passed 53 tests and 524 assertions. The four input/Network additions matched approved hashes and all 1,208 other inputs were unchanged. Whole-project typing and `policy` passed. The first wrapper requested nonexistent `check:policy`; that failed invocation remains separately recorded, not represented as a passing check. Evidence: `.artifacts/input-network-joined-focused-root.log`, `.artifacts/input-network-joined-types-root.log`, `.artifacts/input-network-joined-policy-correct-root.log`, `.artifacts/input-network-post-integration-root.log` and `.artifacts/input-menus-integrated-live-root.log`.

### Checkpoint fifteen

The preceding accepted combined checkpoint started at `2026-09-06T10:29:42.898Z`, input SHA-256 `2aabe9c0c9c653735af920e511eeb849fb8c5b8d3de90308f7693e95d4fb2a66`. Main: **3,816 passed, 83 skipped, zero failed**, 510,842 assertions across 265 files. Selected actual GL: **473 passed, two skipped, zero failed**, 9,493 assertions across 41 files. Strict typing, policy, snapshot integrity and live-tree matching passed. This adds the 20 common lifecycle and download-owner files described below to checkpoint fourteen. The independent final live comparison verified those exact 20 changes against the reviewed preimages and postimages, with no other source/test changes or new findings. Root read the report and reran its comparison. See the [fifteen report](../.artifacts/checks/2026-09-06T10-29-42.898Z.json), `.artifacts/integration-full-check-15.log` and `/tmp/quake3-common-live-review-DEZlp8/REVIEW.md`.

The matching build at `10:41:34.974Z` used that exact snapshot. Root verified executable SHA-256 `c07541f1ae826901efd382433aa6c900905525168bc946cccc746faae78e5c63`. All eight dedicated UDP profiles passed: raw and compiled, both products, quit and SIGTERM. Each reached two gamestates, 14 valid snapshots, two distinct final packets, clean exit and port reuse. All eight retail CPU/GL viewer runs matched the four previously inspected image hashes. Root's compiled CLI rerun passed eight tests and 70 assertions. At `10:42:46.412Z`, the post-probe live snapshot still matched across 1,206 inputs. Evidence uses `.artifacts/checkpoint15-*` and `.artifacts/integration-build-15.log`. This accepts the integrated common runtime unit, not a graphical client, full match or complete milestone. Later documentation edits are outside the checked snapshot; the separately staged Preferences menu is not included.

### Checkpoint fourteen

The preceding accepted combined checkpoint started at `2026-09-06T09:35:23.329Z`, input SHA-256 `c0d0387c37feda57c4d3bd836e52ee149a3986443dc0e0563c9aeaeec1d6bf92`. Main: **3,499 passed, 83 skipped, zero failed**, 506,929 assertions across 263 files. Selected actual GL: **473 passed, two skipped, zero failed**, 9,493 assertions across 41 files. Strict typing, policy, snapshot integrity and live-tree matching passed. This adds the nine font, VM-word and lexer files described below to checkpoint thirteen. The separate combined code/caller review found no defects; root checked its exact nine-file delta and reviewed the report. See the [fourteen report](../.artifacts/checks/2026-09-06T09-35-23.329Z.json), `.artifacts/integration-full-check-14.log` and `/tmp/quake3-check14-integration-review-FRWstV/REVIEW.md`.

The matching build at `09:43:03.981Z` used that exact snapshot. Root verified executable SHA-256 `861d46a7088a4c147bc1b4656c02b230512bac804bedc8f46c37b5e1581a6e81`. All eight dedicated UDP profiles passed: raw and compiled, both products, quit and SIGTERM. Each reached two gamestates, 14 valid snapshots, two distinct final packets, clean exit and port reuse. All eight retail CPU/GL viewer runs matched the four previously inspected image hashes. Root's compiled CLI rerun passed eight tests and 70 assertions. At `09:45:03.890Z`, the post-probe live snapshot still matched across 1,201 inputs. Evidence uses `.artifacts/checkpoint14-*` and `.artifacts/integration-build-14.log`. These checks do not include the staged common lifecycle changes or close a full milestone. Later documentation edits are not retroactively part of the snapshot or executable.

### Checkpoint thirteen

The preceding accepted combined checkpoint started at `2026-09-06T08:45:32.143Z`, input SHA-256 `195661f0e6214a3877dc5feef1ead454437e50f0403a8f7fce319a3ecd5429e9`. Main: **3,476 passed, 83 skipped, zero failed**, 506,533 assertions across 261 files. Selected actual GL: **473 passed, two skipped, zero failed**, 9,493 assertions across 41 files. Strict typing, policy, snapshot integrity and live-tree matching passed. This includes all twelve-b units plus the fourteen UI and PK3 files described below. See the [thirteen report](../.artifacts/checks/2026-09-06T08-45-32.143Z.json) and `.artifacts/integration-full-check-13.log`.

The matching build at `08:54:06.527Z` used that exact snapshot. Root verified executable SHA-256 `d253c63a5cc139586908ed9d336805df4f9b107d28c791aa009443afcf03d885`. All eight dedicated UDP profiles passed: raw and compiled, both products, quit and SIGTERM. Each reached two gamestates, 14 valid snapshots, two distinct final packets, clean exit and port reuse. All eight retail CPU and GL viewer runs matched the four previously inspected image hashes. Root's compiled CLI rerun passed eight tests and 70 assertions. At `09:02:03.420Z`, the post-probe live snapshot still matched. Evidence uses `.artifacts/checkpoint13-*` and `.artifacts/integration-build-13.log`. Later documentation edits are not retroactively part of the snapshot or executable. No complete milestone gate is closed.

### UI and filesystem units included in thirteen

Root integrated three independently reviewed units: the corrected nine-file base UI foundation, Team Arena's two-file 110-entry cvar unit, and three-file ordered PK3 metadata unit. All fourteen integrated hashes match the reviewed files. The live seven-suite UI run passed 84 tests and 995 assertions with one explicit GL skip; live PK3/VFS passed 48 tests and 241 assertions. Whole-project strict typing and policy passed. Checkpoint thirteen and its matching executable now cover these changes together.

Base UI includes all ten widget kinds, menu fields, shared state/drawing/cvars and confirmation behavior. Root reran the unchanged native draw, field, scroll, math, cvar and font comparisons. The bitmap pulse correction additionally matches the inspected shipped QVM's float operations and real queue bytes, and its original independent failure now passes. This does not run the retail QVM or establish universal libm parity. Team Arena registration/update matches the original 110 entries and 241 native trace rows. Its later 39 direct integer-word writes remain a separately designed canonical-mirror extension. PK3 metadata matches 6,837 native record-visits, including 6,803 distinct retail records, without changing existing admission, normalized reads or checksums. Shared filesystem handles and genuine UI file listing are not implemented by this metadata addition.

Evidence: `.artifacts/base-ui-integrated-root.log`, `.artifacts/base-ui-corrected-gate-root.log`, `.artifacts/team-ui-cvars-native-root.log`, `.artifacts/team-ui-cvars-integrated-root.log`, `.artifacts/pk3-source-names-gate-root.log` and `.artifacts/pk3-source-names-integrated-root.log`. Independent reports: `/tmp/quake3-base-ui-pulse-confirm-yiXMU0/CONFIRMATION.md`, `/tmp/quake3-team-ui-cvars-review-0F1myw/REVIEW.md`, and `/tmp/quake3-pk3-source-names-review-Aas6wG/REVIEW.md`. No main menu, product controller, normal playable client or complete gate is claimed.

### Current integration and staged work

The common integration manifest checked 21 possible paths: 20 changed files and `src/engine/common-error.ts`, which was absent both before and after integration. The real error type moved to `src/core/common-error.ts`. The unchanged absence is a checked target, not a 21st changed file or a retained compatibility module.

The common driver is now **integrated and covered by checkpoint fifteen and its matching executable**. All 20 changed live files match the reviewed hashes. The earlier source-control, first-error quit, initialization and terminal-disposal findings have been corrected together. Root's joined rerun passed 653 tests and 10,187 assertions across 23 files, all 39 initializer regressions, strict typing, policy and five raw CLI tests. Both real-game child probes retained games.log and qconsole.log at the recursive source-abort boundary, then explicitly closed both, detached the game, removed listeners and rebound UDP; each reported passed=true with the expected fatal exit. Root also reran the independent 30-case real-host probe across both products, all five error types and frame/limited-run/close entry. The combined reviewer found no remaining defect in this bounded scope. Evidence: `.artifacts/common-combined-staged-root.log`, `.artifacts/common-combined-manifest-root.log`, `.artifacts/common-combined-independent-host-root.jsonl`, `/tmp/quake3-common-root-rerun-f7B8K6`, and `/tmp/quake3-common-combined-review-YDKces/REVIEW.md`. The subsequent independent final live review and full checkpoint are recorded above.

The two-file shared font pulse correction and five-file canonical VM integer-word unit are now integrated after independent review and root's source, test and native checks. Their combined live UI/font/console run passed **103 tests and 1,362 assertions**, with four explicit native-oracle skips and no failures. Actual retail GL ran. Root's staged checks passed 79 font tests and 74 VM-word tests. The VM native rebuild matched 283 rows; its independent boundary suite passed four tests and 2,480 assertions. The font's independently modeled float operations matched 250,020 sampled cases, separate from the real queue tests. All seven integrated hashes match their reviewed files. Checkpoint fourteen and its matching executable now cover these changes. Evidence uses `.artifacts/font-pulse-*`, `.artifacts/cvar-vm-words-*`, `/tmp/quake3-font-pulse-review-m7cJps/REVIEW.md`, and `/tmp/quake3-vm-cvar-words-review-8spiW4/REVIEW.md`.

The two-file canonical lexer correction is now integrated after independent review and root verification. Both native profiles matched 58 cases, 52 tokens and 20 diagnostics; root's independent scanner-state rerun passed four tests and 21 assertions. The live 13-suite rerun passed 179 tests and 15,893 assertions, with two optional goal-oracle skips. Its first live run failed one five-second corpus timeout while whole-project typing and policy ran concurrently; the unchanged serial rerun passed in 6.27 seconds. Both logs are retained. Strict typing and policy passed. Numeric conversion, arbitrary Unicode and the HUD's separate COM_ParseExt ownership gap are not recertified. Evidence: `.artifacts/lexer-*` and `/tmp/quake3-lexer-review-xvkwOo/REVIEW.md`. Checkpoint fourteen and its matching executable now cover all nine font, VM-word and lexer files together.

The integrated common lifetime keeps game/server resource release separate from source shutdown. One server-owned bot capability survives map replacement, and early common adoption preserves source error handling before filesystem construction. Explicit close finitely releases owned resources without replaying abandoned source callbacks. Post-Com_Init startup failure and bounded system-stage cleanup are documented managed adaptations, not native recoverable-jump or infinite-recursion parity. Genuine graphical client/listen ownership and canonical BotLibrary composition remain absent.

The owner prerequisite initially passed 37 ownership tests and 445 adjacent tests, but independent review and root found a real-descriptor handoff gap in a declared provider subclass and a done/debug callback that continued after disposal. The corrected two-file dependency is included in the integration. Root reproduced 36 causal failures becoming 48 passes, retained all 37 prior ownership cases, and passed 493 adjacent tests with strict typing/policy clean. Root's independent 14-case rerun passed 158 assertions, including exact thrown-value identity and safe descriptor-number reuse after late release. The original failure is not a demonstrated leak through the unchanged production provider. Evidence: `.artifacts/common-download-correction-staged-root.log`, `.artifacts/common-download-confirm-probes-root.log`, `/tmp/quake3-download-root-rerun-4pcUZ0`, and `/tmp/quake3-download-confirm-bh2Rbh/CONFIRMATION.md`. Counts overlap and must not be added as unique coverage.

The genuine base Preferences menu is integrated as two new files after independent review and root's current-dependency rerun: 40 tests and 296 assertions passed with strict typing and policy clean. The original forced-cvar-write failures now pass, and permanent tests distinguish pending-latch clearing from non-forced brass reset. The actual menu refresh and CPU queue verify crosshair geometry, texture coordinates and source colors, alongside branch, clamp, key, stable-record and async-lifetime cases. Both live file hashes match the final reviewed stage. Root's before/after manifest check verified every one of the 1,206 borrowed inputs unchanged. Evidence: `.artifacts/preferences-current-deps-root.log`, `.artifacts/preferences-post-integration-root.log`, `/tmp/quake3-base-preferences-final-jez5Y9/HANDOFF.md`, and `/tmp/quake3-preferences-final-review-9gnPan/REVIEW.md`. This is a menu leaf; checkpoint sixteen and its matching executable now cover it.

The engine screen contract remains design-only, with queued per-eye output selection and load-specific redraw revocation required. Its original proposed blanket recursion rejection was corrected to distinguish source behavior from async ownership. Product UI, demo-recording state, cinematic rebinding and full renderer begin-frame behavior remain prerequisites. The genuine Network Options menu is now integrated over the accepted base framework. Independent review passed all 17 author cases and three additional probes, 265 assertions, with scoped typing/policy clean. Root reran its delayed cache/cvar sampling, exact focus order, equal-string pending-latch and actual CPU artwork probes against current dependencies. Its left artwork changed 2,203 pixels inside the expected rectangle and zero outside. Required sibling callbacks are real integration boundaries, not implementations of the other menus. The later Sound Options integration is recorded below. No leaf supplies the product controller or a normal playable menu flow.

The saved client-input unit is now corrected and integrated. Its fixtures use the accepted lifecycle and awaited gamestate path. Native integer joystick axes outside the six slots now throw canonical `CommonError("drop")`, not `RangeError`; all other assembly algorithms are unchanged. Root reproduced the final tests against the original production file: 21 passed and six failed, then passed all 27 corrected cases, including both-product common-frame drop recovery and genuine keys-to-session history. Independent confirmation passed 49 tests and 358 assertions with scoped typing/policy clean. Root freshly built the unchanged approved native oracle and reproduced **15,436 exact SSE public rows across both products**, 13 cases and 3,223 source frames. x87 differed in 254 rows but no finished-command fields within this corpus. This is the selected binary32/wrap profile, not universal native/QVM equivalence; valid-axis native cases do not prove the separate invalid-axis error path. Native intermediate command-number/history/graph publication remains a documented integration qualification. SDL event delivery and production send scheduling remain open. Evidence: `.artifacts/client-input-correction-red-root.log`, `.artifacts/client-input-native-root.log`, `.artifacts/input-network-independent-root.log` and `/tmp/quake3-input-recovery-confirm-y0wZgk/CONFIRMATION.md`.

The two-catch session CommonError correction and base Sound Options are now integrated after independent component review. Root's 23-suite combined run passed 469 tests with three GL-only skips and 7,676 assertions; whole-project typing and policy passed. The five-suite live run passed 146 tests and 4,001 assertions. All four source/test postimages match the reviewed candidate and all 1,211 borrowed inputs were unchanged. Independent final combined review, checkpoint seventeen and its matching executable now cover these four files.

The session correction preserves canonical common-error identity and source partial publication without changing ordinary parser failures. Root reproduced five original failures and the permanent 16-pass/62-fail RED matrix, then passed current-dependency asynchronous and synchronous probes, five tests and 20 assertions each. The permanent corrected matrix passed 78 tests and 2,694 assertions in independent review. The pre-admission reliable ring is now integrated as described below. The broader connection owner still needs source-ordered packet timing and authority to tear down a sticky-failed session. Evidence: `.artifacts/client-session-control-final-red-root.log`, `.artifacts/session-control-synchronous-current-root.log`, `/tmp/quake3-session-control-review-8i1oRY/REVIEW.md` and `/tmp/quake3-client-connection-design-jwChCU/CONTRACT.md`.

Sound Options passed root's current-dependency rerun: 19 tests and 758 assertions, including 538 independent exact-rational comparisons for the selected float32/default-C-locale formatting profile. Actual framework tests cover forced volume writes, source-order menu closure and queued `snd_restart`. The menu does not implement the restart command or product controller. Evidence: `.artifacts/sound-options-current-deps-root.log`, `.artifacts/sound-session-joined-focused-root.log`, `.artifacts/sound-session-integrated-live-root.log`, `.artifacts/sound-session-post-integration-root.log` and `/tmp/quake3-sound-options-review-0H5xW2/REVIEW.md`.

The corrected SDL contract is accepted as design constraints only. It preserves view/input state at focus and video boundaries, supplies source control characters absent from SDL text input, and separates defined joystick behavior from managed rejection of undefined source cases. No graphical platform implementation is approved yet. The shared Unix resource-owner extraction is now integrated after independent component review and root verification. It gives console, UDP, clock and signal resources one owner shared with the dedicated caller before adding SDL event delivery. Evidence: `/tmp/quake3-sdl-common-contract-addendum-BmBXIs/ADDENDUM.md` and `/tmp/quake3-unix-io-contract-1DqtKk/CONTRACT.md`.

The extraction changes five existing files and adds `src/platform/unix-io.ts`. All six hashes match the reviewed candidate; all 1,210 borrowed inputs from the current-dependency baseline are unchanged. Root's joined 11-suite run passed 444 tests and 9,935 assertions with whole-project typing/policy clean; the three live suites passed 36 tests and 2,006 assertions. Root also reproduced the five independent probes and their 19 assertions against current dependencies. This includes the newer reliable-ring files absent from the author's earlier capture. Independent final combined review and checkpoint nineteen with its matching executable now cover this extraction. Root reproduced the final review's two real host lifecycle probes with 28 assertions and its complete inventory and private typing and policy checks. The review verified 1,198 scoped live inputs against the candidate; the five later documentation changes were excluded explicitly. Evidence: `.artifacts/unix-io-current-combined-corrected-root.log`, `.artifacts/unix-io-post-integration-root.log`, `.artifacts/unix-io-integrated-live-root.log`, `.artifacts/unix-io-independent-current-root.log` and `/tmp/quake3-unix-io-review-w3HCMM/REVIEW.md`.

The separate Unix methods reject a closed resource owner. If a standalone caller closes that owner during a controlled UDP diagnostic print and returns, the old combined method returned an empty event and sampled the clock; the extraction rejects further polling without another sample. This is an explicit managed terminal-lifetime difference. The supported dedicated-host close API rejects overlapping common operations before cleanup, and the source print path does not close networking and return. Console truncation-print closure already rejected in the preimage. The independent report retains both old/new observations and corrects approximate source locators in the frozen author handoff.

Root's source review found a queue-timestamp ordering defect: `Sys_QueEvent` samples zero time after the overflow print, but the old TypeScript sampled before. The causal test observed 55 instead of 1,055 after the print advanced the clock. The independently reviewed two-file correction is now integrated. All 20 existing tests are unchanged; seven new tests cover timing, ownership and exact thrown values. The author's old-code run passed 21 cases and failed six; corrected code passed 27. Root reproduced the original failing test and its passing live result, plus independent null/undefined print-error probes. Injected clock failure retains the documented managed discard-without-append behavior, not native reserved-slot parity. Evidence: `.artifacts/unix-queue-overflow-order-red-root.log`, `.artifacts/unix-queue-overflow-order-live-root.log`, `.artifacts/unix-queue-independent-current-root.log` and `/tmp/quake3-queue-order-review-wFOdjJ/REVIEW.md`.

The four-file reliable-ring migration is also integrated after independent review. The pre-admission connection now owns the existing ring and each session borrows it. Both-product tests transmit queued commands through actual channels, acknowledge them on the same ring, retain it across gamestate changes and reach the real ServerEngine. The eight old-code failures were structural failures caused by the absent new state member, not eight demonstrated packet regressions. Independent review passed 149 tests and 3,428 assertions; root reran its four additional probes against current dependencies. No new source function or complete connection controller is claimed. Evidence: `.artifacts/reliable-owner-independent-current-root.log` and `/tmp/quake3-client-reliable-review-782HVe/REVIEW.md`.

Root's combined 16-suite run passed 514 tests and 9,857 assertions, with whole-project typing and policy clean. Three live suites passed 121 tests and 3,652 assertions. All six reviewed postimages matched the integrated tree, and all 1,209 borrowed inputs from the phase baseline were unchanged. The 1,215-input candidate/live hash was `f73971404715654130d42fe14b28a4a739de8cffea531f962d753eeb95e87377` before later documentation edits. Against checkpoint seventeen itself, the delta also includes three earlier documentation changes, so its unchanged count is 1,206. Independent final review and root's rerun passed 18 joined tests and 430 assertions, plus complete inventory, private typing and policy checks. Checkpoint eighteen and its matching executable now cover both units. Evidence: `.artifacts/queue-reliable-joined-focused-root.log`, `.artifacts/queue-reliable-integrated-live-root.log` and `.artifacts/queue-reliable-post-integration-root.log`, `.artifacts/queue-reliable-final-combined-root.log` and `/tmp/quake3-queue-reliable-combined-review-yjNKF9/REVIEW.md`.

The separate bot BSP-entity worker and later independent inflater bit/history audit stopped after tool safety refusals. Their evidence is preserved; the refused operations are not being retried or delegated to substitutes. Shared filesystem handles depend on the genuine resumable ZIP reader design, whose normal-stream/API work is available but whose bit/history qualification remains unverified. No decoder implementation is approved. No product controller or BotLibrary coordinator is established by these units.

### Checkpoint twelve-b

The preceding accepted combined checkpoint started at `2026-09-06T08:08:10.096Z`, input SHA-256 `c5aaea61a01c14d55f0a25a2dd7dbbbf640f21c7e64c63277de76cf9a7a3ca48`. Main: **3,430 passed, 83 skipped, zero failed**, 505,718 assertions across 257 files. Selected actual GL: **473 passed, two skipped, zero failed**, 9,493 assertions across 41 files. Strict typing, policy, snapshot integrity and live-tree matching all passed. This supersedes failed checkpoint twelve and includes the integrated clock/parser, console, alternative routing, font, VM cvars, bot logger and explicit-file test runner. See the [twelve-b report](../.artifacts/checks/2026-09-06T08-08-10.096Z.json) and `.artifacts/integration-full-check-12b.log`.

The matching build at `08:15:46.853Z` used that exact snapshot. Root verified executable SHA-256 `3aa2c1840afdfdb6699f3bce313e0827757736aa96515e3059c41526444afec1`. All eight dedicated UDP profiles passed: raw/compiled, both products, quit/SIGTERM. Each reached two gamestates, 14 or 15 valid snapshots, distinct final packets and disconnect sequences, clean exit and port reuse. All eight retail CPU/GL viewer runs matched the four previously inspected image hashes. Root's compiled CLI rerun passed eight tests and 70 assertions. The post-probe live snapshot still matched. Evidence uses `.artifacts/checkpoint12b-*` and `.artifacts/integration-build-12b.log`. The later UI, PK3 and documentation changes above are not part of that snapshot or executable.

### Integrated units included in twelve-b

Bot logging is now integrated after independent review and root's complete source/test review. Root reproduced 57 staged tests with 338 assertions and 29 frozen-native comparisons, then passed **102 live tests and 810 assertions across seven suites**, including both retail products. The seven integrated hashes match the reviewed manifest. The 4096-byte printf-output buffer is a measured Linux/glibc profile; managed cleanup and home/product path rules are explicit adaptations. No BotLibrary coordinator or full bot-match acceptance is implied. Evidence: `.artifacts/bot-log-staged-root.log`, `.artifacts/bot-log-native-root.log`, `.artifacts/bot-log-integrated-root.log` and `/tmp/quake3-bot-log-independent-review-FINAL.md`.

The explicit-file test runner is also live and root-reviewed. Root passed **30 tests and 145 assertions across three suites**, including actual subprocess checks that a missing later test path prevents every test from starting. The Luna worker violated its isolated-stage assignment by adding these two files to the live tree during checkpoint twelve. No existing file was overwritten. Root preserved and reviewed the exact files, stopped that worker's writes, and recorded the failed integrity gate below. The new runner does not retroactively validate earlier mistyped test commands. Evidence: `.artifacts/focused-runner-cvar-root.log` and `.artifacts/checkpoint12-input-diff-manifests-root.json`.

Alternative routing, the signed-byte base UI font correction and stable VM cvar mirrors are also integrated after independent review. Root's four staged native routing profiles each passed 18 tests and 73,481 assertions; the live ten-suite routing/movement run passed **237 tests and 96,345 assertions**. The integrated font/console retail GL run passed **31 tests and 213 assertions**, with five native-oracle skips. The font change also matches all 256 native byte inputs and the inspected shipped UI bytecode's signed load. The cvar mirror passed root's 24 staged tests, three separate independent probes, ten native rows and **88 live adjacent tests with 930 assertions**. The cvar API preserves cleared-slot identity and partial count publication; its typed error still needs the future engine trap boundary. Whole-project typing/policy passed after routing/font integration; the subsequent cvar addition was pending before twelve-b and is included in that accepted checkpoint. Evidence uses `.artifacts/alternative-*`, `.artifacts/font-console-integrated-gl-root.log` and `.artifacts/cvar-vm-*`. These are bounded components, not bot matches, a product UI or complete native numeric parity.

The reviewed client clock/parser and corrected console/text units are integrated, and are now covered by checkpoint twelve-b and its matching executable. Root verified all 17 clock file hashes, including the separate admission-concurrency correction. The live clock/keys/server/client run passed **223 tests and 2,841 assertions across 14 files**; whole-project typing and policy passed. After integrating the five exact console files, the combined console/clock/keys/client run passed **91 tests and 901 assertions across eight files**. Actual OpenGL and retail CPU console checks for both products passed **11 tests and 77 assertions**; whole-project typing and policy passed again. Each of three staged native console profiles also passed 11 tests and 73 assertions. Logs use `.artifacts/client-clock-integrated-*`, `.artifacts/console-corrected-*`, `.artifacts/console-clock-integrated-root.log` and `.artifacts/console-integrated-*`.

The pause-point archive was completed and compared successfully before resume; [RECOVERY.md](RECOVERY.md) records its verified location, hash and staged work. Resumed changes are not retroactively covered by that archive. Current staged findings are recorded above.

### Checkpoint twelve failed live-tree integrity

The run started at `2026-09-06T07:47:38.255Z`, snapshot SHA-256 `2369e0034b54c9445c48a68afcaebc6a4d0539684dbd974038bee8b5809c014b`. Strict typing and policy passed. Main tests: **3,384 passed, 83 skipped, zero failed**, 505,421 assertions across 253 files. Selected actual GL: **473 passed, two skipped, zero failed**, 9,493 assertions across 41 files. The owned snapshot stayed unchanged, but the live tree did not match, so the overall command exited **1**. This is not an accepted live-tree checkpoint and has no matching accepted build.

Root compared both manifests and found only two added paths: `tools/run-focused-tests.ts` and `tests/focused-test-runner.test.ts`. The worker acknowledged three live `apply_patch` calls despite its stage-only assignment. Root reviewed and reran the exact files before accepting them as a bounded addition. The bot logger was integrated afterward. A new combined checkpoint must cover all current inputs. See the [failed report](../.artifacts/checks/2026-09-06T07-47-38.255Z.json) and `.artifacts/integration-full-check-12.log`. At that point, checkpoint eleven remained the latest accepted full checkpoint and executable.

### Checkpoint eleven

The latest full green checkpoint, eleven, started at `2026-09-06T06:24:02.164Z`, input SHA-256 `1ad7de3e8360df93bccc213325e7b1ec3f74597ade0577616da0b97d384089f6`. Main: **3,308 passed, 74 skipped, zero failed**, 504,919 assertions across 246 files. The selected actual-GL rerun passed **468 tests**, with one skip, zero failures and 9,445 assertions across 40 files. Strict typing, policy, snapshot integrity and live-tree matching passed. This includes the reviewed keys/edit-fields revision. It does not accept the separately staged input, clock, console, common driver, UI or bot work. See the [checkpoint eleven report](../.artifacts/checks/2026-09-06T06-24-02.164Z.json) and `.artifacts/integration-full-check-11.log`.

The matching build at `06:31:41.737Z` used that exact snapshot. Root verified actual executable SHA-256 `d2bf6b6018acaee47b5efd4a728ab725f31c3376f0b931183779a63121dcf4c4`. All eight real dedicated UDP profiles passed: raw and compiled, both products, quit and SIGTERM. Every profile reached two gamestates, 14 valid movement snapshots and two distinct shutdown datagrams, with map changes, configuration/log checks, exit zero and port reuse. All eight retail CPU/GL viewer runs reproduced the four previously inspected image hashes. Root reran the independent compiled CLI suite: eight passed, zero failed, 70 assertions. The live snapshot still matched after all probes. Evidence uses `.artifacts/checkpoint11-*` and `.artifacts/integration-build-11.log`. These certify the dedicated-command and viewer profiles, not normal matches. Later edits, including this report, are not retroactively part of that snapshot.

### Checkpoint ten

The preceding full green checkpoint started at `2026-09-06T05:37:05.142Z`, input SHA-256 `c848b072cff747f98e9f2fb524353023b7a4ed773d8fc913081802e8457a7757`. Main: **3,280 passed, 74 skipped, zero failed**, 504,482 assertions across 244 files. Actual GL: **468 passed, one skipped, zero failed**, 9,445 assertions across 40 files. Strict typing, policy, snapshot integrity and live-tree matching all passed. This accepts the independently reviewed native-angle boundary, AAS entity history, route prediction, synchronous loaders and typed command fallback together. Optional native-oracle tests remain separately evidenced by root's focused runs; skips are not passes. See the [checkpoint report](../.artifacts/checks/2026-09-06T05-37-05.142Z.json) and `.artifacts/integration-full-check-10.log`.

The matching build at `05:47:15.141Z` used that exact input; root verified actual executable SHA-256 `aa52865eb06eaa296f7af557dcf2ecb01966a8fc4d50e7cfed87ce4d5a407b6a`. All eight real dedicated UDP profiles passed: raw/compiled, both products, quit/SIGTERM. Each checked movement, fast/full maps, two gamestates, 14 valid snapshots, two distinct shutdown datagrams, configuration/logs, exit zero and port reuse with SDL deliberately unavailable. All eight CPU/GL retail viewer profiles reproduced the four previously inspected image hashes. The independent compiled CLI suite, rerun by root, passed eight tests and 70 assertions. Logs use `.artifacts/checkpoint10-*` and `.artifacts/integration-build-10.log`. Root rechecked the unchanged live snapshot after these probes. Keys/input, clock/parser, alternative routes, common-client and console/screen stages are excluded. Later documentation edits are not retroactively covered. These are dedicated-command and viewer checks, not normal matches.

### Checkpoint nine

The previous full green checkpoint started at `2026-09-06T04:33:01.923Z`, input SHA-256 `8d55e6a701cd861db2bdeb1c1909ea2f7375850968a8872141d253af77121543`. Main: **3,192 passed, 69 skipped, zero failed**, 502,693 assertions across 238 files. Actual GL: **468 passed, one skipped, zero failed**, 9,445 assertions across 40 files. Strict typing, policy, snapshot integrity and live-tree matching all passed. This accepts the reviewed LibVars, nullable routing-time and signed-byte client-boundary additions together. See the [checkpoint report](../.artifacts/checks/2026-09-06T04-33-01.923Z.json) and `.artifacts/integration-full-check-9.log`.

The matching build at `04:41:25.968Z` used the exact same input; actual executable SHA-256 `63fe2eb513fd8cc55907d2bb07830e9399b903071036431a3f403bfaf31f4807`. All eight real dedicated UDP profiles passed again: raw/compiled, both products, quit/SIGTERM. Each checked movement, fast/full maps, two gamestates, 14 or 15 valid snapshots, two distinct final disconnect datagrams, configuration/logs, exit zero and port reuse, with SDL deliberately unavailable. All eight CPU/GL retail viewer probes passed and reproduce the established four image hashes. Evidence uses `.artifacts/checkpoint9-dedicated-*`, `.artifacts/checkpoint9-<map>-<renderer>-<raw-or-compiled>` and `.artifacts/integration-build-9.log`. These are dedicated-command and viewer checks, not normal game matches. Staged input, loader, prediction and entity-history work is excluded, as are later documentation edits.

### Checkpoint eight

The earlier checkpoint started at `2026-09-06T03:52:42.396Z`, input SHA-256 `04f9c1362b174b399f0b7597598ed34c4a96c88900ebaa10961bf10c9af32aa9`. Strict typing and policy passed. The main suite passed **3,143 tests**, with 65 skips, zero failures and 502,440 assertions across 236 files. The selected actual-GL rerun passed **468 tests**, with one skip, zero failures and 9,445 assertions across 40 files. Both snapshot integrity checks passed. See the [checkpoint report](../.artifacts/checks/2026-09-06T03-52-42.396Z.json) and `.artifacts/integration-full-check-8.log`.

The matching build at `04:00:23.150Z` used exactly that input. Its actual executable SHA-256 is `edadabdbd9b67c1a7dd24d6076bb91d0e6b72f832fbbd10bd64f423932d8ea11`. Root passed all **eight dedicated UDP cases**: raw/compiled, both products, quit/SIGTERM. Every case checked actual horizontal movement, fast/full map changes, two gamestates, 14 valid snapshots, two distinct shutdown datagrams, archived configuration, logs, exit zero and socket rebinding. Results use `.artifacts/checkpoint8-dedicated-<product>-<raw-or-compiled>-<quit-or-signal>.json`. Every process had an invalid SDL library path; no credentials were read.

All **eight retail viewer probes** also passed at 160 by 120 pixels over four frames. Raw and compiled CPU/GL pairs reproduce the four previously inspected image hashes below. Files use `.artifacts/checkpoint8-<map>-<renderer>-<raw-or-compiled>`. Independent compiled CLI review and root's identical rerun passed **eight tests and 70 assertions**, including EOF continuation and SIGTERM. Independent and root real PTY sessions both restarted the map and exited zero on Ctrl-C. Root's log is `.artifacts/dedicated-compiled-cli-independent-root.log`; PTY common/game logs remain under `/tmp/q3-compiled-pty-root-aPUBPV`. This accepts the dedicated-command slice, not initialized cgame, full matches, bots, pure mode, complete native terminal behavior or full M11. Later documentation and implementation edits are not retroactively covered by this snapshot.

### Earlier dated evidence

The previous full green checkpoint started at `2026-09-06T03:09:15.872Z`, input SHA-256 `493caa56de8e93fefdd9cee32a5e595636093f3785f319407321ee9e62ed2e74`. Strict typing and policy passed. The main suite passed 3,132 tests with 65 skips, zero failures and 502,338 assertions across 234 files. Its selected actual-GL rerun passed 468 tests with one skip, zero failures and 9,445 assertions across 40 files. Selected GL tests repeat coverage rather than add unique feature completion. Both snapshot-integrity checks passed. The [checkpoint report](../.artifacts/checks/2026-09-06T03-09-15.872Z.json) and `.artifacts/integration-full-check-7.log` retain the evidence.

The build at `03:17:55.353Z` used exactly that input and produced binary SHA-256 `8220ac0b95ed5f181dbdc0ded683a6210a18989f76ff398bb25815d4d6b6fa9b`. All eight retail viewer probes passed at 160 by 120 pixels over four frames. Each raw/compiled pair reproduced the four previously inspected checkpoint image hashes listed below. Logs and images use `.artifacts/checkpoint7-<map>-<renderer>-<raw-or-compiled>` names. This checkpoint accepts the renderer caller/tool migration and callable host, but predates the new dedicated CLI and verifier corrections. Later documentation and implementation edits need their own checkpoint.

The new raw dedicated command passes root's final 52-test, 896-assertion combined CLI/verifier/client/common/host rerun, including the original independent negative peer. Root also ran all four actual external UDP cases: both products, each with quit and SIGTERM. Each returned two gamestate generations, 14 valid snapshots, distinct final datagrams 15 and 16, unique disconnect sequences, exit zero and successful port rebinding. Actual horizontal usercmd movement, fast/full map changes, archived config and logs were checked. Every child deliberately received an invalid SDL library path. Latest results are `.artifacts/dedicated-final-raw-<product>-<quit-or-signal>-root.json`; these are protocol-client tests, not initialized cgame or full matches. Independent CLI review passed seven additional process tests and real PTY checks. Independent verifier failure-path review is accepted. The original continuing-NOP peer now reaches its initial-gamestate deadline at about 15 seconds instead of failing only at its 17-second exit. A permanent real-UDP regression also checks child cleanup and socket rebinding. Immutable deadlines are shared through nested semantic-phase loops and checked before consuming already-queued packets. The final focused log is `.artifacts/dedicated-combined-retail-final-root.log`; the later compiled checkpoint is recorded above.

The remaining paragraphs retain earlier dated evidence and corrections; they do not supersede the latest checkpoint above.

The earlier full check at `2026-09-06T02:45:35.425Z` failed and is superseded by the green checkpoint above. Its input SHA-256 was `9d2f315f2e66e0afc67dfd7a36c694f68dacd474829cc6c6516ff59e1d16b346`. Strict typing and policy passed. The main suite passed 3,132 tests with 65 skips, zero failures and 502,337 assertions across 234 files. The selected GL rerun passed 467 tests with one skip and one failure. The sole failure was the retail MD3 test exceeding its five-second timeout at about fourteen seconds, without a reported pixel assertion failure. Both snapshot integrity checks passed. See the [failed checkpoint report](../.artifacts/checks/2026-09-06T02-45-35.425Z.json) and `.artifacts/integration-full-check-6.log`.

The unchanged cinematic-then-model sequence reproduced an 8.8-second timeout in independent review. Its cinematic test observer retained hundreds of frames of geometry and upload payloads. A test-only correction retains current-submit bindings and one latest upload, with no production changes, forced collection, timeout increases or relaxed pixel gates. Independent ordered verification passed nine tests and 1,884 assertions, with peak memory about 1.20 GiB. Root's same ordered rerun also passed nine tests and 1,884 assertions; peak memory was about 1.09 GiB and the MD3 test took 336 milliseconds. Earlier instrumented runs observed 5.5 to 6.2 GB retained. The excess retention is removed; the exact mechanism of the original long pause remains unproven. The corrected tree subsequently passed the full check and matching build recorded above.

The previous full green checkpoint started at `2026-09-06T00:48:33.457Z` and checked input SHA-256 `8db40505697f62a9f0c3ad2e6a2ba02eb27a62d1be692392504d3eac32cf584c`.

- Main suite: 2,847 passed, 53 skipped, zero failed, 498,061 assertions across 217 files.
- Selected OpenGL rerun: 422 passed, one skipped, zero failed, 125,092 assertions across 37 files. These repeat selected tests and are not additional unique coverage.
- Strict type checking and the TypeScript-only policy passed.
- The recorded snapshot stayed unchanged and matched the live tree at completion.
- The corresponding Bun build at `00:54:54.820Z` used the same input snapshot and had binary SHA-256 `e42035e1042a002d00ed6f85605e1d198333fefd153ae460b2a1cdc80d30b952`.

The [checkpoint report](../.artifacts/checks/2026-09-06T00-48-33.457Z.json) identifies its inputs. Logs are `.artifacts/integration-full-check-5.log` and `.artifacts/integration-build-5.log`. Later edits, including this status update, are not covered by that check or executable. Build output can be replaced by subsequent builds. Focused tests do not replace a new full checkpoint.

All eight raw/compiled retail viewer runs passed at 160 by 120 pixels over four frames. Each raw/compiled pair produced identical PNG hashes: q3dm1 CPU `9e8514c28932878c3a7533c6701e362eedf294c2ecaef3b2e23407b4b3a04015`, q3dm1 GL `12a97eb55a6ca7c59f71280e7e0ee56d2a283924360cdffdc1f95e15dac5b444`, mpteam1 CPU `d8dd46354ab16614c5ed531cf1080fe09a02cc2e8ad4536b73f3fd10ac91f9e9`, mpteam1 GL `4ab10d89ad9e7b814802d80071894ff76967ac2a1927d3045b7c5a808d468b3b`. Images were visually inspected. These are viewer probes, not normal matches.

The viewer outputs and logs use `.artifacts/checkpoint5-<map>-<renderer>-<raw-or-compiled>` names. All eight runs exited zero before this report was written.

The current tree is not covered by that checkpoint. At approximately `01:49 UTC`, typecheck reported 959 diagnostics, 941 in tests and 18 in tools, none in `src`. The renderer caller migration subsequently reached a clean whole-tree typecheck; policy checking also passed. A later new dedicated verifier introduced one unused type-only import, which root removed. These are intermediate checks, not a new full checkpoint. Source-supported expectation disputes were reviewed before changing tests; pixel tolerances were not loosened.

Four raw retail viewer runs through the new target reproduce the four checkpoint image hashes. Root also reran real GameRuntime-to-client-level CPU/GL tests for baseq3, empty-name baseq3 loading and missionpack through the shared target. Together with built-in image journal and new terminal-failure tests, seven tests and 266 assertions passed. Mean channel errors were about 0.063 and 0.073, with maximum errors 10 and 11 respectively, within unchanged limits of 0.2 and 14. These remain controlled client frames, not normal matches. The later `03:17 UTC` executable covers this migration; this paragraph records its earlier focused evidence.

Independent client review reproduced stale queued drawing after failed initialization or HUD production. Root confirmed the failing regressions, fixed the terminal renderer-target boundary, and reran the passing tests. Normal level close and stale-level rejection still preserve the engine-owned queue. Full engine error/disconnect/audio teardown remains a separate integration gap.

Independent tool review found and confirmed fixes for four regressions: a clean cinematic request was mislabeled as an actual upload, later image metadata could rewrite an earlier frame's fingerprint, GL timing omitted synchronizing readback, and analytic CPU benchmarks unnecessarily required retail data. Final review also fixed index-count aliasing and clarified instrumentation overhead. Root's permanent two-suite rerun passed six tests and 25 assertions; the independent expanded run passed ten tests and 40 assertions. The replacement checksum describes immutable successful backend-request inputs, not effective texture storage or retained state. CPU reports separate backend-call timing from instrumented full-pass totals, which are not directly comparable to the older uninstrumented totals. GL reports summed interleaved backend-call time plus synchronized readback, not GPU-only time.

The preceding `00:41:08.164Z` check failed two tests. One expected invalid cvar text to be rejected after VM publication rather than at the engine boundary. The other compared a newly evaluated texture-binding wrapper by identity instead of the canonical image and full batch data. Test corrections preserve rejection atomicity, image identity, geometry, UV, color and sampling assertions. The frozen rerun above passed. Earlier failed and superseded reports remain available, including the `22:45` fixture failures and `22:55` workspace-mismatch report. New implementation requires another combined check.

Root independently reran 12 current server suites: 168 passed, zero failed, 3,920 assertions. The command and results are in `.artifacts/status-server-tests.log`. Coverage includes server state, cvars, configstrings, snapshots, lifecycle, commands, downloads, pure checks and frame scheduling. This is not production-hosted match acceptance.

The [retail asset audit](../.artifacts/retail-owned-audit.json) passed 5,733 assets with zero failures: 57 BSP, 515 MD3, 2,224 TGA, 1,132 JPEG, 1,700 WAV, 56 AAS and 49 RoQ. This measures decoding for the merged installed corpus, not all runtime behavior.

At 11:56:47 UTC, root refreshed the source ledger: **143 of 283 core-runtime files have TypeScript mappings, 50.5%**, with 153 mapped files across all categories and 182 distinct existing target paths. All mapped source files remain `in-progress`; none is claimed complete. This measures partial file accounting, not implemented functions, behavior coverage or the overall engineering estimate. It includes the four newly integrated files but not later private queue/reliable work. Evidence: `.artifacts/checkpoint17-source-ledger-count-root.json`; reproduce with `bun /tmp/quake3-check17-root-ENWHV4/ledger-status.ts`.

At 12:31:01 UTC, the partial client connection header mapping raises that count to **144 of 283 core files, 50.9%**, with 154 mapped files across all categories. All 182 distinct target paths exist. Every mapped source file remains `in-progress`. The overall estimate and zero closed milestone gates are unchanged. Evidence: `.artifacts/checkpoint18-source-ledger-count-root.json`.

The 12:53 UTC Unix ownership update keeps those mapped-source counts unchanged and adds one target, for 183 existing target paths. This is an ownership extraction, not another completed source function. Evidence: `.artifacts/checkpoint19-source-ledger-count-root.json`.

At `14:21:10.498Z`, root's checkpoint-twenty ledger refresh still reports **144 of 283 core-runtime files mapped, 50.9%**, 154 mapped files across all categories and 183 existing target paths. Every mapped source remains `in-progress`. The new AAS trace evidence updates an existing source record, and the staging tool does not count as ported source behavior. Evidence: `.artifacts/checkpoint20-source-ledger-count-root.json`.

At `15:49:18.044Z`, the three new menu mappings raise the ledger to **147 of 283 core-runtime files, 51.9%**, with 157 mapped files across all categories and 186 existing target paths. All mapped sources remain `in-progress`; none is claimed complete. Evidence: `.artifacts/checkpoint21-source-ledger-count-root.json`. Root also inspected all three 640-by-480 CPU menu captures in `.artifacts/checkpoint21-menu-*-cpu.png`, recorded by `.artifacts/checkpoint21-menu-captures-root.jsonl`. These are direct menu fixtures without a normal product controller or a gameplay background, not playable application screenshots.

At `16:49:53.351Z`, Specify Server, Cinematics and its partial `UI_CanShowTierVideo` source mapping raise the ledger to **150 of 283 core-runtime files, 53.0%**. There are 160 mapped sources across all categories and 188 existing target paths. Every mapping remains `in-progress`. The game-info mapping covers only that progression query, not the full source file. This accounting does not resolve the newly observed Specify Server visual defect or increase the overall engineering estimate. Evidence: `.artifacts/checkpoint22-source-ledger-count-root.json`.

At `17:31:32.305Z`, the getter adds a partial `cl_ui.c` mapping: **151 of 283 core-runtime files, 53.4%**, 161 mapped sources across all categories and 188 existing target paths. Every mapping remains `in-progress`. This covers only the retained snapshot client-number read, not complete UI traps or the entire source file. Overall engineering completion and zero closed gates are unchanged. Evidence: `.artifacts/checkpoint22-integrated-source-ledger-count-root.json`.

At `18:33:11.746Z`, the two new menu mappings raise the ledger to **153 of 283 core-runtime files, 54.1%**, 163 mapped sources across all categories and 190 existing target paths. Every mapping remains `in-progress`. Save Config is compiled but source-disabled; neither mapping proves a product UI or complete source-file behavior. Overall remains 50% with roughly ten percentage points of uncertainty and zero closed milestone gates. Evidence: `.artifacts/checkpoint23-source-ledger-count-root.json`.

Newer focused evidence includes original-source loading comparisons with 74 checks, both empty-name and ordinary loading reentry, actual retail Team Arena HUD drawing, shared cinematic buffers and engine-owned audio positions. Collision patch counts match all 525 patches in q3dm1 and mpteam1 under the recorded GCC i386 O2 x87 profile. Its collision allocation sequences also match all 466 and 1,700 requests respectively. Compiler profiles produce different redundant plane counts; these are qualified comparisons, not universal native equality.

Hunk accounting reserves actual backing storage for implemented source allocations. Complete memory pressure remains unavailable because renderer initialization, shader/image registration ordering and final stitched grids are missing. The full-budget query rejects those incomplete profiles rather than reporting an invented remaining-memory value.

## Historical next work

The following checkpoint-era blockers are superseded by the current summary and [worklist](WORKLIST.md). They are retained as history, not the active queue.

Checkpoint twenty-two and its matching executable remain the latest accepted whole-project result. Team Orders and Save Config are now integrated after independent review and root reproduction; checkpoint twenty-three and its matching build/runtime acceptance remain pending. Finish the new helper's isolated failure-path review/reproduction before running that checkpoint. The next source unit is exactly `UI_SetLerpFrameAnimation` and `UI_RunLerpFrame`, approved in private stage `/tmp/quake3-port-stage-VieskB`. It implements source-defined timing over caller-owned records, not a loaded preview or complete Controls. Root read its full contract, checked decisive source/error paths and verified all 29 input hashes. Evidence: `/tmp/quake3-ui-lerp-contract-FClJPgOk/CONTRACT.md`.

Controls' contract is complete but implementation remains blocked by the missing source-owned base UI player preview and its mandatory shared animation-file handles. Root read the contract and verified the decisive original call chain. No cgame animation substitute, previewless menu or UI-private filesystem is approved. Evidence: `/tmp/quake3-controls-contract-9f39Nv/CONTRACT.md`.

Remove Bots remains unapproved. The source rereads current client configstrings during later scrolling; `CL_Disconnect(false)` can leave the UI alive while clearing the client, and local map loading can clear configstrings without replacing the connection. No production graphical client provider currently owns those transitions. Root read the independent caller-first report and verified its 33 input hashes; a test-populated session holder would not resolve that dependency. Evidence: `/tmp/quake3-current-client-contract-nBUMhrbs/CONTRACT.md`. The Save Config/Remove Bots source contracts are separately recorded in `/tmp/quake3-save-remove-contract-wXyi09/CONTRACT.md`.

Root read the SDL and genuine UI controller designs and verified their 55- and 66-input hash inventories before this documentation update. The designs reuse the existing Unix event queues, client modules and UI framework. They require actual UI initialization and client ownership, not viewer relabeling or fabricated services. Their critical path starts with the unresolved shared filesystem dependency shown above. Independent menu work does not remove that dependency. Evidence: `/tmp/quake3-sdl-delivery-design-UvHvCL/CONTRACT.md`, `/tmp/quake3-base-ui-controller-design-5tczJ7/CONTRACT.md`, `.artifacts/sdl-delivery-design-inputs-root.log` and `.artifacts/base-ui-controller-design-inputs-root.log`.

The canonical staging CLI is required for new private stages. Display Options remains deferred until genuine SDL gamma and UI configuration ownership exist. A standalone sticky-session teardown bypass remains deferred. Refused BSP-entity and inflater audit operations remain stopped and unaccepted. None of these designs supplies a complete human or bot match.

The following dated paragraphs retain earlier integration history.

After checkpoint eight, root integrated independently reviewed `BotLibVars` and the same-owner nullable routing-time API with exact reviewed hashes. The root adjacent/native rerun passed 225 tests and 4,906 assertions across eight files, with zero failures; whole-project typing and policy passed. Both native routing profiles also passed an independent 1,635-query sweep rerun by root. LibVars matched 23,029 native numeric words and 15 lifecycle rows, with sanitizer evidence for its two explicit undefined-source rejections. The client-session signed-byte boundary was corrected to accept the source's legitimate -128 movement; both-product wire regressions failed before the fix and passed afterward. Independent review accepted it; root's four adjacent suites passed 94 tests and 1,585 assertions with two optional-QVM skips. Checkpoint nine and its matching executable now cover these three accepted units. Logs: `.artifacts/libvars-routing-integrated-native-root.log`, `.artifacts/libvars-native-root.log`, `.artifacts/client-input-signed-byte-green.log` and `.artifacts/game-ai-selected-design-root.log`.

Historically, the first client-input candidate was frozen with 19 author tests and a failed review worker; that was not accepted implementation. The corrected integration and current native evidence are recorded above. An inherited extreme-angle conversion issue in the canonical client session was separately corrected and independently accepted as a bounded component. Root reran 96 adjacent tests with 1,683 assertions and two optional-QVM skips, plus an independent 4,623-input native sweep with 44,786 assertions. The correction preserves defined signed conversion endpoints and rejects undefined conversions before command publication. Logs use `.artifacts/client-angle-boundary-*`; checkpoint nine and its executable predate that correction, and later accepted checkpoints include it.

At 05:19 UTC, AAS entity history and route prediction passed independent source review and root's reruns; their exact reviewed files are now integrated. Root's broader entity/spatial run passed 37 tests and 91,444 assertions, including all enabled retail-map tests. The independent prediction sweep passed 12,960 generated comparisons per native O0/O2 profile, 13,251 assertions each. The combined ten-suite native run passed 240 tests and 23,017 assertions. Logs use `.artifacts/aas-entity-*` and `.artifacts/predict-route-*`. The next full checkpoint and matching executable remain pending.

At 05:35 UTC, the corrected synchronous parser/five-loader migration and typed command fallback are integrated after independent component review. The original loader review found four reentry defects despite its green 224-test gate; root reproduced them before correction. Post-read identity, capacity and free-slot checks now pass the original causal probes and 34 permanent reentry tests. The corrected loader's independent run passed 258 tests and 17,324 assertions. Command fallback's independent run passed 133 tests and 1,061 assertions. Root verified exact file hashes and the combined shared-test edit, then passed 413 tests and 19,149 assertions across 24 loader/command/client/server suites, with zero failures and clean whole-project typing/policy. Evidence: `.artifacts/loaders-commands-integrated-root.log`, `.artifacts/sync-loader-reentry-independent-green-root.log` and `/tmp/q3-sync-loader-review-ZKLaBi/confirmation.md`. Independent review of the combined interaction and the next full checkpoint remain pending; checkpoint nine's executable does not contain these changes.

The immediate dependency-ordered work is:

1. Preserve checkpoint fifteen's accepted source-error, quit, initialization/shared-clock and terminal resource-disposal behavior while composing the actual client/listen owners (M2, M3, M11). The common correction and matching executable are no longer pending.
2. Use the integrated shared filesystem and COM_ParseExt to implement genuine arena/bot loading and progression, then remaining product UI dependencies and controllers. The retained-reader qualifications remain explicit M2 parity work (M2 -> M17).
3. Wire SDL delivery to the accepted input assembler and implement production send/connection/listen ownership, engine screen composition and audio lifecycle with genuine UI entrypoints. Canonical keys, command assembly, gamestate barriers, server-time ownership and console drawing already pass accepted checkpoints. A console-only diagnostic cannot impersonate a loaded product UI (M3, M10, M11, M15, M16, M17).
4. Prove a normal human-playable match with movement, combat, match transitions, restart and reconnect; repeat for both products and backends (first M18 vertical slice, not full M18 closure).
5. In parallel, finish AAS/botlib lifecycle and game-AI service composition, then verify complete bot matches. Alternative-route goals are already integrated. The stopped BSP-entity and inflater audit paths remain unaccepted; do not retry their refused operations (M2, M13 -> M14 -> M18).
6. Complete product menus/progression, server browsing, demos, downloads and live pure-pack enforcement (M2, M10, M17, M18).
7. Close remaining renderer/gameplay parity, performance, resilience, source accounting and raw/compiled release gates (M19).

The combined loader/command interaction review subsequently passed; root reran its two causal tests and all four loader reentry probes. Checkpoint ten and its matching executable cover that integration. The later keys revision also passed independent review and is now integrated with exact nine-file hashes. It preserves the independent chat team/player words and removes the pure keys unit's dependency on unaccepted input. Root reran 50 staged tests with 575 assertions and 20,224 native comparison rows across 35 cases. The live adjacent run passed 166 tests and 1,341 assertions across nine files, with clean whole-project typing and policy. Separate deferred input compatibility passed two tests and 18 assertions; this does not accept the input implementation. Evidence uses `.artifacts/client-keys-*`. Checkpoint eleven and its matching executable now cover this keys/fields integration.

Checkpoint eleven now covers the keys integration described above. The saved independent delta verdict is `/tmp/quake3-client-keys-revision-PrAo1h/INDEPENDENT-REVIEW.md`, SHA-256 `8e239369f2ed62093ec04d9263a8c4a67b0effa460dbfd2c9eea8406cbce06fd`. This records the already completed review, not a new run. It resolves the missing on-disk review citation identified by the separate trail reviewer.

Client timing and console/text drawing are now integrated with the focused live evidence above. Independent clock review and root's staged run each passed 195 tests and 2,404 assertions. Review found a test-only regression that serialized previously concurrent admissions; the integrated correction restores that concurrency. Root also rebuilt and reran the unchanged-source clock/public packet comparison: four tests and 17,921 assertions, including 960 public packet-driven scenarios. The nested-loading source probe passed 182 assertions for both products. Console review caught caller-owned rectangle values being reread after shader registration; the integrated correction captures the four source float arguments before the await and passes the causal CPU regression. These units do not implement the full client host, engine screen frame, or arbitrary malformed-demo behavior. Independent evidence: `/tmp/quake3-client-clock-independent-review.md` and `/tmp/quake3-console-capture-confirmation.md`.

Alternative routing and bot logging are integrated and covered by checkpoint twelve-b and later checks. The corrected base UI foundation, Team Arena cvars and ordered PK3 metadata are included in thirteen; the shared pulse, VM integer words and lexer correction are included in fourteen. The common driver and corrected source-error, initialization and managed-disposal behavior are included in fifteen. Neither product has a complete menu controller. Genuine UI filesystem, client pure/CD capabilities, game bot decisions and service composition remain prerequisites. These components do not supply a normal client loop or playable match. The selected bot ownership design is recorded in [GAME_AI_DESIGN.md](GAME_AI_DESIGN.md).

Original-source image/cinematic checks pass 21 cases and 50 checks per product. A separate unchanged-source fog trace passes 54 recorder and 82 genuine GL checks. It proves that the original fog border call modifies texture object zero, not the named fog image. Both new backends implement that behavior with focused native-GL tests; complete renderer parity remains open.

Pure-pack metadata, server validation, downloads, admission and frame scheduling have bounded implementations. Client download installation, live pure filesystem enforcement, production client/listen ownership and complete engine configuration remain open. Test credentials stay outside the workspace; known credential filenames are ignored and rejected by build/check snapshot input collection before contents are read. No actual key was read or imported.

Complete server hosting, client frame composition, product UI and game bot AI before claiming full matches or closing integration gates.

## Forward planning

There is no evidence-backed completion date. Previous overall percentages, checkpoint-start intervals and task-size allowances did not measure full-port throughput and are not current forecasts.

Both product controllers and the graphical/listen owners are integrated. Bounded ordinary menu-to-match, intermission and next-map runs pass on all four raw product/backend combinations. Sector ownership and image-grid registration are accepted. The active bounded queue is the splash-damage rounding correction, server/common commands, signed-demo ownership and sound-loading warnings. Full competitive/objective matches, complete bot integration and remaining workflow, parity and release requirements remain open.

Bot integration, remaining renderer behavior, pure/download/demo workflows and final release qualification remain separate graph obligations. Do not infer completion from elapsed time, agent count, files added or tests passed. Re-estimate from actual completed application flows once they exist.

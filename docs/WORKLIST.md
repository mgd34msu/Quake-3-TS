# Quake 3 TypeScript port worklist

Current work, September 10: the dependency graph is complete. Newly discovered functionality and corrections are tracked in [PARITY_PUNCH_LIST.md](PARITY_PUNCH_LIST.md), separately from the completed graph. Prior acceptance covered declared profiles, not unrestricted parity. All 941 source files and 56 build scripts have examination accounts; source-account dispositions are not proof that every behavior is correct.

The September 10 21:47 UTC executable is published to dist and qfiles/q3a, with byte equality verified. Its four compiled gameplay/recovery runs pass after reached serial nearest-mip certification and one dlight UV-only capture call. Matched raw CPU playback gains are 4.0–5.1% for Quake 3 and 3.3% for Team Arena; fixed-scene color/depth remain exact. The earlier eight-case raw/compiled matrix remains accepted evidence. Latest compiled timing still belongs to 20:39: CPU/GL medians 9.8/39.7 for Quake 3 and 9.1/30.1 for Team Arena. CPU remains too slow. The reached public mip-cache candidate was rejected after a noise-sized gain; source and tests again match the published build. Rejected changes remain removed and recorded in RECOVERY.md. See RELEASE.md and STATUS.md; older entries below are historical.

Current checkpoint, September 8 19:14 UTC: the 19:13 executable adds retained BSP visibility, world/PVS controls, live lighting and rail settings, and source SF_ENTITY execution to the earlier command/draw-surface work. Root's combined CPU checks and independent review passed. No devices or SDL; see STATUS.md and RECOVERY.md. This is a bounded implementation checkpoint, not full-port completion.

- [x] Ground: inventory original sources, retail assets, runtime, and subsystem flow.
- [x] Sketch: compare independent architectures and derive typed contracts.
- [x] Agree: synthesize the architecture and publish the port plan and dependency map. User authorized proceeding without a checkpoint.
- [x] Implement: complete the dependency-ordered milestones under their recorded profiles.
- [ ] Complete the separate post-plan parity punch list, including newly discovered source functionality and integration corrections.
- [x] Scrap if needed: replace the rejected per-triangle worker strategy with reviewed draw batching; retain strict types and explicit ownership.
- [ ] Optimize actual client FPS without changing source behavior or lowering rendering quality.

Design comparison phases complete: frame, fan out, cross-judge, pick, graft, verify. Independent judge gpt-5.6-terra/high scored source-aligned 19/20 and data-oriented 18/20. Adopted snapshot ownership and packed render batches; rejected mandatory packed gameplay and weak optional-plane trace results.

Completion requires the full base game and Team Arena, CPU and OpenGL rendering through SDL2, Bun source execution and executables, and evidence of parity. Partial milestones do not establish completion.

## Current dependency queue

- [x] Connect M2/M13 routing graph/cache/head/link/crossing allocation and M14 embedded AI-record joins. Actual both-product match completion and map replacement pass, including Team Arena CTF capture.
- [x] Replace M2 retained PK3 whole-entry decompression with the bundled DEFLATE behavior in TypeScript and bounded incremental input/output; both actual dedicated products consume it.
- [x] Connect actual shared-zone console/packet/journal payloads and source-backed lexer text/cursors/punctuation tables through bot readers and map lifetimes.
- [x] Implement M2 preprocessor source/hash/conditional storage and reached reader-error cleanup.
- [x] Replace M17 direct menu eager preprocessing with incremental reads, reached publication/recovery and the actual shared script-handle owner. Device-based UI acceptance remains open.
- [x] Complete M2 heap token/define storage, retained lexer/VM token publication and recursive-include behavior within the documented numeric profiles.
- [x] Complete M4/M7 source shader lookup, MD3/MD4 registration ordering and mip-by-mip image uploads.
- [x] Connect M7/M8/M9 renderer counters and their actual frontend/backend producers, image-frame accounting and overdraw. Actual GL overdraw readback remains unexecuted.
- [x] Replace eager production BSP parsing and batched collision/renderer reservations with source-ordered loaders and actual allocated records (M4 → M5/M7). Bounded loading/query units are integrated; remaining representation qualifications do not close the full milestones.
- [x] Connect M3/M7/M15 stereo frame sequencing and CPU/GL draw-buffer commands. Hardware stereo and device acceptance remain open.
- [x] Connect M7 allocated backend entity/poly/light records to retained animation and M8/M9/M15/M17 consumers, including source non-threaded SMP initialization. Root's combined actual CPU client-level checks and both compiled dedicated products pass; hardware and native representation qualifications remain.
- [x] Connect M7 bounded command and draw-surface storage, source sorting/shader fixups and backend skipping to actual M8/M9/M15/M17 execution. Root's combined208 checks, independent production review, typing/policy and both compiled dedicated products pass. Documented typed-pointer and diagnostic-producer qualifications remain.
- [x] Connect M7 retained source BSP visibility marking and actual world/PVS controls to traversal, preserving area-mask, cluster-cache, portal and modified-cvar behavior. Root's combined CPU selection passed 93 cases; independent review identified a positive leaf-index alias, which is corrected and exercised through actual CPU frames.
- [x] Replace hardcoded entity and point-light scales with live source r_ambientScale/r_directedScale values.
- [x] Connect source r_railWidth/r_railCoreWidth/r_railSegmentLength to reached rail geometry, without frontend sampling before actual SF_ENTITY execution. Removed the unused eager batch planner and its helper-only tests. Root's combined 199-case CPU selection and separate actual retail portal/overflow case passed; independent source review was clear within documented profiles.
- [x] Implement the M4 → M18 QVM decoder/interpreter prerequisite: source memory/byte-PC behavior, all opcode families, recursive asynchronous syscall ownership and data restart. Root's 39 focused cases and both independent reviews pass. External-mod adapters/loading remain open; retail remains TypeScript and the 05:44 executable is unchanged.
- [x] Join common, filesystem, renderer-resource/2D, sound, client-state and key traps to actual owners. Root's combined device-free selection passes 834 cases; documented source qualifications remain open.
- [x] Implement cinematic traps over the engine's sixteen slots and CPU drawing. Independent review is clear within the retained-header reset qualification.
- [x] Complete collision, font and UI browser VM adapter units, with their actual owner corrections and independent reviews. Documented profiles remain qualified.
- [x] Share frame polygon/light storage across actual cgame, HUD and menu callers; independent review and both-product CPU client-level checks pass.
- [x] Implement reached sound loading for nonresident registered slots through the actual bank and VM playback calls.
- [x] Connect numeric scene VM calls to shared allocation and defer resource lookup until actual renderer consumption. Retained source pose and autosprite axis selection passed independent review.
- [x] Finish joined review of world-loading, entity-token, and visibility VM calls. Collision PVS storage retains the source allocation and loader-owned header values.
- [x] Finish parser review corrections and shared-handle VM integration. Incremental reads, shared globals, clean EOF writes, and all three role dispatches share the actual source owner.
- [x] Implement and review bot action, character, weapon, and UI key adapters.
- [x] Connect external UI loading, asynchronous entrypoints, recursive calls, and shutdown to EngineClient. Memory and calendar qualifications remain open.
- [x] Finish goal, movement, chat, AAS, and library lifecycle adapters and their component reviews.
- [x] Connect external cgame lifecycle and actual cleared client-active state; independent joined review is clear.
- [x] Implement shared server game data, raw entity pointers, suspendable commands/frames/snapshots, bot callbacks and external GAME loader/dispatch.
- [x] Finish authored-VM server integration and independent review of the combined GAME connection. Actual admission, snapshots, fast restart and reconnect pass; broader external-mod compatibility remains open.
- [x] Finish the bounded M7 lazy shader/view/stage execution and M8/M9 source state ordering unit, including whole-array shader publication, generic iterator entry and independent combined review. Actual both-product CPU client-level checks pass; no new GL device acceptance is claimed.
- [ ] Improve actual M8 retail frame time. The current 20:39 compiled timedemo medians are 9.8 FPS for Quake 3 and 9.1 FPS for Team Arena at 640×480. CPU optimization remains open; earlier render-pass timings are not whole-game FPS.
- [x] Implement M4 four-component CMYK/YCCK JPEG decoding through the existing sequential and progressive decoder, preserving Quake's color conversion and alpha behavior. Thirty focused cases pass; existing standalone RGB/grayscale compatibility qualifications remain.
- [x] Complete bounded M5 transformed/capsule trace and M6 movement source comparisons and fix demonstrated gaps. Independent review is clear; root passed 92 cases and 807 assertions. This does not close either full milestone.
- [x] Finish the bounded M10 message-boundary/download receive corrections and connect live packet/delta diagnostics to M15 output. Block-zero size now publishes before payload decoding, with live callback state and lifetime checks; independent review is clear.
- [x] Replace M15 detached parse-entity storage with the retained 2,048-cell ring and stable baselines used by actual decoder and snapshot callers. Joined protocol/client and both-product CPU client-level execution pass; independent review corrected live diagnostic callback context.
- [x] Verify the M14 bot delta-angle conversion through the joined runtime; the updated executable runs source bots in base combat and Team Arena CTF.
- [x] Verify the assembled source and 00:14 executable after this integration. External UI/cgame/GAME loading is connected; broader mod interoperability remains open. Retail gameplay remains direct TypeScript.

The dated executable entries below are historical; current source and executable state are at the top of STATUS.md and RECOVERY.md.

The 00:32 executable adds accepted sector ownership and `sectorlist`. Root's 378-case combined selection and both raw/compiled product/backend runs pass real clear/repeated-clear/replacement behavior. Evidence and previous binary: `/tmp/quake3-sector-build-13lK0E/`. Image-grid frontend, CPU, GL and existing-caller workers have separate files; root joins them privately before acceptance.

The 00:10 September 8 executable is accepted with server info/userinfo and dedicated-console chat. All five reviewed paths are integrated. Root's 206-case combined selection, both raw graphical runs, both raw dedicated runs, both compiled graphical runs and both compiled dedicated runs pass. Actual chat and kick disconnect travel over localhost UDP. Evidence and the previous 23:47 executable: `/tmp/quake3-server-info-build-pbTsRO/`; dedicated runs: `/tmp/q3-server-console-live-oNPIvW/`. The selected engine-owned sector design is in implementation; `r_showImages` has an independent source-contract owner.

The matching 23:47 executable is accepted. Both compiled products pass debug controls, host-protected kick commands, mode changes, renderer commands, restart, captures, movement/fire, pure admission and 20-snapshot recordings. Evidence and the preceding executable: `/tmp/quake3-debug-build-GdcM5d/`.

Latest accepted source, 23:43 UTC: source triangle/normal drawing and debug-sort cutoff are integrated with real server kick commands. Root's combined selection passes 314 cases, and all four raw product/backend runs pass actual debug controls, movement/fire, audio, pure admission and restart. Independent reviews are clear. The accepted 23:47 executable includes this source. Evidence: `/tmp/quake3-port-stage-8jIGnt/` and `/tmp/quake3-port-stage-nF5mVg/`. The server info/userinfo/console-chat unit is proceeding independently through existing common and lifecycle owners.

Latest accepted production changes, 21:48 UTC: all six renderer commands are integrated through their actual owners after component and final wiring reviews. Root's combined branch passes 91 cases and its final mode-list successor passes 32. The 21:51 executable passes compiled base CPU and Team Arena GL commands during active play and after restart, captures, movement/fire, pure admission, 19-snapshot recording and clean shutdown. Evidence: `/tmp/quake3-port-stage-nrTdN9/` and `/tmp/quake3-renderer-commands-build-Kpu4FM/`. All eleven recorded stale renderer fixture failures are resolved. Ordinary raw CPU UDP reconnect passes for both products. The nearest-word optimization was rejected. The later source primitive-selection join is integrated and included in the accepted 22:49 executable, which passes actual compiled retail CPU/GL play. Previously refused branches remain stopped.

As of September 7, 2026, 16:30 UTC. Both product controllers, the graphical client and listen server are joined. Actual human clients reach FFA kills/respawn and CTF, One Flag and Harvester objective/intermission flows on CPU and GL. These are bounded, source-assisted probes, not complete competitive matches. The accepted 16:08 executable includes MD4 skeletal/retained-state handling, shared renderer fonts, the menu-to-map repair, synchronous portal timing, BeginFrame error checking, channel diagnostics, single-player postgame/podium behavior and CPU/GL r_nobind. Both compiled products pass capture, movement/fire, pure validation, video restart and recording checks. Those sound, Upload32 and ordered-sky branches are now integrated in source; the later source checkpoint above supersedes this executable-era report. The private CPU sampling optimization was rejected. See the [status report](STATUS.md) for exact evidence and qualifications.

- [x] Integrate shared common-lived read/write handles, real retained PK3 cursors and source-ordered bounded listing.
- [x] Migrate common/server restart/disposal, script loading and inspection cleanup to those actual owners.
- [x] Independently review the combined 26-file batch and correct post-close bot-log acquisition. Root also corrected empty-entry CRC validation and restored the script allocation cap.
- [x] Run the existing full check and matching executable/runtime checks. Main 4,264 passed; selected actual GL 473 passed; eight UDP and eight retail viewer runs passed.
- [x] Review and integrate source-compatible COM_ParseExt. Root reproduced 17 tests and 151 assertions, including a live run.
- [x] Implement and independently review M17 base arena/bot loading and single-player progression through the real shared filesystem and source parser. Correct the short-read boundary at actual byte access and integrate the exact source. Root's live four-suite union passed 89 tests and 676 assertions, with strict typing and policy.
- [x] Implement, independently review and integrate single-player difficulty/start and Add Bots using the real catalog. Difficulty's recovered null arena now follows source empty-info behavior.
- [x] Implement and join real product UI dependencies and controllers, with actual Main/in-game/settings/SP/browser destinations and services. Complete live flows remain separate acceptance work below.
- [x] Review, reproduce and integrate the preserved VieskB UI interpolation pair. Its 44 tests pass. This does not by itself supply a preview or Controls.
- [x] Implement, independently review and integrate actual player previews, model/skin/weapon registration, source animation sequencing and CPU scene submission.
- [x] Follow the preview edge into Controls. Integrate the independently reviewed menu, real bindings and cvar writes; fix complete source reset before failed cache loading.
- [x] Implement and independently review Remove Bots and Demos against real protocol/configstring and common filesystem owners. These generate source commands; execution remains downstream.
- [x] Independently review and integrate Player Model/Player Settings and Start Server/Server Options/Bot Select. Correct the Player Model/Settings numeric conversions and missing bot-icon overflow diagnostics; reviewers confirmed both fixes.
- [x] Follow the completed menu dependencies into Level Select and Postgame. Independently review and integrate both, including Level Select's post-print retirement correction.
- [x] Preserve actual zero-allocated UI cvar cells and source registration/update order. Migrate both original callers to explicit registration.
- [x] Run the existing full check/build on the menu wave through Start Server: 4,421 main passes and 473 selected GL passes; eight UDP and eight viewer raw/compiled runs passed.
- [x] Implement and independently review actual SDL output ownership and source sound-buffer clearing, retaining the painted clock and background-track lifetime.
- [x] Connect the sound owner to shared server-relative file reads and mod discovery, then implement Mods. Independent reviews passed; root's live nine-suite union passed 122 tests and 1,209 assertions, followed by strict typing and policy.
- [x] Integrate settings and both actual product controllers. Physical input, device behavior and complete flow acceptance remain open.
- [x] Run the existing 21:40 full check/build on the menu/Mods/SDL wave: 4,510 main passes and 473 selected GL passes. Eight UDP and eight retail viewer runs pass with matching raw/compiled frame images.
- [x] Join actual SDL input delivery, client/listen ownership and screen/audio lifecycle through the base graphical entrypoint. Team Arena UI and whole-match acceptance remain open.
- [x] Independently review and integrate SDL input. Root reproduced 18 actual X11 tests and 97 assertions. Shifted punctuation/layout binding parity remains open; an integrated producer is not full platform parity.
- [x] Implement and independently review post-resolution client admission through real network owners, without fabricated cgame or authorization services.
- [x] Implement and independently review Connect Screen against actual client records and session configstrings. Correct empty-versus-missing configstring presence at the session owner after reproducing the failed CPU drawing case.
- [x] Integrate the source mixer's deferred-start and repaintable-timeline prerequisite and migrate all actual allocation-clock callers. Root's combined 64-file integration run passed 1,083 tests with strict typing and policy clean.
- [x] Connect outgoing client packets to actual channel delivery and source packet-rate gating. Both independently reviewed changes are integrated; full command-send and client ownership remain downstream.
- [x] Produce genuine renderer configuration and exclusive gamma ownership for Graphics/Display settings. The revised seven-file unit passed independent review and root's actual isolated CPU/GL/input run: 63 passed, one expected skip, 349 assertions.
- [x] Implement Graphics, Driver Info and Display menus using the actual retained UI configuration snapshot. Product-controller wiring and renderer restart execution remain downstream.
- [x] Implement real engine server-browser/ping ownership through Unix I/O. The reviewed four-file integration passed root's five-suite live run: 63 passed, 1,558 assertions including actual server getinfo replies.
- [x] Implement the dependent ArenaServers menu against that owner, sharing the actual Specify Server, Start Server and Confirm menus.
- [x] Implement common-lived local CD-key storage/validation and its base menu. Independent review and root's live six-suite run passed: 83 tests, 1,046 assertions, strict typing and policy. No credential files were accessed; persistence and public authorization remain separate.
- [x] Implement source sound-frame scheduling and actual SDL queue replacement. Reviewed eight-file unit integrated; defined signed paint ranges, actual queue replacement and lifetime handling verified. Hardware-buffered playback, starvation timing and native DMA-wrap behavior remain explicit qualifications.
- [x] Integrate Main with its actual banner, local key prompt, error recovery and Team Arena detection, plus Setup and In-game navigation.
- [x] Implement source userinfo updates, packet cvars/printing and CL_SendCmd for actual sessions. Command generation precedes packet throttling and continues during demo playback. Standalone cinematic active-state ownership remains open.
- [x] Connect the base menu graph through source UI initialization and entry points. Disconnected postgame/team-order client-active state and the Team Arena controller remain open.
- [x] Move cgame presentation to the engine screen so menus and console draw before the single SDL presentation. The engine-lived screen survives renderer replacement; stereo and full renderer begin/end behavior remain open.
- [x] Initialize sound and its commands over actual common, mixer, bank, music and SDL owners. Disabled-sound UI works; disabled-sound cgame and hardware timing qualifications remain open.
- [x] Implement actual demo recording files and the screen indicator through the connection owner. Normal command/disconnect orchestration follows in the client owner.
- [x] Remove the cinematic startup dependency on a pre-existing mixer; follow actual output replacement and discard disabled output at source raw-audio calls.
- [x] Implement source client cvar registration and independent console commands, including reliable forwarding, configstrings, model selection and real network rcon delivery.
- [x] Join CommonFrameDriver, SDL input, client startup/frame/connection ownership and the listen server in the graphical application. Raw CPU/GL runs and compiled CPU demo/GL map entry passed; complete matches remain open.
- [x] Implement and wire source server-status requests/replies through actual common clocks and Unix/loopback transports.
- [x] Port Team Arena game/team metadata, source UI string storage, arena/bot catalogs and Q3 player-model listing through actual files and renderer resources. Independent source review is clear; root focused checks and typing/policy pass.
- [x] Implement and independently review Team Arena cached-server persistence and saved-score loading/clearing through actual common handles. Root reproduced their focused suites.
- [x] Integrate metadata-backed selection, resource registration, mod/movie/demo lists and actual-session player lists. Root checks include retail assets, real protocol records and retained file bytes.
- [x] Preserve source font no-write outcomes in renderer registration, Team Arena resources and cgame HUD destinations. Independent review and root retail run: 81 passed, one GL-only skip, zero failures.
- [x] Integrate source-accounted shared menu allocation, postgame calculation and active-menu transitions. Correct NULL activation side effects and forced UI cvar writes; root joined run passed 94 tests with strict typing/policy.
- [x] Integrate retained menu loading and keyboard/mouse entrypoints, preserving the source character flag through non-text dispatch.
- [x] Integrate Team Arena browser sorting/filtering/refresh, status/search and connection/download drawing using actual engine services; preserve retained buffers and source ordering.
- [x] Integrate actual engine-slot cinematic traps, game/player/score visibility and source settings callbacks. Root joined run passed 97 tests; independent callback-integration review is clear. Menu reload preserves reached string reads, including empty and unnamed-only menu sets.
- [x] Integrate actual Team Arena feeders and separate-head/team-skin player previews after independent review; preserve the source preview sound channel and BSS shared selection words.
- [x] Implement and independently review source menu reload, refresh/FPS/cursor ordering, console commands and actual menu movie/sound caching. The final five-suite joined retail run passed 64 tests and 793 assertions; strict typing and policy passed.
- [x] Integrate Team Arena owner-key dispatch after independent source review; actual key/feeder checks passed 12 tests and 245 assertions.
- [x] Integrate Team Arena owner drawing and menu scripts after independent review; root joined drawing/script/key checks passed 19 tests and 373 assertions.
- [x] Implement and wire actual Team Arena product initialization, source cache-saving shutdown and separate cgame HUD ownership into EngineClient. Strict typing/policy and independent retail CPU menu/restart/closure checks passed.
- [x] Fix the renderer's source byte-color conversion exposed by a longer retail menu fade. Actual CPU/GL drawing checks pass; both renderers complete retail Team Arena menu input through active CTF map/HUD initialization and normal shutdown.
- [x] Exercise Team Arena Red team joining, alive movement and firing through actual client/server commands on CPU and OpenGL. Bots, opponents, objective scoring and physical SDL input remain outside these bounded runs.
- [x] Integrate projected model shadows through actual renderer model, lighting, shader and geometry owners after independent source and CPU/GL review. Stencil shadows remain separate.
- [x] Integrate patch subdivision, stitching/shared errors and actual world/inline-model distance LOD after separate algorithm and CPU/GL reviews. Preserve full geometry for marks/allocation; record the narrow source out-of-bounds midpoint repair.
- [x] Integrate actual unique file reads and retained music streams, source loop timing and current-filesystem access. Retained demo and RoQ consumers follow this dependency.
- [x] Integrate SDL layout-aware key delivery and the measured, byte-identical CPU bilinear word-load optimization. Physical gameplay and broader performance remain open.
- [x] Integrate independently reviewed filesystem pure/config restart and client download/authorization/retained-demo owners through actual production callers. Root verified 33 exact postimages, typing, 107 focused and 47 retail-enabled cases; stock pure module provenance remains open.
- [x] Correct final music retirement after a real joined Team Arena run failed disposal. Source stop/shutdown behavior is unchanged; root's repeated CPU and GL map/input runs now exit cleanly.
- [x] Integrate retained RoQ streaming and all actual constructor callers after independent review. Real cinematic startup and normal disposal pass; source decoder and PK3 timing qualifications remain open.
- [x] Integrate world portal/mirror view scheduling and CPU/GL clip-plane execution after separate frontend/backend reviews. Portal-sorted entity/MD3 prepasses remain explicit gaps.
- [x] Correct source temporary box-plane handling exposed by a newly visible second human client. Independent review and actual CPU/GL CTF capture/intermission probes pass; production prediction is unchanged.
- [x] Acquire actual pure-filtered retail module identities at UI/cgame creation and use their supported replacement product for construction. Combined CPU two-client CTF passes real pure validation. QVM/native modes and stock-peer pure acceptance remain open.
- [x] Implement and independently review projected world/inline dynamic-light passes through the shared frontend. Root source and actual CPU/GL world runs passed; the separately reproduced native first-sky depth problem remains open.
- [x] Preserve source nonthrowing UDP syscall-send errors exposed by peer shutdown. Independent real-socket review passed; source diagnostics remain visible and receive behavior is unchanged.
- [x] Integrate stencil shadow volumes and finish passes on CPU and GL after independent backend/frontend/caller review. Root verified all 93 postimages, the 111-case joined native run, final portal/marker corrections, typing and policy. Actual both-product clients render nonempty volumes, move/fire, pass pure validation and recover after `vid_restart`. The separate first-sky depth issue remains open.
- [x] Port text0 through text7 deformations using actual retained tess attributes and refdef text, then join the world and picture callers. Independent review is clear; root joined retail CPU/GL checks pass, followed by a live native rerun. This postdates the 08:57 executable.
- [x] Integrate reviewed video-mode selection and fullscreen, including custom-cvar precedence and real window/aspect/UI restart behavior.
- [x] Integrate source image upload/mip/filter handling, SDL pixel/refresh selection and actual Graphics Apply. Independent source/caller review is clear; actual CPU and GL menu runs change texture size/format/filter, then move/fire and retain filtering across map flush and restart.
- [x] Integrate reached source-empty flare dispatch, startup shader registration and three cvars. The later source-completion wave also implements all six visibility/fade/pool helpers and the ordered depth-read/draw path; the original disabled producer remains disabled.
- [x] Preserve source silent-audio gating before RoQ PCM decode/body validation. Four reviewed files are integrated and the live focused run passes 56 cases.
- [x] Connect model LOD and projection to live source cvars. Independent review confirms prepared portal-child values and source-clamped infinite LOD scale; root's combined CPU/GL run passes 53 cases.
- [x] Replace integer JPEG decoding with the source-enabled floating-point path after independent review. Root's 17 selected cases pass, with exact pinned-source comparisons; binary32/LP64 and existing decoder extensions remain qualified.
- [x] Implement path, dir, fdir and touchFile through actual common filesystem owners after independent review. Root's live four-suite selection passes 48 cases with one deliberate retail skip.
- [x] Add source home/current-game file existence and loaded-world basename to their real owners. These supply screenshot numbering and levelshot naming without private filesystem or world state.
- [x] Integrate independently reviewed TGA screenshots, levelshots and cl_avidemo through the actual renderer queue and common files. Root's corrected live screenshot/command selection passes 23 cases on CPU/GL.
- [x] Implement and independently review the source-configured JPEG encoder. Root's live six-case run includes 65 exact source streams; other encoder modes remain outside this unit.
- [x] Connect screenshotJPEG and confirm both screenshot review fixes for AVI cvar rereading and failed-open diagnostics. Separate consumer and lifecycle reviews are clear; live typing and policy pass.
- [x] Integrate the independently reviewed MD4 skeletal model reader. Root's checksum/model run passes 42 cases; checksum code and tests remain unchanged.
- [x] Integrate the reviewed MD4 allocation/animation resource and retained scene-entity owner. Root's live selections pass 52 and 21 cases, followed by joined typing and policy. Actual model rendering remains downstream.
- [x] Integrate reviewed MD4 registration, bounds/tags, skeletal scene submission, actual retained entity/tess state and stencil adjacency. Root verifies 44 further postimages, actual base CPU/Team Arena GL move/fire/restart, typing/policy and the combined live selection. Missing native allocation/pose profiles and the inherited portal scheduling gap remain explicit; this does not close M4 or M7.
- [x] Correct synchronous portal frontend timing and scene completion before queued backend execution; preserve actual immediate effects without draining pending commands. Independent source/merge reviews are clear. Root's combined 51 focused and six retail CPU/GL cases pass, followed by actual Team Arena GL movement/fire/stencil/pure/restart. All 13 exact postimages are integrated; the 15:06 executable predates this correction.
- [x] Integrate reviewed channel receive/drop diagnostics through live client/server cvars and matched address owners. Actual both-product loopback/UDP checks and the base CPU client trace pass; malformed-message qualifications remain separate.
- [x] Integrate the reviewed native GL error check at RE_BeginFrame. Root's joined checks and actual Team Arena GL move/fire/restart run pass with checking enabled. The separate Upload32 error-consumption call remains open.
- [x] Integrate reviewed r_nobind through actual CPU/GL image binding/storage owners. Joined typing/policy, focused/native checks and actual client toggles pass. CPU stencil plus nonuniform override remains qualified; the volume rejects conservatively and the later color-writing finish needs source-indeterminate coordinates.
- [x] Integrate the actual Upload32 GL_CheckErrors call with unconditional one-error consumption and live checking flag after independent review and real client runs.
- [x] Integrate game-owned single-player postgame information and victory-podium entities and retire all unavailable service placeholders. Independent source/caller reviews, root's 289-case join and actual normal-speed base CPU postgame/podium/celebration run pass. Bot-driven progression remains separate.
- [x] Integrate source s_show and s_testsound through actual registered PCM identities, retained mixer channels and the SDL output owner. Both-product clients verify actual queued tone samples, ordinary playback and clean restart/closure.
- [x] Integrate developer Cvar_Get/Cvar_Set2 traces and reset command ordering. Correct artificial final server/download disposal writes exposed by real developer-enabled clients; normal source shutdown writes remain.
- [x] Integrate actual SDL windowed fallback without rewriting requested fullscreen/mode cvars. Both products retain truthful window/configuration state across video restart on an isolated display with no fitting mode.
- [x] Integrate ordered sky state and actual world/inline/model/2D consumers on CPU and GL, including empty material-stage execution and live r_showsky. Correct the viewer triangle-count caller. Full renderer parity remains open.
- [x] Integrate common-owned event/config journaling through actual filesystem and event owners after independent review. Record/replay and source handle lifetimes pass; the remaining FS_ReadFile asset callers are a separate dependency below.
- [x] Migrate actual server-map, demo-numbering, sound, shader/image/BSP, font, module and model/skin FS_ReadFile callers. Independent source/caller reviews are clear. Root's 193-case join and actual base CPU/Team Arena GL sound/move/fire/pure/restart runs pass. Generic FOpenFile consumers remain unchanged.
- [x] Fix ordinary retail intro EOF playback, unknown-chunk looping and new-movie scratch lifetime after independent review. The final combined source reaches Main with no input or movie skips and sound enabled on base CPU and Team Arena GL.
- [x] Implement and independently review PCX/BMP decoding. Root's joined 30 cases pass. Preserve source palette/RLE/row/header quirks; BMP16 undefined output reads and other unsafe-memory paths remain explicitly rejected. Production renderer loading is separate below.
- [x] Connect PCX/BMP to actual renderer image loading and preserve source extension dispatch/retry order and diagnostics. The 62-file join has independent source review, root typing/policy and 107 focused passes. Both actual clients pass movement/fire, pure admission, queued sound and video restart. Eleven broader renderer failures reproduce on the unchanged baseline and remain open.
- [x] Preserve the invalid cached g_gametype transient through source startup and the first ordinary cvar update. The reviewed game consumers compare the actual cached integer without an early refresh or cast. Root's combined game/CPU selection passes 156 cases, with 18 optional GL skips. Actual base CPU startup at -1 and Team Arena GL startup at 8 correct the engine cvar, reach active play, move/fire, pass pure admission and restart video.
- [x] Correct the recorded immediate Main-to-map missing-UI failure. The two-file server-owner repair is independently reviewed and integrated; author/reviewer actual CPU demo recovery succeeds without an idle frame or cvar override, and root's live five-case selection passes. This is an explicit source-bug repair; broader recovery remains open.
- [x] Integrate the renderer-lived font owner shared by UI and cgame, preserving reset on renderer replacement including retained-window map flush. Independent review and 25 actual CPU/GL font cases pass; 92 affected UI/HUD caller cases pass. Existing parser and render-thread barrier gaps remain separate.
- [x] Complete portal-sorted entity/model prepasses using actual retained backend state. Independent backend/frontend review is clear; root joined CPU/GL run passes 117 cases, including retail portal views. Source-indeterminate retained attributes remain qualified.
- [x] Run the source sound-frame update before audible pre-INFO stereo decoding, through the actual EngineSound owner. Independent review and joined retail/client checks pass; all callers use the explicit engine or diagnostic sound profile.
- [x] Drive the actual compiled TypeScript client through original native pure admission and authoritative movement for both products. Native logs prove `cp` -> active -> ClientBegin; normal demos contain 76/77 active snapshots. Reciprocal native-client acceptance remains blocked by native JIT/cgame crashes.
- [x] Implement and independently review actual client MOTD request/response. Root joined checks and an actual ClientHost/SDL/localhost UDP probe verify normal `cl_motd 1` requests, rejection and reply publication. Public service availability is untested.
- [x] Fix the source integer timelimit read after an actual CPU match exposed premature fractional-limit intermission. Verify q3dm1 timed intermission, player ready input and q3dm2 entry with a fresh match clock on CPU and OpenGL. Broader match acceptance remains open.
- [x] Replace cgame's mandatory output-device dependency with actual engine sound services. Raw q3dm1 runs passed with disabled, enabled and unavailable SDL output. This change postdates the current client executable.
- [x] Complete normal menu-to-match play, combat, intermission, restart and reconnect for both products and both renderers under the declared profiles. Later M18 match evidence and M19's complete raw/compiled gameplay/recovery matrix supersede this checkpoint's open items. Current release refreshes pass all four compiled cases. See RELEASE.md and STATUS.md for matches, virtual-display input and retained qualifications; no claim of physical-device verification.
- [x] Drive actual two-human FFA machinegun kills/respawn and Team Arena One Flag/Harvester delivery through pure clients on CPU and GL. Scores, intermission and remote presentation pass; source-assisted positioning and suicide-generated Harvester cubes remain explicit qualifications. No gameplay defect was exposed.
- [x] Implement canonical BSP entities, AAS/BotLibrary lifetime, per-game source AI and server/client attachment. Actual raw and compiled bots move, fight and respawn in both products; CPU restart and Team Arena GL map replacement retain bots and the human client.
- [x] Implement and independently review AAS generation, clustering, optimization, writing and actual writable seek; exercise generation/write/reload and compiled stored-AAS bot play. Regeneration of stripped retail geometry remains qualified.
- [x] Run autonomous bot matches through intermission in FFA, Tournament, TDM, CTF, One Flag, Overload and Harvester on selected retail maps, without forced objectives or combat.
- [x] Complete persisted routing caches and replay the corrected mpteam2 routing path. Compiled dedicated, CPU and GL save/reload pass; mpteam2 One Flag and Harvester finish. Overload remains bounded without a winner.
- [x] Exercise human SDL ready input after bot-earned intermission, then fresh pure admission and retained bots on base CPU and Team Arena GL next maps.
- [x] Implement shared collision debug ownership, actual renderer callbacks and CPU/GL debug polygons. Exercise real traces, mode changes and video restart in both products.
- [x] Implement and independently review live collision curve/area controls and the source hyperspace cull-cache fix.
- [x] Preserve source eye-space portal planes through hyperspace projection changes; independently review CPU/GL state and actual command ordering.
- [x] Correct jump-pad aiming sqrt selection, trigger cooldown publication and shallow-water float stores. Independent source review and existing regressions pass.
- [x] Drive actual record, move/fire, stop, disconnect, replay-to-EOF and subsequent pure map admission on base CPU/GL and Team Arena CPU/GL. All 37 replay snapshots match each live session. The final Team Arena GL run includes the joined EOF source and source-sequence observation. These raw runs retain an ordinary menu idle frame; physical input, bots and full matches remain separate.
- [x] Complete demos, downloads, pure enforcement, mod discovery and the documented rendering/parity/release gates. M9, M10, M18 and M19 acceptance records supersede this earlier open item. COMPATIBILITY.md retains the declared runtime profiles and failed stricter cross-driver diagnostics. CPU performance improvement remains open above.

- [x] Connect source image listing to the actual renderer catalog and client console. Independent review and root combined CPU/GL restart/shutdown checks pass; exact early-startup registration timing remains qualified.
- [x] Complete source shader, model, skin, mode and graphics-information commands through their actual owners, then join shared client registration and shutdown. Separate reviews and root combined checks pass; source startup timing, safe skin storage and primitive-selection qualifications remain explicit.
- [x] Port source primitive selection through the actual source-stage boundary and CPU/GL consumers. All 33 source/test/caller paths are integrated after independent reviews. Actual base CPU and Team Arena GL clients each present 121 frames, change modes, move/fire, pass pure validation, check queued sound and restart video. Nonempty paired discrete mode remains an explicit undefined-source rejection. The measurement-count review correction passes; the separate framebuffer-alpha failure reproduces before this feature. The 22:49 executable passes both compiled products with mode changes, restart, captures, movement/fire, pure admission and 19 valid recorded snapshots.
- [x] Port source debug-sort cutoff and triangle/normal drawing through the existing source-stage queue and both backends. Independent reviews and corrections are complete. Root's combined actual-GL/server selection passes 314 cases; all four raw products/backends exercise actual controls and video restart. Retained depth, material effects after empty text deformation and portal-clipped outlines are covered. Invalid consumed GL numeric data still rejects before geometry submission.
- [x] Implement server kick/clientkick through actual command registration and lifecycle owners. A passed vote reaches authoritative player removal and reliable disconnect delivery over localhost UDP. Source name/group/slot selection, host protection and zombie timestamp refresh pass independent review. Bot-backed allbots removal remains unproven.
- [x] Implement serverinfo, systeminfo, dumpuser and dedicated-console say using existing cvar, shared Info_Print and reliable-command owners. Independent review, root combined/raw checks and actual compiled graphical/dedicated runs pass for both products. Remote userinfo and source registration lifetime are preserved.
- [x] Implement actual stable server sector membership and sectorlist through source clear/restart boundaries. The later source MAXCOUNT callback and all callers are now also integrated and independently reviewed. Root combined and actual raw/compiled product/backend checks pass; no count-cache substitute was used.
- [x] Port r_showImages through actual image and frame-end owners, including retained color, requested upload dimensions and unconditional registration completion. Independent review, root combined checks and both raw/compiled product/backend runs pass; 01:00 executable accepted.
- [x] Preserve QVM float32 subtraction before splash falloff multiplication. Both-product grenade-profile regression proves 53 damage instead of 54; source and actual-client combined checks pass.
- [x] Implement developer error command severity/registration and ordered developer WAV warnings through actual common output. Independent review and root combined checks pass; 01:21 sound-enabled executable accepted.
- [x] Integrate signed-demo sequence publication through session, history, cgame initialization and retained engine player state. Independent review, root focused checks, actual snapshot-zero/expired-history input and console behavior, and both raw product/backend runs pass. Included in the 01:50 executable.
- [x] Integrate source demo header publication before EOF, length errors and truncated payload handling through the actual reader/session owner. Independent review, root combined checks and final actual Team Arena GL EOF/re-entry pass. Included in the matching, checked 01:50 executable.

The old checkpoint-twenty-three helper cycle is superseded by the functional filesystem batch. No new checkpoint helper was needed. Team Orders and source-disabled Save Config are included in the batch's full check. Frozen older evidence and executable backups remain available.

Verification supports these implementation units. Do not start another harness, exhaustive audit matrix or repeated whole-tree check at each leaf. Run focused checks for changed behavior and exercise the combined application as its dependencies become available.

The earlier incremental-inflater-first design constraint is superseded by root's qualified retained-reader decision. Open and read use actual shared resources; first nonzero packed read still allocates and verifies the whole entry. Incremental memory/error timing and directory-enumeration qualifications remain open parity work. There is no UI-private filesystem.

Graphics/Display now have the real renderer-configuration predecessor. The accepted gamma lease is exclusive, single-display and focus-qualified; installed SDL actually rejects gamma setting. It does not claim positive physical gamma or move/replug continuity. The earlier rejected candidate remains private. Remove Bots now receives the graphical application's actual session, but game bot AI remains unavailable. Player Model borrows the UI/cgame arena; shared common/server/renderer and JavaScript memory accounting remain qualified. Do not fabricate capability flags or ready owners.

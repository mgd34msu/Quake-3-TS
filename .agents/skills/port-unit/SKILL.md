# Port one Quake subsystem

Read the root PORT_PLAN.md and the exact upstream implementation for your assignment. Upstream is `/home/buzzkill/Projects/qsrc/quake-iii-arena` at the commit recorded in docs/GROUNDING.md. Work only in your assigned files. Other agents own adjacent modules. Ask the lead about contract changes, and continue independent work while it resolves them. Do not spawn children without the lead assigning capacity.

Unless the lead explicitly assigns live edits, create a private stage from the project root with `bun tools/create-port-stage.ts`. Its optional argument is an existing parent directory outside the checkout. Edit only assigned paths in the returned `candidate`. Preserve `baseline` and `CAPTURE.json`. Put extra review helpers and logs outside both copies. Use this canonical command, not a hand-maintained root list or dependency symlink back to live files. The command verifies both copies and the source before returning. Its manifest includes canonical files, modes and permitted dependency links, not empty directories or an atomic filesystem freeze. The lead still excludes concurrent live writers. Report exact owned changes and unchanged borrowed inputs before integration. A failed stage is preserved at the path in the error; create a fresh stage for retries without rewriting frozen evidence.

All project code is TypeScript. Use strict types, `.ts` imports, type-only imports, checked indexed access and exact optional properties. No explicit or unsafe inferred `any`, type assertions including `as const`, non-null/definite-assignment assertions, diagnostic suppressions, ambient declaration shims, native code, JS implementation, eval, or WASM. Use constructors, concrete interfaces, narrowing and `satisfies`. Unknown external data must be parsed. Do not weaken compiler settings or the policy checker. Do not use `?? 0` or defaults to conceal invalid indexes or missing required input.

Translate original semantics, including 32-bit wrap, float32 storage boundaries, packet field order, product variants, event timing and edge cases. Use per-instance state in place of C globals. Keep code directly traceable to upstream files/functions. New CPU renderer algorithms need analytic fixtures and reference images. A simplified stand-in is not a port. Unsupported cases must be explicit and reported, not silently rendered/parsed as a convenient alternative.

Before editing, state the input/output contract in your report to the lead. Add only implementation needed by your assignment. Keep exports small. No production stubs, no speculative interfaces with unimplemented methods, and no optional fields that encode inconsistent states. Pure algorithms should not import platform code.

Use apply_patch for edits. Verify the actual behavior with Bun tests and inspect your diff. Binary readers need valid, truncated, malformed and boundary cases. Source algorithms need independent expected values or source-reference fixtures. Use installed retail data where appropriate, keep paths configurable, and never copy it into the repository. Test successful behavior and rejection, not implementation internals alone. Report exact commands, results, and remaining gaps honestly.

Do not edit shared configuration, root docs, source ledger, or another worker's files unless the lead assigns them. Do not commit. Preserve all existing user input archives.

Handoff: changed paths, exported contracts, source references, test commands/results, integration requirements, and explicit unimplemented behavior. Implemented and verified are separate statuses; the lead decides milestone completion using end-to-end evidence.
